check_segments = function (route = "", errors = []) {
	// Segment restrictions in 5D nav
	if (route.includes("ACK SIE") { errors.push("'ACK-SIE' is an invalid segment."); }
	if (route.includes("ACK WHALE") { errors.push("'ACK-WHALE' is an invalid segment."); }
	if (route.includes("ACK SEY") { errors.push("'ACK-SEY' is an invalid segment."); }
	if (route.includes("ACK ALLEX") { errors.push("'ACK-ALLEX' is an invalid segment."); }
	if (route.includes("ACK DOVEY") { errors.push("'ACK-DOVEY' is an invalid segment."); }
	if (route.includes("ACOMA ABQ") { errors.push("'ACOMA-ABQ' is an invalid segment."); }
	if (route.includes("ACORD EGRET") { errors.push("'ACORD-EGRET' is an invalid segment."); }
	if (route.includes("ADORO DETOX") { errors.push("'ADORO-DETOX' is an invalid segment."); }
	if (route.includes("ADORO BANAL") { errors.push("'ADORO-BANAL' is an invalid segment."); }
	if (route.includes("ALB TOPPS") { errors.push("'ALB-TOPPS' is an invalid segment."); }
	if (route.includes("ALB MIILS") { errors.push("'ALB-MIILS' is an invalid segment."); }
	if (route.includes("ALB KJOHN") { errors.push("'ALB-KJOHN' is an invalid segment."); }
	if (route.includes("ALKEM CUU") { errors.push("'ALKEM-CUU' is an invalid segment."); }
	if (route.includes("ALLEX IRLOK") { errors.push("'ALLEX-IRLOK' is an invalid segment."); }
	if (route.includes("ALLEX CAM") { errors.push("'ALLEX-CAM' is an invalid segment."); }
	if (route.includes("ALLEX BOS") { errors.push("'ALLEX-BOS' is an invalid segment."); }
	if (route.includes("ALLEX ACK") { errors.push("'ALLEX-ACK' is an invalid segment."); }
	if (route.includes("ALLEX BGR") { errors.push("'ALLEX-BGR' is an invalid segment."); }
	if (route.includes("AAR OVANO") { errors.push("'AAR-OVANO' is an invalid segment."); }
	if (route.includes("ABRAG MALBE") { errors.push("'ABRAG-MALBE' is an invalid segment."); }
	if (route.includes("ADIME BENJO") { errors.push("'ADIME-BENJO' is an invalid segment."); }
	if (route.includes("ADORO ZMR") { errors.push("'ADORO-ZMR' is an invalid segment."); }
	if (route.includes("ADSAM AYROU") { errors.push("'ADSAM-AYROU' is an invalid segment."); }
	if (route.includes("ADSAM YFB") { errors.push("'ADSAM-YFB' is an invalid segment."); }
	if (route.includes("ADSAM YTE") { errors.push("'ADSAM-YTE' is an invalid segment."); }
	if (route.includes("ADSAM YFB") { errors.push("'ADSAM-YFB' is an invalid segment."); }
	if (route.includes("ADSAM TEFFO") { errors.push("'ADSAM-TEFFO' is an invalid segment."); }
	if (route.includes("ADSAM BEPEG") { errors.push("'ADSAM-BEPEG' is an invalid segment."); }
	if (route.includes("ADSAM YXP") { errors.push("'ADSAM-YXP' is an invalid segment."); }
	if (route.includes("ADSAM ALSAK") { errors.push("'ADSAM-ALSAK' is an invalid segment."); }
	if (route.includes("ADSAM BERUS") { errors.push("'ADSAM-BERUS' is an invalid segment."); }
	if (route.includes("ADSAM IKMAN") { errors.push("'ADSAM-IKMAN' is an invalid segment."); }
	if (route.includes("ADSAM YFB") { errors.push("'ADSAM-YFB' is an invalid segment."); }
	if (route.includes("AERRO LHS") { errors.push("'AERRO-LHS' is an invalid segment."); }
	if (route.includes("AGARD SPEAK") { errors.push("'AGARD-SPEAK' is an invalid segment."); }
	if (route.includes("AGDUT TVC") { errors.push("'AGDUT-TVC' is an invalid segment."); }
	if (route.includes("AGDUT LETAK") { errors.push("'AGDUT-LETAK' is an invalid segment."); }
	if (route.includes("AJJAY OOSHN") { errors.push("'AJJAY-OOSHN' is an invalid segment."); }
	if (route.includes("AKPEK ISPAS") { errors.push("'AKPEK-ISPAS' is an invalid segment."); }
	if (route.includes("ALG RUTOM") { errors.push("'ALG-RUTOM' is an invalid segment."); }
	if (route.includes("ALKEM KIPAL") { errors.push("'ALKEM-KIPAL' is an invalid segment."); }
	if (route.includes("ALLEX JAROM") { errors.push("'ALLEX-JAROM' is an invalid segment."); }
	if (route.includes("ALLEX BEEKN") { errors.push("'ALLEX-BEEKN' is an invalid segment."); }
	if (route.includes("ALLEX KAYCC") { errors.push("'ALLEX-KAYCC' is an invalid segment."); }
	if (route.includes("ALLRY MIGLI") { errors.push("'ALLRY-MIGLI' is an invalid segment."); }
	if (route.includes("ALSOP JOVIE") { errors.push("'ALSOP-JOVIE' is an invalid segment."); }
	if (route.includes("AMEKE IWANY") { errors.push("'AMEKE-IWANY' is an invalid segment."); }
	if (route.includes("AMEKE IWANY") { errors.push("'AMEKE-IWANY' is an invalid segment."); }
	if (route.includes("ANCER BAREE") { errors.push("'ANCER-BAREE' is an invalid segment."); }
	if (route.includes("ANGLL CANIT") { errors.push("'ANGLL-CANIT' is an invalid segment."); }
	if (route.includes("ANN YYD") { errors.push("'ANN-YYD' is an invalid segment."); }
	if (route.includes("ANNVA FLO") { errors.push("'ANNVA-FLO' is an invalid segment."); }
	if (route.includes("ANTIG PIREX") { errors.push("'ANTIG-PIREX' is an invalid segment."); }
	if (route.includes("ANTIG NUMBR") { errors.push("'ANTIG-NUMBR' is an invalid segment."); }
	if (route.includes("ANTIG ANVER") { errors.push("'ANTIG-ANVER' is an invalid segment."); }
	if (route.includes("ANTIG WINGZ") { errors.push("'ANTIG-WINGZ' is an invalid segment."); }
	if (route.includes("ANTIG BALOO") { errors.push("'ANTIG-BALOO' is an invalid segment."); }
	if (route.includes("ANTIG SEAVR") { errors.push("'ANTIG-SEAVR' is an invalid segment."); }
	if (route.includes("ANTIG BOVIC") { errors.push("'ANTIG-BOVIC' is an invalid segment."); }
	if (route.includes("ANTIG LAZEY") { errors.push("'ANTIG-LAZEY' is an invalid segment."); }
	if (route.includes("ANU ELOPO") { errors.push("'ANU-ELOPO' is an invalid segment."); }
	if (route.includes("ANU SKB") { errors.push("'ANU-SKB' is an invalid segment."); }
	if (route.includes("ANVER SHEIL") { errors.push("'ANVER-SHEIL' is an invalid segment."); }
	if (route.includes("ANVER JIMAC") { errors.push("'ANVER-JIMAC' is an invalid segment."); }
	if (route.includes("ANVER WINGZ") { errors.push("'ANVER-WINGZ' is an invalid segment."); }
	if (route.includes("ANVER NUMBR") { errors.push("'ANVER-NUMBR' is an invalid segment."); }
	if (route.includes("ANVER PIREX") { errors.push("'ANVER-PIREX' is an invalid segment."); }
	if (route.includes("ANVER BALTN") { errors.push("'ANVER-BALTN' is an invalid segment."); }
	if (route.includes("ANVER GECAL") { errors.push("'ANVER-GECAL' is an invalid segment."); }
	if (route.includes("ANVER ANTIG") { errors.push("'ANVER-ANTIG' is an invalid segment."); }
	if (route.includes("ANVER WINGZ") { errors.push("'ANVER-WINGZ' is an invalid segment."); }
	if (route.includes("ANVER NUMBR") { errors.push("'ANVER-NUMBR' is an invalid segment."); }
	if (route.includes("ALRIN CUL") { errors.push("'ALRIN-CUL' is an invalid segment."); }
	if (route.includes("ALRIN ILAVI") { errors.push("'ALRIN-ILAVI' is an invalid segment."); }
	if (route.includes("ALSAK MCKEE") { errors.push("'ALSAK-MCKEE' is an invalid segment."); }
	if (route.includes("ALSAK FEDDY") { errors.push("'ALSAK-FEDDY' is an invalid segment."); }
	if (route.includes("ALSAK IKMAN") { errors.push("'ALSAK-IKMAN' is an invalid segment."); }
	if (route.includes("ALSAK BERUS") { errors.push("'ALSAK-BERUS' is an invalid segment."); }
	if (route.includes("ALSAK SEMTO") { errors.push("'ALSAK-SEMTO' is an invalid segment."); }
	if (route.includes("ALSAK BODRA") { errors.push("'ALSAK-BODRA' is an invalid segment."); }
	if (route.includes("ALSAK DUGNO") { errors.push("'ALSAK-DUGNO' is an invalid segment."); }
	if (route.includes("ALSAK DARUB") { errors.push("'ALSAK-DARUB' is an invalid segment."); }
	if (route.includes("ALSAK EPMAN") { errors.push("'ALSAK-EPMAN' is an invalid segment."); }
	if (route.includes("ALSAK HINGE") { errors.push("'ALSAK-HINGE' is an invalid segment."); }
	if (route.includes("ALSAK COPUR") { errors.push("'ALSAK-COPUR' is an invalid segment."); }
	if (route.includes("ALSAK TUNNI") { errors.push("'ALSAK-TUNNI' is an invalid segment."); }
	if (route.includes("ALSAK KELMU") { errors.push("'ALSAK-KELMU' is an invalid segment."); }
	if (route.includes("ALSAK KENKI") { errors.push("'ALSAK-KENKI' is an invalid segment."); }
	if (route.includes("ALSAK GRIBS") { errors.push("'ALSAK-GRIBS' is an invalid segment."); }
	if (route.includes("ALSAK CANEL") { errors.push("'ALSAK-CANEL' is an invalid segment."); }
	if (route.includes("ALSAK JELCO") { errors.push("'ALSAK-JELCO' is an invalid segment."); }
	if (route.includes("ALSAK AYROU") { errors.push("'ALSAK-AYROU' is an invalid segment."); }
	if (route.includes("ALSAK YXP") { errors.push("'ALSAK-YXP' is an invalid segment."); }
	if (route.includes("ALSAK RODBO") { errors.push("'ALSAK-RODBO' is an invalid segment."); }
	if (route.includes("ALSAK LAKES") { errors.push("'ALSAK-LAKES' is an invalid segment."); }
	if (route.includes("ALSAK LOMTA") { errors.push("'ALSAK-LOMTA' is an invalid segment."); }
	if (route.includes("ALSAK NALDI") { errors.push("'ALSAK-NALDI' is an invalid segment."); }
	if (route.includes("ALSAK PEPKI") { errors.push("'ALSAK-PEPKI' is an invalid segment."); }
	if (route.includes("ALSAK NADMA") { errors.push("'ALSAK-NADMA' is an invalid segment."); }
	if (route.includes("ALSAK YTE") { errors.push("'ALSAK-YTE' is an invalid segment."); }
	if (route.includes("ALSAK FASSA") { errors.push("'ALSAK-FASSA' is an invalid segment."); }
	if (route.includes("ALSAK LOPVI") { errors.push("'ALSAK-LOPVI' is an invalid segment."); }
	if (route.includes("ALSAK MUSVA") { errors.push("'ALSAK-MUSVA' is an invalid segment."); }
	if (route.includes("ALSAK DAXER") { errors.push("'ALSAK-DAXER' is an invalid segment."); }
	if (route.includes("ALSAK MIBNO") { errors.push("'ALSAK-MIBNO' is an invalid segment."); }
	if (route.includes("ALSAK YZS") { errors.push("'ALSAK-YZS' is an invalid segment."); }
	if (route.includes("ALYRA CARPX") { errors.push("'ALYRA-CARPX' is an invalid segment."); }
	if (route.includes("ALYRA PELCN") { errors.push("'ALYRA-PELCN' is an invalid segment."); }
	if (route.includes("ALYRA GRUBR") { errors.push("'ALYRA-GRUBR' is an invalid segment."); }
	if (route.includes("ALYRA ROWSY") { errors.push("'ALYRA-ROWSY' is an invalid segment."); }
	if (route.includes("ALYRA TROUT") { errors.push("'ALYRA-TROUT' is an invalid segment."); }
	if (route.includes("AMAKR SHENU") { errors.push("'AMAKR-SHENU' is an invalid segment."); }
	if (route.includes("AMBIN KINOP") { errors.push("'AMBIN-KINOP' is an invalid segment."); }
	if (route.includes("AMUKI OTEXA") { errors.push("'AMUKI-OTEXA' is an invalid segment."); }
	if (route.includes("AMUKI CUU") { errors.push("'AMUKI-CUU' is an invalid segment."); }
	if (route.includes("AMUKI OTUPA") { errors.push("'AMUKI-OTUPA' is an invalid segment."); }
	if (route.includes("AMUKI MIDRA") { errors.push("'AMUKI-MIDRA' is an invalid segment."); }
	if (route.includes("ANADA GND") { errors.push("'ANADA-GND' is an invalid segment."); }
	if (route.includes("ANADA POS") { errors.push("'ANADA-POS' is an invalid segment."); }
	if (route.includes("ANATI OTONA") { errors.push("'ANATI-OTONA' is an invalid segment."); }
	if (route.includes("ANATI SSM") { errors.push("'ANATI-SSM' is an invalid segment."); }
	if (route.includes("ANATI LETAK") { errors.push("'ANATI-LETAK' is an invalid segment."); }
	if (route.includes("ANATI KAPUX") { errors.push("'ANATI-KAPUX' is an invalid segment."); }
	if (route.includes("ANATI POLTY") { errors.push("'ANATI-POLTY' is an invalid segment."); }
	if (route.includes("ANCER LETAK") { errors.push("'ANCER-LETAK' is an invalid segment."); }
	if (route.includes("ANCER POLTY") { errors.push("'ANCER-POLTY' is an invalid segment."); }
	if (route.includes("ANLUP MAKBU") { errors.push("'ANLUP-MAKBU' is an invalid segment."); }
	if (route.includes("ANTIG GALVN") { errors.push("'ANTIG-GALVN' is an invalid segment."); }
	if (route.includes("ANTOV KJOHN") { errors.push("'ANTOV-KJOHN' is an invalid segment."); }
	if (route.includes("ANVER LAZEY") { errors.push("'ANVER-LAZEY' is an invalid segment."); }
	if (route.includes("ANVER SEAVR") { errors.push("'ANVER-SEAVR' is an invalid segment."); }
	if (route.includes("AVPUT IKMAN") { errors.push("'AVPUT-IKMAN' is an invalid segment."); }
	if (route.includes("AVUTI ALSOP") { errors.push("'AVUTI-ALSOP' is an invalid segment."); }
	if (route.includes("AVUTI BAREE") { errors.push("'AVUTI-BAREE' is an invalid segment."); }
	if (route.includes("AZEZU RESQU") { errors.push("'AZEZU-RESQU' is an invalid segment."); }
	if (route.includes("BALDI PSP") { errors.push("'BALDI-PSP' is an invalid segment."); }
	if (route.includes("BALOO SOORY") { errors.push("'BALOO-SOORY' is an invalid segment."); }
	if (route.includes("BALOO ANTIG") { errors.push("'BALOO-ANTIG' is an invalid segment."); }
	if (route.includes("BALOO SELIM") { errors.push("'BALOO-SELIM' is an invalid segment."); }
	if (route.includes("BALOO JIMAC") { errors.push("'BALOO-JIMAC' is an invalid segment."); }
	if (route.includes("BALOO NUMBR") { errors.push("'BALOO-NUMBR' is an invalid segment."); }
	if (route.includes("BALOO BOVIC") { errors.push("'BALOO-BOVIC' is an invalid segment."); }
	if (route.includes("BALOO SEAVR") { errors.push("'BALOO-SEAVR' is an invalid segment."); }
	if (route.includes("BALOO DASER") { errors.push("'BALOO-DASER' is an invalid segment."); }
	if (route.includes("BALOO PIREX") { errors.push("'BALOO-PIREX' is an invalid segment."); }
	if (route.includes("BALOO SHEIL") { errors.push("'BALOO-SHEIL' is an invalid segment."); }
	if (route.includes("BALOO WINGZ") { errors.push("'BALOO-WINGZ' is an invalid segment."); }
	if (route.includes("BALOO ENAPI") { errors.push("'BALOO-ENAPI' is an invalid segment."); }
	if (route.includes("BALOO SELIM") { errors.push("'BALOO-SELIM' is an invalid segment."); }
	if (route.includes("BALOO GECAL") { errors.push("'BALOO-GECAL' is an invalid segment."); }
	if (route.includes("BALOO BALTN") { errors.push("'BALOO-BALTN' is an invalid segment."); }
	if (route.includes("BALTN NUMBR") { errors.push("'BALTN-NUMBR' is an invalid segment."); }
	if (route.includes("BALTN BOVIC") { errors.push("'BALTN-BOVIC' is an invalid segment."); }
	if (route.includes("BALTN SEAVR") { errors.push("'BALTN-SEAVR' is an invalid segment."); }
	if (route.includes("BALTN DASER") { errors.push("'BALTN-DASER' is an invalid segment."); }
	if (route.includes("BALTN PIREX") { errors.push("'BALTN-PIREX' is an invalid segment."); }
	if (route.includes("BALTN ANVER") { errors.push("'BALTN-ANVER' is an invalid segment."); }
	if (route.includes("BALTN LAZEY") { errors.push("'BALTN-LAZEY' is an invalid segment."); }
	if (route.includes("BALTN WINGZ") { errors.push("'BALTN-WINGZ' is an invalid segment."); }
	if (route.includes("BALTN BALOO") { errors.push("'BALTN-BALOO' is an invalid segment."); }
	if (route.includes("BARDI DETOX") { errors.push("'BARDI-DETOX' is an invalid segment."); }
	if (route.includes("BARDI ERPES") { errors.push("'BARDI-ERPES' is an invalid segment."); }
	if (route.includes("BAREE MELDI") { errors.push("'BAREE-MELDI' is an invalid segment."); }
	if (route.includes("BAREE AVUTI") { errors.push("'BAREE-AVUTI' is an invalid segment."); }
	if (route.includes("BAREE RIKAL") { errors.push("'BAREE-RIKAL' is an invalid segment."); }
	if (route.includes("APACK HNL") { errors.push("'APACK-HNL' is an invalid segment."); }
	if (route.includes("APACK ZOULU") { errors.push("'APACK-ZOULU' is an invalid segment."); }
	if (route.includes("APE CXR") { errors.push("'APE-CXR' is an invalid segment."); }
	if (route.includes("AQABA BQN") { errors.push("'AQABA-BQN' is an invalid segment."); }
	if (route.includes("ARBOR FITES") { errors.push("'ARBOR-FITES' is an invalid segment."); }
	if (route.includes("ARNAL OSUPA") { errors.push("'ARNAL-OSUPA' is an invalid segment."); }
	if (route.includes("ARROW MAGGI") { errors.push("'ARROW-MAGGI' is an invalid segment."); }
	if (route.includes("ASIVO GABIM") { errors.push("'ASIVO-GABIM' is an invalid segment."); }
	if (route.includes("ATENE LETAK") { errors.push("'ATENE-LETAK' is an invalid segment."); }
	if (route.includes("AUDIA SNAXX") { errors.push("'AUDIA-SNAXX' is an invalid segment."); }
	if (route.includes("AZEZU SQUAD") { errors.push("'AZEZU-SQUAD' is an invalid segment."); }
	if (route.includes("AZEZU OKONU") { errors.push("'AZEZU-OKONU' is an invalid segment."); }
	if (route.includes("AZEZU SILLY") { errors.push("'AZEZU-SILLY' is an invalid segment."); }
	if (route.includes("AZO BOHRR") { errors.push("'AZO-BOHRR' is an invalid segment."); }
	if (route.includes("BAAGR GRATX") { errors.push("'BAAGR-GRATX' is an invalid segment."); }
	if (route.includes("BAAGR CNNOR") { errors.push("'BAAGR-CNNOR' is an invalid segment."); }
	if (route.includes("BADDI HAR") { errors.push("'BADDI-HAR' is an invalid segment."); }
	if (route.includes("BAGSO ERNAN") { errors.push("'BAGSO-ERNAN' is an invalid segment."); }
	if (route.includes("BANAL RIVRO") { errors.push("'BANAL-RIVRO' is an invalid segment."); }
	if (route.includes("BANAL ADORO") { errors.push("'BANAL-ADORO' is an invalid segment."); }
	if (route.includes("BARDI BANAL") { errors.push("'BARDI-BANAL' is an invalid segment."); }
	if (route.includes("BAREE REEDO") { errors.push("'BAREE-REEDO' is an invalid segment."); }
	if (route.includes("BAREE JANJO") { errors.push("'BAREE-JANJO' is an invalid segment."); }
	if (route.includes("BAREE IKMOL") { errors.push("'BAREE-IKMOL' is an invalid segment."); }
	if (route.includes("BAREE ANCER") { errors.push("'BAREE-ANCER' is an invalid segment."); }
	if (route.includes("BAREE DORYY") { errors.push("'BAREE-DORYY' is an invalid segment."); }
	if (route.includes("BAREE ENNSO") { errors.push("'BAREE-ENNSO' is an invalid segment."); }
	if (route.includes("BAREE SAXAN") { errors.push("'BAREE-SAXAN' is an invalid segment."); }
	if (route.includes("BAREE PELTU") { errors.push("'BAREE-PELTU' is an invalid segment."); }
	if (route.includes("BAREE IRLOK") { errors.push("'BAREE-IRLOK' is an invalid segment."); }
	if (route.includes("BAREE NEEKO") { errors.push("'BAREE-NEEKO' is an invalid segment."); }
	if (route.includes("BAYYS PUT") { errors.push("'BAYYS-PUT' is an invalid segment."); }
	if (route.includes("BEERD CANIT") { errors.push("'BEERD-CANIT' is an invalid segment."); }
	if (route.includes("BEROS LLANO") { errors.push("'BEROS-LLANO' is an invalid segment."); }
	if (route.includes("BEZOV EGRET") { errors.push("'BEZOV-EGRET' is an invalid segment."); }
	if (route.includes("BGD IPIME") { errors.push("'BGD-IPIME' is an invalid segment."); }
	if (route.includes("BGR BRADD") { errors.push("'BGR-BRADD' is an invalid segment."); }
	if (route.includes("BGR ALLEX") { errors.push("'BGR-ALLEX' is an invalid segment."); }
	if (route.includes("BILEE SBI") { errors.push("'BILEE-SBI' is an invalid segment."); }
	if (route.includes("BNA BEVEE") { errors.push("'BNA-BEVEE' is an invalid segment."); }
	if (route.includes("BODLO FORST") { errors.push("'BODLO-FORST' is an invalid segment."); }
	if (route.includes("BODRA YFB") { errors.push("'BODRA-YFB' is an invalid segment."); }
	if (route.includes("BODRA YYQ") { errors.push("'BODRA-YYQ' is an invalid segment."); }
	if (route.includes("BAREE SSM") { errors.push("'BAREE-SSM' is an invalid segment."); }
	if (route.includes("BAREE POLTY") { errors.push("'BAREE-POLTY' is an invalid segment."); }
	if (route.includes("BASER DINGO") { errors.push("'BASER-DINGO' is an invalid segment."); }
	if (route.includes("BDA DASER") { errors.push("'BDA-DASER' is an invalid segment."); }
	if (route.includes("BDF WELTS") { errors.push("'BDF-WELTS' is an invalid segment."); }
	if (route.includes("BEAUT MALIT") { errors.push("'BEAUT-MALIT' is an invalid segment."); }
	if (route.includes("BEAUT RIFFT") { errors.push("'BEAUT-RIFFT' is an invalid segment."); }
	if (route.includes("BECON DRRVR") { errors.push("'BECON-DRRVR' is an invalid segment."); }
	if (route.includes("BEDRA DIXIS") { errors.push("'BEDRA-DIXIS' is an invalid segment."); }
	if (route.includes("BEGAS LASNO") { errors.push("'BEGAS-LASNO' is an invalid segment."); }
	if (route.includes("BEGAS DESAT") { errors.push("'BEGAS-DESAT' is an invalid segment."); }
	if (route.includes("BEGAS KORAV") { errors.push("'BEGAS-KORAV' is an invalid segment."); }
	if (route.includes("BEPEG BODRA") { errors.push("'BEPEG-BODRA' is an invalid segment."); }
	if (route.includes("BEPEG YDP") { errors.push("'BEPEG-YDP' is an invalid segment."); }
	if (route.includes("BEPEG NALDI") { errors.push("'BEPEG-NALDI' is an invalid segment."); }
	if (route.includes("BEPEG DUGNO") { errors.push("'BEPEG-DUGNO' is an invalid segment."); }
	if (route.includes("BEPEG DARUB") { errors.push("'BEPEG-DARUB' is an invalid segment."); }
	if (route.includes("BEPEG PEPKI") { errors.push("'BEPEG-PEPKI' is an invalid segment."); }
	if (route.includes("BEPEG RODBO") { errors.push("'BEPEG-RODBO' is an invalid segment."); }
	if (route.includes("BEPEG ADSAM") { errors.push("'BEPEG-ADSAM' is an invalid segment."); }
	if (route.includes("BEPEG HELMO") { errors.push("'BEPEG-HELMO' is an invalid segment."); }
	if (route.includes("BEPEG JELCO") { errors.push("'BEPEG-JELCO' is an invalid segment."); }
	if (route.includes("BEPEG KLIPS") { errors.push("'BEPEG-KLIPS' is an invalid segment."); }
	if (route.includes("BEPEG PELSI") { errors.push("'BEPEG-PELSI' is an invalid segment."); }
	if (route.includes("BEPEG COPUR") { errors.push("'BEPEG-COPUR' is an invalid segment."); }
	if (route.includes("BEPEG YVP") { errors.push("'BEPEG-YVP' is an invalid segment."); }
	if (route.includes("BEPEG MUSVA") { errors.push("'BEPEG-MUSVA' is an invalid segment."); }
	if (route.includes("BEPEG KAGLY") { errors.push("'BEPEG-KAGLY' is an invalid segment."); }
	if (route.includes("BEPEG DAXER") { errors.push("'BEPEG-DAXER' is an invalid segment."); }
	if (route.includes("BEPEG GRIBS") { errors.push("'BEPEG-GRIBS' is an invalid segment."); }
	if (route.includes("BEPEG SEMTO") { errors.push("'BEPEG-SEMTO' is an invalid segment."); }
	if (route.includes("BEPEG AYROU") { errors.push("'BEPEG-AYROU' is an invalid segment."); }
	if (route.includes("BEPEG MIBNO") { errors.push("'BEPEG-MIBNO' is an invalid segment."); }
	if (route.includes("BEPEG KEMSA") { errors.push("'BEPEG-KEMSA' is an invalid segment."); }
	if (route.includes("BEPEG YTE") { errors.push("'BEPEG-YTE' is an invalid segment."); }
	if (route.includes("BEPEG LOMTA") { errors.push("'BEPEG-LOMTA' is an invalid segment."); }
	if (route.includes("BEPEG MIBNA") { errors.push("'BEPEG-MIBNA' is an invalid segment."); }
	if (route.includes("BEPEG BOPUT") { errors.push("'BEPEG-BOPUT' is an invalid segment."); }
	if (route.includes("BEPEG KENKI") { errors.push("'BEPEG-KENKI' is an invalid segment."); }
	if (route.includes("BEPEG IKMAN") { errors.push("'BEPEG-IKMAN' is an invalid segment."); }
	if (route.includes("BEPEG EPMAN") { errors.push("'BEPEG-EPMAN' is an invalid segment."); }
	if (route.includes("BEPEG TEALS") { errors.push("'BEPEG-TEALS' is an invalid segment."); }
	if (route.includes("BEPEG LOPVI") { errors.push("'BEPEG-LOPVI' is an invalid segment."); }
	if (route.includes("BEPEG YXP") { errors.push("'BEPEG-YXP' is an invalid segment."); }
	if (route.includes("BEPEG FASSA") { errors.push("'BEPEG-FASSA' is an invalid segment."); }
	if (route.includes("BEPEG TUNNI") { errors.push("'BEPEG-TUNNI' is an invalid segment."); }
	if (route.includes("BEPEG BERUS") { errors.push("'BEPEG-BERUS' is an invalid segment."); }
	if (route.includes("BERUX HIDRA") { errors.push("'BERUX-HIDRA' is an invalid segment."); }
	if (route.includes("BESSI CYN") { errors.push("'BESSI-CYN' is an invalid segment."); }
	if (route.includes("BEXOV DLH") { errors.push("'BEXOV-DLH' is an invalid segment."); }
	if (route.includes("BGI BORUS") { errors.push("'BGI-BORUS' is an invalid segment."); }
	if (route.includes("BGQ RNICH") { errors.push("'BGQ-RNICH' is an invalid segment."); }
	if (route.includes("BGR MIILS") { errors.push("'BGR-MIILS' is an invalid segment."); }
	if (route.includes("BILET RESPE") { errors.push("'BILET-RESPE' is an invalid segment."); }
	if (route.includes("BILET NOTWO") { errors.push("'BILET-NOTWO' is an invalid segment."); }
	if (route.includes("BILET VXV") { errors.push("'BILET-VXV' is an invalid segment."); }
	if (route.includes("BITAC HARBG") { errors.push("'BITAC-HARBG' is an invalid segment."); }
	if (route.includes("BITAC SOLEI") { errors.push("'BITAC-SOLEI' is an invalid segment."); }
	if (route.includes("BITTA NAPUA") { errors.push("'BITTA-NAPUA' is an invalid segment."); }
	if (route.includes("BNEPL FOF") { errors.push("'BNEPL-FOF' is an invalid segment."); }
	if (route.includes("BOBTA LETAK") { errors.push("'BOBTA-LETAK' is an invalid segment."); }
	if (route.includes("BOBTU JAROM") { errors.push("'BOBTU-JAROM' is an invalid segment."); }
	if (route.includes("BOBZY BRAYN") { errors.push("'BOBZY-BRAYN' is an invalid segment."); }
	if (route.includes("BODLO ONGAN") { errors.push("'BODLO-ONGAN' is an invalid segment."); }
		if (route.includes("BOKTO TIGIP") { errors.push("'BOKTO-TIGIP' is an invalid segment."); }
	if (route.includes("BOKTO ALSOP") { errors.push("'BOKTO-ALSOP' is an invalid segment."); }
	if (route.includes("BOS BRADD") { errors.push("'BOS-BRADD' is an invalid segment."); }
	if (route.includes("BOS ALLEX") { errors.push("'BOS-ALLEX' is an invalid segment."); }
	if (route.includes("BOSOX GRAYM") { errors.push("'BOSOX-GRAYM' is an invalid segment."); }
	if (route.includes("BOSOX GRAYM") { errors.push("'BOSOX-GRAYM' is an invalid segment."); }
	if (route.includes("BOSOX GRIPE") { errors.push("'BOSOX-GRIPE' is an invalid segment."); }
	if (route.includes("BOUSY HCM") { errors.push("'BOUSY-HCM' is an invalid segment."); }
	if (route.includes("BOUSY RICCS") { errors.push("'BOUSY-RICCS' is an invalid segment."); }
	if (route.includes("BOUSY HPW") { errors.push("'BOUSY-HPW' is an invalid segment."); }
	if (route.includes("BOUSY LYH") { errors.push("'BOUSY-LYH' is an invalid segment."); }
	if (route.includes("BOUSY RONZZ") { errors.push("'BOUSY-RONZZ' is an invalid segment."); }
	if (route.includes("BOUSY FAK") { errors.push("'BOUSY-FAK' is an invalid segment."); }
	if (route.includes("BOUSY MOL") { errors.push("'BOUSY-MOL' is an invalid segment."); }
	if (route.includes("BOUSY GSO") { errors.push("'BOUSY-GSO' is an invalid segment."); }
	if (route.includes("BOUSY KIWII") { errors.push("'BOUSY-KIWII' is an invalid segment."); }
	if (route.includes("BOUSY GVE") { errors.push("'BOUSY-GVE' is an invalid segment."); }
	if (route.includes("BOUSY ORF") { errors.push("'BOUSY-ORF' is an invalid segment."); }
	if (route.includes("BOUSY RYCKI") { errors.push("'BOUSY-RYCKI' is an invalid segment."); }
	if (route.includes("BOUSY JIMAR") { errors.push("'BOUSY-JIMAR' is an invalid segment."); }
	if (route.includes("BOUSY NUTZE") { errors.push("'BOUSY-NUTZE' is an invalid segment."); }
	if (route.includes("BOUSY DILNN") { errors.push("'BOUSY-DILNN' is an invalid segment."); }
	if (route.includes("BOUSY RIC") { errors.push("'BOUSY-RIC' is an invalid segment."); }
	if (route.includes("BOUSY ISO") { errors.push("'BOUSY-ISO' is an invalid segment."); }
	if (route.includes("BOUSY BAWDS") { errors.push("'BOUSY-BAWDS' is an invalid segment."); }
	if (route.includes("BOUSY BYSCO") { errors.push("'BOUSY-BYSCO' is an invalid segment."); }
	if (route.includes("BOUSY PSK") { errors.push("'BOUSY-PSK' is an invalid segment."); }
	if (route.includes("BOUSY PEETT") { errors.push("'BOUSY-PEETT' is an invalid segment."); }
	if (route.includes("BOUSY TYI") { errors.push("'BOUSY-TYI' is an invalid segment."); }
	if (route.includes("BOUSY CVI") { errors.push("'BOUSY-CVI' is an invalid segment."); }
	if (route.includes("BOUSY ARGAL") { errors.push("'BOUSY-ARGAL' is an invalid segment."); }
	if (route.includes("BOUSY AARNN") { errors.push("'BOUSY-AARNN' is an invalid segment."); }
	if (route.includes("BOUSY ECG") { errors.push("'BOUSY-ECG' is an invalid segment."); }
	if (route.includes("BOUSY RDU") { errors.push("'BOUSY-RDU' is an invalid segment."); }
	if (route.includes("BOUSY MASHI") { errors.push("'BOUSY-MASHI' is an invalid segment."); }
	if (route.includes("BOVIC PIREX") { errors.push("'BOVIC-PIREX' is an invalid segment."); }
	if (route.includes("BOVIC LAZEY") { errors.push("'BOVIC-LAZEY' is an invalid segment."); }
	if (route.includes("BOVIC SEAVR") { errors.push("'BOVIC-SEAVR' is an invalid segment."); }
	if (route.includes("BOVIC BALTN") { errors.push("'BOVIC-BALTN' is an invalid segment."); }
	if (route.includes("BOVIC WINGZ") { errors.push("'BOVIC-WINGZ' is an invalid segment."); }
	if (route.includes("BOVIC ANTIG") { errors.push("'BOVIC-ANTIG' is an invalid segment."); }
	if (route.includes("BOVIC NUMBR") { errors.push("'BOVIC-NUMBR' is an invalid segment."); }
	if (route.includes("BOVIC GECAL") { errors.push("'BOVIC-GECAL' is an invalid segment."); }
	if (route.includes("BOVIC BALOO") { errors.push("'BOVIC-BALOO' is an invalid segment."); }
	if (route.includes("BOVIC SHEIL") { errors.push("'BOVIC-SHEIL' is an invalid segment."); }
	if (route.includes("BOHRR AZO") { errors.push("'BOHRR-AZO' is an invalid segment."); }
	if (route.includes("BOILE FLW") { errors.push("'BOILE-FLW' is an invalid segment."); }
	if (route.includes("BOLMO LETAK") { errors.push("'BOLMO-LETAK' is an invalid segment."); }
	if (route.includes("BONAB TVC") { errors.push("'BONAB-TVC' is an invalid segment."); }
	if (route.includes("BOOKE APACK") { errors.push("'BOOKE-APACK' is an invalid segment."); }
	if (route.includes("BOOKE CKH") { errors.push("'BOOKE-CKH' is an invalid segment."); }
	if (route.includes("BOPUT BEPEG") { errors.push("'BOPUT-BEPEG' is an invalid segment."); }
	if (route.includes("BOPUT IKMAN") { errors.push("'BOPUT-IKMAN' is an invalid segment."); }
	if (route.includes("BOPUT YFB") { errors.push("'BOPUT-YFB' is an invalid segment."); }
	if (route.includes("BOPUT KENKI") { errors.push("'BOPUT-KENKI' is an invalid segment."); }
	if (route.includes("BOPUT BERUS") { errors.push("'BOPUT-BERUS' is an invalid segment."); }
	if (route.includes("BOPUT NALDI") { errors.push("'BOPUT-NALDI' is an invalid segment."); }
	if (route.includes("BOPUT AYROU") { errors.push("'BOPUT-AYROU' is an invalid segment."); }
	if (route.includes("BOPUT YZS") { errors.push("'BOPUT-YZS' is an invalid segment."); }
	if (route.includes("BOPUT EPMAN") { errors.push("'BOPUT-EPMAN' is an invalid segment."); }
	if (route.includes("BOPUT NADMA") { errors.push("'BOPUT-NADMA' is an invalid segment."); }
	if (route.includes("BOPUT YXP") { errors.push("'BOPUT-YXP' is an invalid segment."); }
	if (route.includes("BOPUT GRIBS") { errors.push("'BOPUT-GRIBS' is an invalid segment."); }
	if (route.includes("BOPUT DAPAK") { errors.push("'BOPUT-DAPAK' is an invalid segment."); }
	if (route.includes("BOPUT JULET") { errors.push("'BOPUT-JULET' is an invalid segment."); }
	if (route.includes("BOPUT MEDPA") { errors.push("'BOPUT-MEDPA' is an invalid segment."); }
	if (route.includes("BOPUT KAGLY") { errors.push("'BOPUT-KAGLY' is an invalid segment."); }
	if (route.includes("BOPUT YTE") { errors.push("'BOPUT-YTE' is an invalid segment."); }
	if (route.includes("BOPUT MUSVA") { errors.push("'BOPUT-MUSVA' is an invalid segment."); }
	if (route.includes("BOPUT FEDDY") { errors.push("'BOPUT-FEDDY' is an invalid segment."); }
	if (route.includes("BORUS FOF") { errors.push("'BORUS-FOF' is an invalid segment."); }
	if (route.includes("BOUSY COUPN") { errors.push("'BOUSY-COUPN' is an invalid segment."); }
	if (route.includes("BRADD BGR") { errors.push("'BRADD-BGR' is an invalid segment."); }
	if (route.includes("BRADD ENE") { errors.push("'BRADD-ENE' is an invalid segment."); }
	if (route.includes("BRHMA ZONLO") { errors.push("'BRHMA-ZONLO' is an invalid segment."); }
	if (route.includes("BRHMA LBB") { errors.push("'BRHMA-LBB' is an invalid segment."); }
	if (route.includes("BRHMA CISAP") { errors.push("'BRHMA-CISAP' is an invalid segment."); }
	if (route.includes("BRIDJ UKIPE") { errors.push("'BRIDJ-UKIPE' is an invalid segment."); }
	if (route.includes("BRIDJ MIDLE") { errors.push("'BRIDJ-MIDLE' is an invalid segment."); }
	if (route.includes("BUDAR BAREE") { errors.push("'BUDAR-BAREE' is an invalid segment."); }
	if (route.includes("BUNTR HOMRR") { errors.push("'BUNTR-HOMRR' is an invalid segment."); }
	if (route.includes("BVT ERNEY") { errors.push("'BVT-ERNEY' is an invalid segment."); }
	if (route.includes("BZN BIL") { errors.push("'BZN-BIL' is an invalid segment."); }
	if (route.includes("CAM ALLEX") { errors.push("'CAM-ALLEX' is an invalid segment."); }
	if (route.includes("CAMAR TISSY") { errors.push("'CAMAR-TISSY' is an invalid segment."); }
	if (route.includes("CARML MOL") { errors.push("'CARML-MOL' is an invalid segment."); }
	if (route.includes("CARML LYH") { errors.push("'CARML-LYH' is an invalid segment."); }
	if (route.includes("CARML GVE") { errors.push("'CARML-GVE' is an invalid segment."); }
	if (route.includes("BRADD BEEKN") { errors.push("'BRADD-BEEKN' is an invalid segment."); }
	if (route.includes("BRADD KAYCC") { errors.push("'BRADD-KAYCC' is an invalid segment."); }
	if (route.includes("BRADD RIFLE") { errors.push("'BRADD-RIFLE' is an invalid segment."); }
	if (route.includes("BRINY ALCOA") { errors.push("'BRINY-ALCOA' is an invalid segment."); }
	if (route.includes("BRK EZEEE") { errors.push("'BRK-EZEEE' is an invalid segment."); }
	if (route.includes("BROBB WINDU") { errors.push("'BROBB-WINDU' is an invalid segment."); }
	if (route.includes("BURGG DEDDY") { errors.push("'BURGG-DEDDY' is an invalid segment."); }
	if (route.includes("BURME COMDY") { errors.push("'BURME-COMDY' is an invalid segment."); }
	if (route.includes("BURWA LETAK") { errors.push("'BURWA-LETAK' is an invalid segment."); }
	if (route.includes("BUTBA BUTAL") { errors.push("'BUTBA-BUTAL' is an invalid segment."); }
	if (route.includes("BYSEL RADDS") { errors.push("'BYSEL-RADDS' is an invalid segment."); }
	if (route.includes("CABCI KJOHN") { errors.push("'CABCI-KJOHN' is an invalid segment."); }
	if (route.includes("CABCI MIILS") { errors.push("'CABCI-MIILS' is an invalid segment."); }
	if (route.includes("CABCI OLABA") { errors.push("'CABCI-OLABA' is an invalid segment."); }
	if (route.includes("CABCI DANOL") { errors.push("'CABCI-DANOL' is an invalid segment."); }
	if (route.includes("CABCI TAFFY") { errors.push("'CABCI-TAFFY' is an invalid segment."); }
	if (route.includes("CANEL EPMAN") { errors.push("'CANEL-EPMAN' is an invalid segment."); }
	if (route.includes("CANEL YXP") { errors.push("'CANEL-YXP' is an invalid segment."); }
	if (route.includes("CANEL NADMA") { errors.push("'CANEL-NADMA' is an invalid segment."); }
	if (route.includes("CANEL YZS") { errors.push("'CANEL-YZS' is an invalid segment."); }
	if (route.includes("CANEL MUSVA") { errors.push("'CANEL-MUSVA' is an invalid segment."); }
	if (route.includes("CANEL VODEK") { errors.push("'CANEL-VODEK' is an invalid segment."); }
	if (route.includes("CANEL JULET") { errors.push("'CANEL-JULET' is an invalid segment."); }
	if (route.includes("CANEL JELCO") { errors.push("'CANEL-JELCO' is an invalid segment."); }
	if (route.includes("CANEL YFB") { errors.push("'CANEL-YFB' is an invalid segment."); }
	if (route.includes("CANEL MIBNO") { errors.push("'CANEL-MIBNO' is an invalid segment."); }
	if (route.includes("CANEL AYROU") { errors.push("'CANEL-AYROU' is an invalid segment."); }
	if (route.includes("CANEL YFY") { errors.push("'CANEL-YFY' is an invalid segment."); }
	if (route.includes("CANEL ALSAK") { errors.push("'CANEL-ALSAK' is an invalid segment."); }
	if (route.includes("CANEL RODBO") { errors.push("'CANEL-RODBO' is an invalid segment."); }
	if (route.includes("CANEL YTE") { errors.push("'CANEL-YTE' is an invalid segment."); }
	if (route.includes("CANEL NALDI") { errors.push("'CANEL-NALDI' is an invalid segment."); }
	if (route.includes("CANEL KENKI") { errors.push("'CANEL-KENKI' is an invalid segment."); }
	if (route.includes("CANEL ADSAM") { errors.push("'CANEL-ADSAM' is an invalid segment."); }
	if (route.includes("CANEL TEFFO") { errors.push("'CANEL-TEFFO' is an invalid segment."); }
	if (route.includes("CANEL GRIBS") { errors.push("'CANEL-GRIBS' is an invalid segment."); }
	if (route.includes("CANEL ARVEE") { errors.push("'CANEL-ARVEE' is an invalid segment."); }
	if (route.includes("CANEL IKMAN") { errors.push("'CANEL-IKMAN' is an invalid segment."); }
	if (route.includes("CANEL MEDPA") { errors.push("'CANEL-MEDPA' is an invalid segment."); }
	if (route.includes("CANEL FEDDY") { errors.push("'CANEL-FEDDY' is an invalid segment."); }
	if (route.includes("CANEL BERUS") { errors.push("'CANEL-BERUS' is an invalid segment."); }
	if (route.includes("CANEL PEPKI") { errors.push("'CANEL-PEPKI' is an invalid segment."); }
	if (route.includes("CANEL KAGLY") { errors.push("'CANEL-KAGLY' is an invalid segment."); }
	if (route.includes("CANIT NUCAR") { errors.push("'CANIT-NUCAR' is an invalid segment."); }
	if (route.includes("CANME IRDUV") { errors.push("'CANME-IRDUV' is an invalid segment."); }
	if (route.includes("CANSO REEDO") { errors.push("'CANSO-REEDO' is an invalid segment."); }
	if (route.includes("CANSO SSM") { errors.push("'CANSO-SSM' is an invalid segment."); }
	if (route.includes("CANSO TVC") { errors.push("'CANSO-TVC' is an invalid segment."); }
	if (route.includes("CARAC JEBBY") { errors.push("'CARAC-JEBBY' is an invalid segment."); }
	if (route.includes("CARET BULET") { errors.push("'CARET-BULET' is an invalid segment."); }
	if (route.includes("CARML BBDOL") { errors.push("'CARML-BBDOL' is an invalid segment."); }
	if (route.includes("CARPX WHOOS") { errors.push("'CARPX-WHOOS' is an invalid segment."); }
	if (route.includes("CARPX HOAGG") { errors.push("'CARPX-HOAGG' is an invalid segment."); }
	if (route.includes("CARPX IDOLS") { errors.push("'CARPX-IDOLS' is an invalid segment."); }
	if (route.includes("CATAR SDAIL") { errors.push("'CATAR-SDAIL' is an invalid segment."); }
	if (route.includes("CBULA SIFRA") { errors.push("'CBULA-SIFRA' is an invalid segment."); }
	if (route.includes("CBULA LAROS") { errors.push("'CBULA-LAROS' is an invalid segment."); }
	if (route.includes("CBULA DUBUD") { errors.push("'CBULA-DUBUD' is an invalid segment."); }
	if (route.includes("CBULA LENRI") { errors.push("'CBULA-LENRI' is an invalid segment."); }
	if (route.includes("CCS MANOX") { errors.push("'CCS-MANOX' is an invalid segment."); }
	if (route.includes("CCS DETOX") { errors.push("'CCS-DETOX' is an invalid segment."); }
	if (route.includes("CCS KOMUT") { errors.push("'CCS-KOMUT' is an invalid segment."); }
	if (route.includes("CCS BANAL") { errors.push("'CCS-BANAL' is an invalid segment."); }
	if (route.includes("CCS DETOX") { errors.push("'CCS-DETOX' is an invalid segment."); }
	if (route.includes("CCS ARMED") { errors.push("'CCS-ARMED' is an invalid segment."); }
	if (route.includes("CEFOU DORYY") { errors.push("'CEFOU-DORYY' is an invalid segment."); }
	if (route.includes("CEFOU VESMI") { errors.push("'CEFOU-VESMI' is an invalid segment."); }
	if (route.includes("CEFOU HOIST") { errors.push("'CEFOU-HOIST' is an invalid segment."); }
	if (route.includes("CEFOU IRLOK") { errors.push("'CEFOU-IRLOK' is an invalid segment."); }
	if (route.includes("CEFOU URTAK") { errors.push("'CEFOU-URTAK' is an invalid segment."); }
	if (route.includes("CEFOU BOKTO") { errors.push("'CEFOU-BOKTO' is an invalid segment."); }
	if (route.includes("CEFOU ENNSO") { errors.push("'CEFOU-ENNSO' is an invalid segment."); }
	if (route.includes("CEFOU JANJO") { errors.push("'CEFOU-JANJO' is an invalid segment."); }
	if (route.includes("CEFOU KODIK") { errors.push("'CEFOU-KODIK' is an invalid segment."); }
	if (route.includes("CEFOU SAXAN") { errors.push("'CEFOU-SAXAN' is an invalid segment."); }
	if (route.includes("CEFOU MELDI") { errors.push("'CEFOU-MELDI' is an invalid segment."); }
	if (route.includes("CEFOU PELTU") { errors.push("'CEFOU-PELTU' is an invalid segment."); }
	if (route.includes("CEFOU AVUTI") { errors.push("'CEFOU-AVUTI' is an invalid segment."); }
	if (route.includes("CEFOU LOMSI") { errors.push("'CEFOU-LOMSI' is an invalid segment."); }
	if (route.includes("CELTK FRILL") { errors.push("'CELTK-FRILL' is an invalid segment."); }
	if (route.includes("CHRRI DOLIE") { errors.push("'CHRRI-DOLIE' is an invalid segment."); }
	if (route.includes("CHS CHACA") { errors.push("'CHS-CHACA' is an invalid segment."); }
	if (route.includes("CIGAR CAMJO") { errors.push("'CIGAR-CAMJO' is an invalid segment."); }
	if (route.includes("CLOWR JAINS") { errors.push("'CLOWR-JAINS' is an invalid segment."); }
	if (route.includes("CNERY DECAS") { errors.push("'CNERY-DECAS' is an invalid segment."); }
	if (route.includes("CNNOR ZFP") { errors.push("'CNNOR-ZFP' is an invalid segment."); }
	if (route.includes("CNX ONM") { errors.push("'CNX-ONM' is an invalid segment."); }
	if (route.includes("COHOE TUNNL") { errors.push("'COHOE-TUNNL' is an invalid segment."); }
	if (route.includes("CORLA BALDI") { errors.push("'CORLA-BALDI' is an invalid segment."); }
	if (route.includes("CTY OCF") { errors.push("'CTY-OCF' is an invalid segment."); }
	if (route.includes("CUDDY MEMSO") { errors.push("'CUDDY-MEMSO' is an invalid segment."); }
	if (route.includes("CUDDY BAREE") { errors.push("'CUDDY-BAREE' is an invalid segment."); }
	if (route.includes("CUDDY NOWAA") { errors.push("'CUDDY-NOWAA' is an invalid segment."); }
	if (route.includes("CUTAN BQS") { errors.push("'CUTAN-BQS' is an invalid segment."); }
	if (route.includes("CVG LOVEY") { errors.push("'CVG-LOVEY' is an invalid segment."); }
	if (route.includes("DANCO BKW") { errors.push("'DANCO-BKW' is an invalid segment."); }
	if (route.includes("DANCO NEALS") { errors.push("'DANCO-NEALS' is an invalid segment."); }
	if (route.includes("DANCO COLZI") { errors.push("'DANCO-COLZI' is an invalid segment."); }
	if (route.includes("DANCO REAVS") { errors.push("'DANCO-REAVS' is an invalid segment."); }
	if (route.includes("DANCO SOT") { errors.push("'DANCO-SOT' is an invalid segment."); }
	if (route.includes("DANCO HVQ") { errors.push("'DANCO-HVQ' is an invalid segment."); }
	if (route.includes("DANCO MOL") { errors.push("'DANCO-MOL' is an invalid segment."); }
	if (route.includes("DANCO SANNY") { errors.push("'DANCO-SANNY' is an invalid segment."); }
	if (route.includes("DANCO MAACK") { errors.push("'DANCO-MAACK' is an invalid segment."); }
	if (route.includes("DANCO GSO") { errors.push("'DANCO-GSO' is an invalid segment."); }
	if (route.includes("DANCO HNN") { errors.push("'DANCO-HNN' is an invalid segment."); }
	if (route.includes("DANCO LYH") { errors.push("'DANCO-LYH' is an invalid segment."); }
	if (route.includes("DANCO ROCKA") { errors.push("'DANCO-ROCKA' is an invalid segment."); }
	if (route.includes("DANCO STACY") { errors.push("'DANCO-STACY' is an invalid segment."); }
	if (route.includes("DANCO ILLSA") { errors.push("'DANCO-ILLSA' is an invalid segment."); }
	if (route.includes("DANCO SCRZA") { errors.push("'DANCO-SCRZA' is an invalid segment."); }
	if (route.includes("DANCO BLUEJ") { errors.push("'DANCO-BLUEJ' is an invalid segment."); }
	if (route.includes("CCS LUTAK") { errors.push("'CCS-LUTAK' is an invalid segment."); }
	if (route.includes("CCS GUNTI") { errors.push("'CCS-GUNTI' is an invalid segment."); }
	if (route.includes("CCS ERPES") { errors.push("'CCS-ERPES' is an invalid segment."); }
	if (route.includes("CDS CEVEX") { errors.push("'CDS-CEVEX' is an invalid segment."); }
	if (route.includes("CEFOU POLTY") { errors.push("'CEFOU-POLTY' is an invalid segment."); }
	if (route.includes("CERDA LETON") { errors.push("'CERDA-LETON' is an invalid segment."); }
	if (route.includes("CEW MCN") { errors.push("'CEW-MCN' is an invalid segment."); }
	if (route.includes("CHIKI RIZON") { errors.push("'CHIKI-RIZON' is an invalid segment."); }
	if (route.includes("CHOPZ THRSR") { errors.push("'CHOPZ-THRSR' is an invalid segment."); }
	if (route.includes("CIGAR KNOST") { errors.push("'CIGAR-KNOST' is an invalid segment."); }
	if (route.includes("CIKAN EWM") { errors.push("'CIKAN-EWM' is an invalid segment."); }
	if (route.includes("CINNY MQO") { errors.push("'CINNY-MQO' is an invalid segment."); }
	if (route.includes("CKH JOELE") { errors.push("'CKH-JOELE' is an invalid segment."); }
	if (route.includes("CLAPY SKARP") { errors.push("'CLAPY-SKARP' is an invalid segment."); }
	if (route.includes("CLAVY KENKI") { errors.push("'CLAVY-KENKI' is an invalid segment."); }
	if (route.includes("CLAVY NALDI") { errors.push("'CLAVY-NALDI' is an invalid segment."); }
	if (route.includes("CLAWD SITTR") { errors.push("'CLAWD-SITTR' is an invalid segment."); }
	if (route.includes("CLETT MORME") { errors.push("'CLETT-MORME' is an invalid segment."); }
	if (route.includes("CNNOR JIMAC") { errors.push("'CNNOR-JIMAC' is an invalid segment."); }
	if (route.includes("CODDY EBBER") { errors.push("'CODDY-EBBER' is an invalid segment."); }
	if (route.includes("CODDY FITES") { errors.push("'CODDY-FITES' is an invalid segment."); }
	if (route.includes("COORZ MDANO") { errors.push("'COORZ-MDANO' is an invalid segment."); }
	if (route.includes("CORKY DEFUN") { errors.push("'CORKY-DEFUN' is an invalid segment."); }
	if (route.includes("CORKY CSG") { errors.push("'CORKY-CSG' is an invalid segment."); }
	if (route.includes("CORKY ACORI") { errors.push("'CORKY-ACORI' is an invalid segment."); }
	if (route.includes("CORKY MCN") { errors.push("'CORKY-MCN' is an invalid segment."); }
	if (route.includes("CROCK RZC") { errors.push("'CROCK-RZC' is an invalid segment."); }
	if (route.includes("CRP STUFT") { errors.push("'CRP-STUFT' is an invalid segment."); }
	if (route.includes("CRP JIMIE") { errors.push("'CRP-JIMIE' is an invalid segment."); }
	if (route.includes("CRPLR PAACK") { errors.push("'CRPLR-PAACK' is an invalid segment."); }
	if (route.includes("CRPLR EARZZ") { errors.push("'CRPLR-EARZZ' is an invalid segment."); }
	if (route.includes("CRUPE PRCHA") { errors.push("'CRUPE-PRCHA' is an invalid segment."); }
	if (route.includes("CSG BESTT") { errors.push("'CSG-BESTT' is an invalid segment."); }
	if (route.includes("CUDDY JOVIE") { errors.push("'CUDDY-JOVIE' is an invalid segment."); }
	if (route.includes("CUU IMARI") { errors.push("'CUU-IMARI' is an invalid segment."); }
	if (route.includes("CUU AMUKI") { errors.push("'CUU-AMUKI' is an invalid segment."); }
	if (route.includes("CVI SKARP") { errors.push("'CVI-SKARP' is an invalid segment."); }
	if (route.includes("DAGGR MINOW") { errors.push("'DAGGR-MINOW' is an invalid segment."); }
	if (route.includes("DAGUD OSUPA") { errors.push("'DAGUD-OSUPA' is an invalid segment."); }
	if (route.includes("DANOL KODIK") { errors.push("'DANOL-KODIK' is an invalid segment."); }
	if (route.includes("DANOL PELTU") { errors.push("'DANOL-PELTU' is an invalid segment."); }
	if (route.includes("DANOL RIKAL") { errors.push("'DANOL-RIKAL' is an invalid segment."); }
	if (route.includes("DANOL SAXAN") { errors.push("'DANOL-SAXAN' is an invalid segment."); }
	if (route.includes("DANOL MELDI") { errors.push("'DANOL-MELDI' is an invalid segment."); }
	if (route.includes("DANOL NEEKO") { errors.push("'DANOL-NEEKO' is an invalid segment."); }
	if (route.includes("DASER SEAVR") { errors.push("'DASER-SEAVR' is an invalid segment."); }
	if (route.includes("DASER PIREX") { errors.push("'DASER-PIREX' is an invalid segment."); }
	if (route.includes("DASER WINGZ") { errors.push("'DASER-WINGZ' is an invalid segment."); }
	if (route.includes("DASER NUMBR") { errors.push("'DASER-NUMBR' is an invalid segment."); }
	if (route.includes("DASER BALTN") { errors.push("'DASER-BALTN' is an invalid segment."); }
	if (route.includes("DASER LAZEY") { errors.push("'DASER-LAZEY' is an invalid segment."); }
	if (route.includes("DASER GECAL") { errors.push("'DASER-GECAL' is an invalid segment."); }
	if (route.includes("DASER SHEIL") { errors.push("'DASER-SHEIL' is an invalid segment."); }
	if (route.includes("DASER BALOO") { errors.push("'DASER-BALOO' is an invalid segment."); }
	if (route.includes("DAVVY LAWSN") { errors.push("'DAVVY-LAWSN' is an invalid segment."); }
	if (route.includes("DAWTS EFFAY") { errors.push("'DAWTS-EFFAY' is an invalid segment."); }
	if (route.includes("DAWTS FLO") { errors.push("'DAWTS-FLO' is an invalid segment."); }
	if (route.includes("DAWTS JIMAR") { errors.push("'DAWTS-JIMAR' is an invalid segment."); }
	if (route.includes("DAWTS IDDAA") { errors.push("'DAWTS-IDDAA' is an invalid segment."); }
	if (route.includes("DAWTS KAATT") { errors.push("'DAWTS-KAATT' is an invalid segment."); }
	if (route.includes("DAWTS TYI") { errors.push("'DAWTS-TYI' is an invalid segment."); }
	if (route.includes("DAWTS MRPIT") { errors.push("'DAWTS-MRPIT' is an invalid segment."); }
	if (route.includes("DAWTS ISO") { errors.push("'DAWTS-ISO' is an invalid segment."); }
	if (route.includes("DAWTS CAE") { errors.push("'DAWTS-CAE' is an invalid segment."); }
	if (route.includes("DAWTS SARKY") { errors.push("'DAWTS-SARKY' is an invalid segment."); }
	if (route.includes("DAWTS SMPRR") { errors.push("'DAWTS-SMPRR' is an invalid segment."); }
	if (route.includes("DAWTS SLOJO") { errors.push("'DAWTS-SLOJO' is an invalid segment."); }
	if (route.includes("DAWTS GSO") { errors.push("'DAWTS-GSO' is an invalid segment."); }
	if (route.includes("DAWTS AHOEY") { errors.push("'DAWTS-AHOEY' is an invalid segment."); }
	if (route.includes("DAWTS STUKI") { errors.push("'DAWTS-STUKI' is an invalid segment."); }
	if (route.includes("DAWTS GLOVR") { errors.push("'DAWTS-GLOVR' is an invalid segment."); }
	if (route.includes("DAWTS EMCET") { errors.push("'DAWTS-EMCET' is an invalid segment."); }
	if (route.includes("DAWTS POLYY") { errors.push("'DAWTS-POLYY' is an invalid segment."); }
	if (route.includes("DAWTS YURCK") { errors.push("'DAWTS-YURCK' is an invalid segment."); }
	if (route.includes("DAWTS ORACL") { errors.push("'DAWTS-ORACL' is an invalid segment."); }
	if (route.includes("DAWTS CHYPS") { errors.push("'DAWTS-CHYPS' is an invalid segment."); }
	if (route.includes("DAWTS YERBA") { errors.push("'DAWTS-YERBA' is an invalid segment."); }
	if (route.includes("DAWTS ILM") { errors.push("'DAWTS-ILM' is an invalid segment."); }
	if (route.includes("DAWTS RDU") { errors.push("'DAWTS-RDU' is an invalid segment."); }
	if (route.includes("DAWTS HINTZ") { errors.push("'DAWTS-HINTZ' is an invalid segment."); }
	if (route.includes("DAWTS RAANE") { errors.push("'DAWTS-RAANE' is an invalid segment."); }
	if (route.includes("DAWTS RAPZZ") { errors.push("'DAWTS-RAPZZ' is an invalid segment."); }
	if (route.includes("DAWTS TORQD") { errors.push("'DAWTS-TORQD' is an invalid segment."); }
	if (route.includes("DAWTS OKNEE") { errors.push("'DAWTS-OKNEE' is an invalid segment."); }
	if (route.includes("DAWTS ELLDE") { errors.push("'DAWTS-ELLDE' is an invalid segment."); }
	if (route.includes("DBACK HOMRR") { errors.push("'DBACK-HOMRR' is an invalid segment."); }
	if (route.includes("DECAS PSP") { errors.push("'DECAS-PSP' is an invalid segment."); }
	if (route.includes("DARUB KAGLY") { errors.push("'DARUB-KAGLY' is an invalid segment."); }
	if (route.includes("DARUB IKMAN") { errors.push("'DARUB-IKMAN' is an invalid segment."); }
	if (route.includes("DARUB VODEK") { errors.push("'DARUB-VODEK' is an invalid segment."); }
	if (route.includes("DARUB PEPKI") { errors.push("'DARUB-PEPKI' is an invalid segment."); }
	if (route.includes("DARUB ALSAK") { errors.push("'DARUB-ALSAK' is an invalid segment."); }
	if (route.includes("DARUB ADSAM") { errors.push("'DARUB-ADSAM' is an invalid segment."); }
	if (route.includes("DARUB BERUS") { errors.push("'DARUB-BERUS' is an invalid segment."); }
	if (route.includes("DARUB YZS") { errors.push("'DARUB-YZS' is an invalid segment."); }
	if (route.includes("DARUB YTE") { errors.push("'DARUB-YTE' is an invalid segment."); }
	if (route.includes("DARUB NADMA") { errors.push("'DARUB-NADMA' is an invalid segment."); }
	if (route.includes("DARUB GRIBS") { errors.push("'DARUB-GRIBS' is an invalid segment."); }
	if (route.includes("DARUB DAJIM") { errors.push("'DARUB-DAJIM' is an invalid segment."); }
	if (route.includes("DARUB COPUR") { errors.push("'DARUB-COPUR' is an invalid segment."); }
	if (route.includes("DARUB SF") { errors.push("'DARUB-SF' is an invalid segment."); }
	if (route.includes("DARUB YFB") { errors.push("'DARUB-YFB' is an invalid segment."); }
	if (route.includes("DARUB LOPVI") { errors.push("'DARUB-LOPVI' is an invalid segment."); }
	if (route.includes("DARUB MUSVA") { errors.push("'DARUB-MUSVA' is an invalid segment."); }
	if (route.includes("DARUB BEPEG") { errors.push("'DARUB-BEPEG' is an invalid segment."); }
	if (route.includes("DARUB MIBNO") { errors.push("'DARUB-MIBNO' is an invalid segment."); }
	if (route.includes("DARUB RONPU") { errors.push("'DARUB-RONPU' is an invalid segment."); }
	if (route.includes("DARUB DAPAK") { errors.push("'DARUB-DAPAK' is an invalid segment."); }
	if (route.includes("DARUB ARVEE") { errors.push("'DARUB-ARVEE' is an invalid segment."); }
	if (route.includes("DARUB JELCO") { errors.push("'DARUB-JELCO' is an invalid segment."); }
	if (route.includes("DARUB YFY") { errors.push("'DARUB-YFY' is an invalid segment."); }
	if (route.includes("DARUB OTAKU") { errors.push("'DARUB-OTAKU' is an invalid segment."); }
	if (route.includes("DARUB DUTUM") { errors.push("'DARUB-DUTUM' is an invalid segment."); }
	if (route.includes("DARUB BEZED") { errors.push("'DARUB-BEZED' is an invalid segment."); }
	if (route.includes("DARUB TEFFO") { errors.push("'DARUB-TEFFO' is an invalid segment."); }
	if (route.includes("DARUB NALDI") { errors.push("'DARUB-NALDI' is an invalid segment."); }
	if (route.includes("DARUB TUNNI") { errors.push("'DARUB-TUNNI' is an invalid segment."); }
	if (route.includes("DARUB DUGNO") { errors.push("'DARUB-DUGNO' is an invalid segment."); }
	if (route.includes("DARUX RESQU") { errors.push("'DARUX-RESQU' is an invalid segment."); }
	if (route.includes("DASUG TVC") { errors.push("'DASUG-TVC' is an invalid segment."); }
	if (route.includes("DAWTS JOHAR") { errors.push("'DAWTS-JOHAR' is an invalid segment."); }
	if (route.includes("DAXER MAXAR") { errors.push("'DAXER-MAXAR' is an invalid segment."); }
	if (route.includes("DEGVA DLH") { errors.push("'DEGVA-DLH' is an invalid segment."); }
	if (route.includes("DELCO SHUMY") { errors.push("'DELCO-SHUMY' is an invalid segment."); }
	if (route.includes("DENNS NAPUA") { errors.push("'DENNS-NAPUA' is an invalid segment."); }
	if (route.includes("DENNS CKH") { errors.push("'DENNS-CKH' is an invalid segment."); }
	if (route.includes("DEREC LNY") { errors.push("'DEREC-LNY' is an invalid segment."); }
	if (route.includes("DERLO DJB") { errors.push("'DERLO-DJB' is an invalid segment."); }
	if (route.includes("DEYUC ETL") { errors.push("'DEYUC-ETL' is an invalid segment."); }
	if (route.includes("DHP DVALL") { errors.push("'DHP-DVALL' is an invalid segment."); }
	if (route.includes("DJB JHW") { errors.push("'DJB-JHW' is an invalid segment."); }
	if (route.includes("DNW NALSI") { errors.push("'DNW-NALSI' is an invalid segment."); }
	if (route.includes("DORYY BAREE") { errors.push("'DORYY-BAREE' is an invalid segment."); }
	if (route.includes("DRK ELLDA") { errors.push("'DRK-ELLDA' is an invalid segment."); }
	if (route.includes("DSM OVR") { errors.push("'DSM-OVR' is an invalid segment."); }
	if (route.includes("DULEE EBAYY") { errors.push("'DULEE-EBAYY' is an invalid segment."); }
	if (route.includes("DUTAX YXC") { errors.push("'DUTAX-YXC' is an invalid segment."); }
	if (route.includes("DUVBI BOKTO") { errors.push("'DUVBI-BOKTO' is an invalid segment."); }
	if (route.includes("EAGUL HOMRR") { errors.push("'EAGUL-HOMRR' is an invalid segment."); }
	if (route.includes("EAGUL PICHR") { errors.push("'EAGUL-PICHR' is an invalid segment."); }
	if (route.includes("EAR MORGS") { errors.push("'EAR-MORGS' is an invalid segment."); }
	if (route.includes("EBONY JAROM") { errors.push("'EBONY-JAROM' is an invalid segment."); }
	if (route.includes("EBONY RIKAL") { errors.push("'EBONY-RIKAL' is an invalid segment."); }
	if (route.includes("EBONY KODIK") { errors.push("'EBONY-KODIK' is an invalid segment."); }
	if (route.includes("EBONY JANJO") { errors.push("'EBONY-JANJO' is an invalid segment."); }
	if (route.includes("EBONY SAXAN") { errors.push("'EBONY-SAXAN' is an invalid segment."); }
	if (route.includes("EBONY MELDI") { errors.push("'EBONY-MELDI' is an invalid segment."); }
	if (route.includes("EBONY LOMSI") { errors.push("'EBONY-LOMSI' is an invalid segment."); }
	if (route.includes("EBONY PELTU") { errors.push("'EBONY-PELTU' is an invalid segment."); }
	if (route.includes("EBONY HOIST") { errors.push("'EBONY-HOIST' is an invalid segment."); }
	if (route.includes("EBONY IRLOK") { errors.push("'EBONY-IRLOK' is an invalid segment."); }
	if (route.includes("EGETA AGUJA") { errors.push("'EGETA-AGUJA' is an invalid segment."); }
	if (route.includes("EGETA MIKUS") { errors.push("'EGETA-MIKUS' is an invalid segment."); }
	if (route.includes("EGETA ARNAL") { errors.push("'EGETA-ARNAL' is an invalid segment."); }
	if (route.includes("ELMUC LAMER") { errors.push("'ELMUC-LAMER' is an invalid segment."); }
	if (route.includes("DENNS FISHE") { errors.push("'DENNS-FISHE' is an invalid segment."); }
	if (route.includes("DENNS HNL") { errors.push("'DENNS-HNL' is an invalid segment."); }
	if (route.includes("DEREC UPP") { errors.push("'DEREC-UPP' is an invalid segment."); }
	if (route.includes("DEREC FITES") { errors.push("'DEREC-FITES' is an invalid segment."); }
	if (route.includes("DERLO REEDO") { errors.push("'DERLO-REEDO' is an invalid segment."); }
	if (route.includes("DERLO LETAK") { errors.push("'DERLO-LETAK' is an invalid segment."); }
	if (route.includes("DERLO WWSHR") { errors.push("'DERLO-WWSHR' is an invalid segment."); }
	if (route.includes("DESAT ZMR") { errors.push("'DESAT-ZMR' is an invalid segment."); }
	if (route.includes("DESKI LETAK") { errors.push("'DESKI-LETAK' is an invalid segment."); }
	if (route.includes("DETOX INBOM") { errors.push("'DETOX-INBOM' is an invalid segment."); }
	if (route.includes("DETOX VIS") { errors.push("'DETOX-VIS' is an invalid segment."); }
	if (route.includes("DETOX ADORO") { errors.push("'DETOX-ADORO' is an invalid segment."); }
	if (route.includes("DFENC SAWED") { errors.push("'DFENC-SAWED' is an invalid segment."); }
	if (route.includes("DIBAM SELAN") { errors.push("'DIBAM-SELAN' is an invalid segment."); }
	if (route.includes("DICEN REEDO") { errors.push("'DICEN-REEDO' is an invalid segment."); }
	if (route.includes("DICEN POLTY") { errors.push("'DICEN-POLTY' is an invalid segment."); }
	if (route.includes("DIK HML") { errors.push("'DIK-HML' is an invalid segment."); }
	if (route.includes("DIK YBR") { errors.push("'DIK-YBR' is an invalid segment."); }
	if (route.includes("DINTY DUETS") { errors.push("'DINTY-DUETS' is an invalid segment."); }
	if (route.includes("DIVKU BAKRO") { errors.push("'DIVKU-BAKRO' is an invalid segment."); }
	if (route.includes("DLH OVETE") { errors.push("'DLH-OVETE' is an invalid segment."); }
	if (route.includes("DLN ITONE") { errors.push("'DLN-ITONE' is an invalid segment."); }
	if (route.includes("DLN GOGGS") { errors.push("'DLN-GOGGS' is an invalid segment."); }
	if (route.includes("DOGUS NOSTO") { errors.push("'DOGUS-NOSTO' is an invalid segment."); }
	if (route.includes("DONEZ DUKKY") { errors.push("'DONEZ-DUKKY' is an invalid segment."); }
	if (route.includes("DORMR PSTOL") { errors.push("'DORMR-PSTOL' is an invalid segment."); }
	if (route.includes("DORYY ANATI") { errors.push("'DORYY-ANATI' is an invalid segment."); }
	if (route.includes("DOSXX OHARA") { errors.push("'DOSXX-OHARA' is an invalid segment."); }
	if (route.includes("DOVEY RIFLE") { errors.push("'DOVEY-RIFLE' is an invalid segment."); }
	if (route.includes("DOVEY JAWZZ") { errors.push("'DOVEY-JAWZZ' is an invalid segment."); }
	if (route.includes("DRAPP HEDMI") { errors.push("'DRAPP-HEDMI' is an invalid segment."); }
	if (route.includes("DRAYK ARROW") { errors.push("'DRAYK-ARROW' is an invalid segment."); }
	if (route.includes("DRIFT BRIGS") { errors.push("'DRIFT-BRIGS' is an invalid segment."); }
	if (route.includes("DRYED ZIBUT") { errors.push("'DRYED-ZIBUT' is an invalid segment."); }
	if (route.includes("DRYED BERGH") { errors.push("'DRYED-BERGH' is an invalid segment."); }
	if (route.includes("DRYED MARIG") { errors.push("'DRYED-MARIG' is an invalid segment."); }
	if (route.includes("DUBUD CBULA") { errors.push("'DUBUD-CBULA' is an invalid segment."); }
	if (route.includes("DUFEL EZEEE") { errors.push("'DUFEL-EZEEE' is an invalid segment."); }
	if (route.includes("DUNTA BGI") { errors.push("'DUNTA-BGI' is an invalid segment."); }
	if (route.includes("DUSMA TEALS") { errors.push("'DUSMA-TEALS' is an invalid segment."); }
	if (route.includes("DUSMA SSM") { errors.push("'DUSMA-SSM' is an invalid segment."); }
	if (route.includes("DXO FINGL") { errors.push("'DXO-FINGL' is an invalid segment."); }
	if (route.includes("DYEES FEMID") { errors.push("'DYEES-FEMID' is an invalid segment."); }
	if (route.includes("EARZZ SKARP") { errors.push("'EARZZ-SKARP' is an invalid segment."); }
	if (route.includes("EBDOG TVC") { errors.push("'EBDOG-TVC' is an invalid segment."); }
	if (route.includes("EBEAR OKNEE") { errors.push("'EBEAR-OKNEE' is an invalid segment."); }
	if (route.includes("EBEAR OKNEE") { errors.push("'EBEAR-OKNEE' is an invalid segment."); }
	if (route.includes("EBONY BEEKN") { errors.push("'EBONY-BEEKN' is an invalid segment."); }
	if (route.includes("EBONY FOXBO") { errors.push("'EBONY-FOXBO' is an invalid segment."); }
	if (route.includes("EBONY KAYCC") { errors.push("'EBONY-KAYCC' is an invalid segment."); }
	if (route.includes("ECG COUPN") { errors.push("'ECG-COUPN' is an invalid segment."); }
	if (route.includes("ECK FNT") { errors.push("'ECK-FNT' is an invalid segment."); }
	if (route.includes("EEGEE OVETE") { errors.push("'EEGEE-OVETE' is an invalid segment."); }
	if (route.includes("EGETA VUNIB") { errors.push("'EGETA-VUNIB' is an invalid segment."); }
	if (route.includes("ELBOB ERAKA") { errors.push("'ELBOB-ERAKA' is an invalid segment."); }
	if (route.includes("ELBOW ANGLL") { errors.push("'ELBOW-ANGLL' is an invalid segment."); }
	if (route.includes("ELBOW JAZZI") { errors.push("'ELBOW-JAZZI' is an invalid segment."); }
	if (route.includes("ELBOW GRUBR") { errors.push("'ELBOW-GRUBR' is an invalid segment."); }
	if (route.includes("ELD IZAAC") { errors.push("'ELD-IZAAC' is an invalid segment."); }
	if (route.includes("ELKEY SXC") { errors.push("'ELKEY-SXC' is an invalid segment."); }
	if (route.includes("ELLAM RNICH") { errors.push("'ELLAM-RNICH' is an invalid segment."); }
	if (route.includes("ELOTU ALCOT") { errors.push("'ELOTU-ALCOT' is an invalid segment."); }
	if (route.includes("ELP CUU") { errors.push("'ELP-CUU' is an invalid segment."); }
	if (route.includes("ELSIR BRADD") { errors.push("'ELSIR-BRADD' is an invalid segment."); }
	if (route.includes("ELSIR BAREE") { errors.push("'ELSIR-BAREE' is an invalid segment."); }
	if (route.includes("EMABU MLY") { errors.push("'EMABU-MLY' is an invalid segment."); }
	if (route.includes("EMABU NEFTU") { errors.push("'EMABU-NEFTU' is an invalid segment."); }
	if (route.includes("EMP AGEXY") { errors.push("'EMP-AGEXY' is an invalid segment."); }
	if (route.includes("EMUSI AUR") { errors.push("'EMUSI-AUR' is an invalid segment."); }
	if (route.includes("ENAPI PIREX") { errors.push("'ENAPI-PIREX' is an invalid segment."); }
	if (route.includes("ENAPI WINGZ") { errors.push("'ENAPI-WINGZ' is an invalid segment."); }
	if (route.includes("ENAPI BALOO") { errors.push("'ENAPI-BALOO' is an invalid segment."); }
	if (route.includes("ENAPI GECAL") { errors.push("'ENAPI-GECAL' is an invalid segment."); }
	if (route.includes("ENAPI SHEIL") { errors.push("'ENAPI-SHEIL' is an invalid segment."); }
	if (route.includes("ENAPI LAZEY") { errors.push("'ENAPI-LAZEY' is an invalid segment."); }
	if (route.includes("ENAPI SEAVR") { errors.push("'ENAPI-SEAVR' is an invalid segment."); }
	if (route.includes("ENAPI NUMBR") { errors.push("'ENAPI-NUMBR' is an invalid segment."); }
	if (route.includes("ENE BRADD") { errors.push("'ENE-BRADD' is an invalid segment."); }
	if (route.includes("ENE ALLEX") { errors.push("'ENE-ALLEX' is an invalid segment."); }
	if (route.includes("ENI REDWD") { errors.push("'ENI-REDWD' is an invalid segment."); }
	if (route.includes("ENM MCG") { errors.push("'ENM-MCG' is an invalid segment."); }
	if (route.includes("ENO RIDGY") { errors.push("'ENO-RIDGY' is an invalid segment."); }
	if (route.includes("EPENE GEG") { errors.push("'EPENE-GEG' is an invalid segment."); }
	if (route.includes("ETX FJC") { errors.push("'ETX-FJC' is an invalid segment."); }
	if (route.includes("EYNOR OVR") { errors.push("'EYNOR-OVR' is an invalid segment."); }
	if (route.includes("EYULE PUB") { errors.push("'EYULE-PUB' is an invalid segment."); }
	if (route.includes("EYW DVALL") { errors.push("'EYW-DVALL' is an invalid segment."); }
	if (route.includes("FASSA HELMO") { errors.push("'FASSA-HELMO' is an invalid segment."); }
	if (route.includes("FEBEL SMPRR") { errors.push("'FEBEL-SMPRR' is an invalid segment."); }
	if (route.includes("FEBEL MRPIT") { errors.push("'FEBEL-MRPIT' is an invalid segment."); }
	if (route.includes("FEBEL ELLDE") { errors.push("'FEBEL-ELLDE' is an invalid segment."); }
	if (route.includes("FEBEL RAPZZ") { errors.push("'FEBEL-RAPZZ' is an invalid segment."); }
	if (route.includes("FEBEL SARKY") { errors.push("'FEBEL-SARKY' is an invalid segment."); }
	if (route.includes("FFSSH WEMAR") { errors.push("'FFSSH-WEMAR' is an invalid segment."); }
	if (route.includes("FITES OGG") { errors.push("'FITES-OGG' is an invalid segment."); }
	if (route.includes("FLIRT THS") { errors.push("'FLIRT-THS' is an invalid segment."); }
	if (route.includes("FLO OPENS") { errors.push("'FLO-OPENS' is an invalid segment."); }
	if (route.includes("FNT YVV") { errors.push("'FNT-YVV' is an invalid segment."); }
	if (route.includes("FORCK MHZ") { errors.push("'FORCK-MHZ' is an invalid segment."); }
	if (route.includes("ELP OLS") { errors.push("'ELP-OLS' is an invalid segment."); }
	if (route.includes("EMDUN MAXAR") { errors.push("'EMDUN-MAXAR' is an invalid segment."); }
	if (route.includes("EMPTY ROD") { errors.push("'EMPTY-ROD' is an invalid segment."); }
	if (route.includes("EMRON KALIG") { errors.push("'EMRON-KALIG' is an invalid segment."); }
	if (route.includes("ENI SHENU") { errors.push("'ENI-SHENU' is an invalid segment."); }
	if (route.includes("ENSKY OPIHI") { errors.push("'ENSKY-OPIHI' is an invalid segment."); }
	if (route.includes("EPMAN VODEK") { errors.push("'EPMAN-VODEK' is an invalid segment."); }
	if (route.includes("EPMAN ALSAK") { errors.push("'EPMAN-ALSAK' is an invalid segment."); }
	if (route.includes("EVBAK TAKAS") { errors.push("'EVBAK-TAKAS' is an invalid segment."); }
	if (route.includes("EZEEE MDANO") { errors.push("'EZEEE-MDANO' is an invalid segment."); }
	if (route.includes("FAK BBOBO") { errors.push("'FAK-BBOBO' is an invalid segment."); }
	if (route.includes("FAR DUSMA") { errors.push("'FAR-DUSMA' is an invalid segment."); }
	if (route.includes("FAY ISO") { errors.push("'FAY-ISO' is an invalid segment."); }
	if (route.includes("FEBEL JOHAR") { errors.push("'FEBEL-JOHAR' is an invalid segment."); }
	if (route.includes("FEDDY MAXAR") { errors.push("'FEDDY-MAXAR' is an invalid segment."); }
	if (route.includes("FEKIK JOELE") { errors.push("'FEKIK-JOELE' is an invalid segment."); }
	if (route.includes("FELTN SSM") { errors.push("'FELTN-SSM' is an invalid segment."); }
	if (route.includes("FFITZ RNICH") { errors.push("'FFITZ-RNICH' is an invalid segment."); }
	if (route.includes("FIPEK BETIR") { errors.push("'FIPEK-BETIR' is an invalid segment."); }
	if (route.includes("FIPEK BETIR") { errors.push("'FIPEK-BETIR' is an invalid segment."); }
	if (route.includes("FITES LNY") { errors.push("'FITES-LNY' is an invalid segment."); }
	if (route.includes("FITES SAYTO") { errors.push("'FITES-SAYTO' is an invalid segment."); }
	if (route.includes("FLEUR POLTY") { errors.push("'FLEUR-POLTY' is an invalid segment."); }
	if (route.includes("FLM BURGG") { errors.push("'FLM-BURGG' is an invalid segment."); }
	if (route.includes("FLO CHACA") { errors.push("'FLO-CHACA' is an invalid segment."); }
	if (route.includes("FLRDA EBEAR") { errors.push("'FLRDA-EBEAR' is an invalid segment."); }
	if (route.includes("FLRDA EBEAR") { errors.push("'FLRDA-EBEAR' is an invalid segment."); }
	if (route.includes("FLW MCKEY") { errors.push("'FLW-MCKEY' is an invalid segment."); }
	if (route.includes("FOLZZ ZFP") { errors.push("'FOLZZ-ZFP' is an invalid segment."); }
	if (route.includes("FOLZZ GOZZR") { errors.push("'FOLZZ-GOZZR' is an invalid segment."); }
	if (route.includes("FORSS GUTZZ") { errors.push("'FORSS-GUTZZ' is an invalid segment."); }
	if (route.includes("FRAME PIXEY") { errors.push("'FRAME-PIXEY' is an invalid segment."); }
	if (route.includes("FRILL TUSKY") { errors.push("'FRILL-TUSKY' is an invalid segment."); }
	if (route.includes("FRNCK MLF") { errors.push("'FRNCK-MLF' is an invalid segment."); }
	if (route.includes("FRRAM ANNVA") { errors.push("'FRRAM-ANNVA' is an invalid segment."); }
	if (route.includes("GABAR ANU") { errors.push("'GABAR-ANU' is an invalid segment."); }
	if (route.includes("GAHAM YXY") { errors.push("'GAHAM-YXY' is an invalid segment."); }
	if (route.includes("GARED RIDGY") { errors.push("'GARED-RIDGY' is an invalid segment."); }
	if (route.includes("GARIC CLOWR") { errors.push("'GARIC-CLOWR' is an invalid segment."); }
	if (route.includes("GECAL JIMAC") { errors.push("'GECAL-JIMAC' is an invalid segment."); }
	if (route.includes("GECAL SEAVR") { errors.push("'GECAL-SEAVR' is an invalid segment."); }
	if (route.includes("GECAL ANVER") { errors.push("'GECAL-ANVER' is an invalid segment."); }
	if (route.includes("GECAL AMENO") { errors.push("'GECAL-AMENO' is an invalid segment."); }
	if (route.includes("GECAL LAZEY") { errors.push("'GECAL-LAZEY' is an invalid segment."); }
	if (route.includes("GECAL WINGZ") { errors.push("'GECAL-WINGZ' is an invalid segment."); }
	if (route.includes("GECAL ENAPI") { errors.push("'GECAL-ENAPI' is an invalid segment."); }
	if (route.includes("GECAL BOVIC") { errors.push("'GECAL-BOVIC' is an invalid segment."); }
	if (route.includes("GECAL NUMBR") { errors.push("'GECAL-NUMBR' is an invalid segment."); }
	if (route.includes("GECAL DASER") { errors.push("'GECAL-DASER' is an invalid segment."); }
	if (route.includes("GECAL BALOO") { errors.push("'GECAL-BALOO' is an invalid segment."); }
	if (route.includes("GETER STS") { errors.push("'GETER-STS' is an invalid segment."); }
	if (route.includes("GKN TKA") { errors.push("'GKN-TKA' is an invalid segment."); }
	if (route.includes("GKN TOVAD") { errors.push("'GKN-TOVAD' is an invalid segment."); }
	if (route.includes("GND ANADA") { errors.push("'GND-ANADA' is an invalid segment."); }
	if (route.includes("GOSIP BLOOM") { errors.push("'GOSIP-BLOOM' is an invalid segment."); }
	if (route.includes("FOXWD MIILS") { errors.push("'FOXWD-MIILS' is an invalid segment."); }
	if (route.includes("FOXWD DANOL") { errors.push("'FOXWD-DANOL' is an invalid segment."); }
	if (route.includes("FOXWD KANNI") { errors.push("'FOXWD-KANNI' is an invalid segment."); }
	if (route.includes("FOXWD TUSKY") { errors.push("'FOXWD-TUSKY' is an invalid segment."); }
	if (route.includes("FOXWD EBONY") { errors.push("'FOXWD-EBONY' is an invalid segment."); }
	if (route.includes("FOXWD TOPPS") { errors.push("'FOXWD-TOPPS' is an invalid segment."); }
	if (route.includes("FOXWD WHALE") { errors.push("'FOXWD-WHALE' is an invalid segment."); }
	if (route.includes("FOXWD BRADD") { errors.push("'FOXWD-BRADD' is an invalid segment."); }
	if (route.includes("FOXWD VITOL") { errors.push("'FOXWD-VITOL' is an invalid segment."); }
	if (route.includes("FOXWD ENE") { errors.push("'FOXWD-ENE' is an invalid segment."); }
	if (route.includes("FOXWD MIILS") { errors.push("'FOXWD-MIILS' is an invalid segment."); }
	if (route.includes("FOXWD TAFFY") { errors.push("'FOXWD-TAFFY' is an invalid segment."); }
	if (route.includes("FOXWD ALLEX") { errors.push("'FOXWD-ALLEX' is an invalid segment."); }
	if (route.includes("FRALK TVC") { errors.push("'FRALK-TVC' is an invalid segment."); }
	if (route.includes("FRILL VITOL") { errors.push("'FRILL-VITOL' is an invalid segment."); }
	if (route.includes("FRILL KANNI") { errors.push("'FRILL-KANNI' is an invalid segment."); }
	if (route.includes("FRILL ALLEX") { errors.push("'FRILL-ALLEX' is an invalid segment."); }
	if (route.includes("FRILL WHALE") { errors.push("'FRILL-WHALE' is an invalid segment."); }
	if (route.includes("FRILL BRADD") { errors.push("'FRILL-BRADD' is an invalid segment."); }
	if (route.includes("FTI TOTOE") { errors.push("'FTI-TOTOE' is an invalid segment."); }
	if (route.includes("GALIP DADIE") { errors.push("'GALIP-DADIE' is an invalid segment."); }
	if (route.includes("GALVN SUMRS") { errors.push("'GALVN-SUMRS' is an invalid segment."); }
	if (route.includes("GARIC OKNEE") { errors.push("'GARIC-OKNEE' is an invalid segment."); }
	if (route.includes("GARIC TORQD") { errors.push("'GARIC-TORQD' is an invalid segment."); }
	if (route.includes("GAYBL BRADD") { errors.push("'GAYBL-BRADD' is an invalid segment."); }
	if (route.includes("GEECE BNE") { errors.push("'GEECE-BNE' is an invalid segment."); }
	if (route.includes("GEGIS STA") { errors.push("'GEGIS-STA' is an invalid segment."); }
	if (route.includes("GESSO GEECE") { errors.push("'GESSO-GEECE' is an invalid segment."); }
	if (route.includes("GIIBS SEELA") { errors.push("'GIIBS-SEELA' is an invalid segment."); }
	if (route.includes("GIKPU PADUD") { errors.push("'GIKPU-PADUD' is an invalid segment."); }
	if (route.includes("GLD SNY") { errors.push("'GLD-SNY' is an invalid segment."); }
	if (route.includes("GNZZO RYDRR") { errors.push("'GNZZO-RYDRR' is an invalid segment."); }
	if (route.includes("GOATR RANGY") { errors.push("'GOATR-RANGY' is an invalid segment."); }
	if (route.includes("GOBBY BETTI") { errors.push("'GOBBY-BETTI' is an invalid segment."); }
	if (route.includes("GOBBY MARCS") { errors.push("'GOBBY-MARCS' is an invalid segment."); }
	if (route.includes("GOSAM GOW") { errors.push("'GOSAM-GOW' is an invalid segment."); }
	if (route.includes("GOSEP MIBNA") { errors.push("'GOSEP-MIBNA' is an invalid segment."); }
	if (route.includes("GOSEP FEDDY") { errors.push("'GOSEP-FEDDY' is an invalid segment."); }
	if (route.includes("GOSEP DUGNO") { errors.push("'GOSEP-DUGNO' is an invalid segment."); }
	if (route.includes("GOSEP YER") { errors.push("'GOSEP-YER' is an invalid segment."); }
	if (route.includes("GOSEP FASSA") { errors.push("'GOSEP-FASSA' is an invalid segment."); }
	if (route.includes("GOSEP TEFFO") { errors.push("'GOSEP-TEFFO' is an invalid segment."); }
	if (route.includes("GOSEP LOPVI") { errors.push("'GOSEP-LOPVI' is an invalid segment."); }
	if (route.includes("GOSEP SEMTO") { errors.push("'GOSEP-SEMTO' is an invalid segment."); }
	if (route.includes("GOSEP RODBO") { errors.push("'GOSEP-RODBO' is an invalid segment."); }
	if (route.includes("GOSEP JELCO") { errors.push("'GOSEP-JELCO' is an invalid segment."); }
	if (route.includes("GOSEP MOGAG") { errors.push("'GOSEP-MOGAG' is an invalid segment."); }
	if (route.includes("GOSEP IRBUX") { errors.push("'GOSEP-IRBUX' is an invalid segment."); }
	if (route.includes("GOSEP YTE") { errors.push("'GOSEP-YTE' is an invalid segment."); }
	if (route.includes("GOSEP YXZ") { errors.push("'GOSEP-YXZ' is an invalid segment."); }
	if (route.includes("GOSEP TUNNI") { errors.push("'GOSEP-TUNNI' is an invalid segment."); }
	if (route.includes("GOSEP YTL") { errors.push("'GOSEP-YTL' is an invalid segment."); }
	if (route.includes("GOSEP YPO") { errors.push("'GOSEP-YPO' is an invalid segment."); }
	if (route.includes("GOSEP ELVEL") { errors.push("'GOSEP-ELVEL' is an invalid segment."); }
	if (route.includes("GOSEP BODRA") { errors.push("'GOSEP-BODRA' is an invalid segment."); }
	if (route.includes("GOSEP HINGE") { errors.push("'GOSEP-HINGE' is an invalid segment."); }
	if (route.includes("GOSEP GRIBS") { errors.push("'GOSEP-GRIBS' is an invalid segment."); }
	if (route.includes("GOSEP ALSAK") { errors.push("'GOSEP-ALSAK' is an invalid segment."); }
	if (route.includes("GOSEP GRAND") { errors.push("'GOSEP-GRAND' is an invalid segment."); }
	if (route.includes("GOSEP MISAX") { errors.push("'GOSEP-MISAX' is an invalid segment."); }
	if (route.includes("GOSEP YZS") { errors.push("'GOSEP-YZS' is an invalid segment."); }
	if (route.includes("GOSEP YQT") { errors.push("'GOSEP-YQT' is an invalid segment."); }
	if (route.includes("GRI ADRUM") { errors.push("'GRI-ADRUM' is an invalid segment."); }
	if (route.includes("GRIPE GRAYM") { errors.push("'GRIPE-GRAYM' is an invalid segment."); }
	if (route.includes("GUA PAGON") { errors.push("'GUA-PAGON' is an invalid segment."); }
	if (route.includes("GUDEN YXY") { errors.push("'GUDEN-YXY' is an invalid segment."); }
	if (route.includes("GUDEN YXJ") { errors.push("'GUDEN-YXJ' is an invalid segment."); }
	if (route.includes("GUP HAHAA") { errors.push("'GUP-HAHAA' is an invalid segment."); }
	if (route.includes("HAPIE YAHOO") { errors.push("'HAPIE-YAHOO' is an invalid segment."); }
	if (route.includes("HAQHY BATTZ") { errors.push("'HAQHY-BATTZ' is an invalid segment."); }
	if (route.includes("HAR THS") { errors.push("'HAR-THS' is an invalid segment."); }
	if (route.includes("HELON BRISS") { errors.push("'HELON-BRISS' is an invalid segment."); }
	if (route.includes("HEPVA GCK") { errors.push("'HEPVA-GCK' is an invalid segment."); }
	if (route.includes("HEVAN HEPAR") { errors.push("'HEVAN-HEPAR' is an invalid segment."); }
	if (route.includes("HEZ NIFTE") { errors.push("'HEZ-NIFTE' is an invalid segment."); }
	if (route.includes("HGO SXW") { errors.push("'HGO-SXW' is an invalid segment."); }
	if (route.includes("HGO HALEN") { errors.push("'HGO-HALEN' is an invalid segment."); }
	if (route.includes("HINGE JANJO") { errors.push("'HINGE-JANJO' is an invalid segment."); }
	if (route.includes("HLV YOJCO") { errors.push("'HLV-YOJCO' is an invalid segment."); }
	if (route.includes("HLV YOJCO") { errors.push("'HLV-YOJCO' is an invalid segment."); }
	if (route.includes("HLV YOJCO") { errors.push("'HLV-YOJCO' is an invalid segment."); }
	if (route.includes("HLV YOJCO") { errors.push("'HLV-YOJCO' is an invalid segment."); }
	if (route.includes("HLV RUGBB") { errors.push("'HLV-RUGBB' is an invalid segment."); }
	if (route.includes("HNK RIMBA") { errors.push("'HNK-RIMBA' is an invalid segment."); }
	if (route.includes("HOCEE RSK") { errors.push("'HOCEE-RSK' is an invalid segment."); }
	if (route.includes("HOCEE FTI") { errors.push("'HOCEE-FTI' is an invalid segment."); }
	if (route.includes("HOCEE TBC") { errors.push("'HOCEE-TBC' is an invalid segment."); }
	if (route.includes("HOCEE IFI") { errors.push("'HOCEE-IFI' is an invalid segment."); }
	if (route.includes("HOCEE TANER") { errors.push("'HOCEE-TANER' is an invalid segment."); }
	if (route.includes("HOIST BAREE") { errors.push("'HOIST-BAREE' is an invalid segment."); }
	if (route.includes("HOIST DUVBI") { errors.push("'HOIST-DUVBI' is an invalid segment."); }
	if (route.includes("HOIST YYR") { errors.push("'HOIST-YYR' is an invalid segment."); }
	if (route.includes("HOIST FOXXE") { errors.push("'HOIST-FOXXE' is an invalid segment."); }
	if (route.includes("HOIST TIGIP") { errors.push("'HOIST-TIGIP' is an invalid segment."); }
	if (route.includes("HRV BAGGY") { errors.push("'HRV-BAGGY' is an invalid segment."); }
	if (route.includes("HRV BAGGS") { errors.push("'HRV-BAGGS' is an invalid segment."); }
	if (route.includes("HUDAD BGD") { errors.push("'HUDAD-BGD' is an invalid segment."); }
	if (route.includes("GOZZR UKOKA") { errors.push("'GOZZR-UKOKA' is an invalid segment."); }
	if (route.includes("GOZZR SNAGY") { errors.push("'GOZZR-SNAGY' is an invalid segment."); }
	if (route.includes("GOZZR SUMRS") { errors.push("'GOZZR-SUMRS' is an invalid segment."); }
	if (route.includes("GOZZR CANIT") { errors.push("'GOZZR-CANIT' is an invalid segment."); }
	if (route.includes("GRAHM BADDI") { errors.push("'GRAHM-BADDI' is an invalid segment."); }
	if (route.includes("GRAHM BADDI") { errors.push("'GRAHM-BADDI' is an invalid segment."); }
	if (route.includes("GRAMP TVC") { errors.push("'GRAMP-TVC' is an invalid segment."); }
	if (route.includes("GRATX MORME") { errors.push("'GRATX-MORME' is an invalid segment."); }
	if (route.includes("GRAZN PROWD") { errors.push("'GRAZN-PROWD' is an invalid segment."); }
	if (route.includes("GRN HMO") { errors.push("'GRN-HMO' is an invalid segment."); }
	if (route.includes("GROGU FICKY") { errors.push("'GROGU-FICKY' is an invalid segment."); }
	if (route.includes("GSO CHSLY") { errors.push("'GSO-CHSLY' is an invalid segment."); }
	if (route.includes("GTH WLLTR") { errors.push("'GTH-WLLTR' is an invalid segment."); }
	if (route.includes("GUFFS THMPR") { errors.push("'GUFFS-THMPR' is an invalid segment."); }
	if (route.includes("GUNTI PORLI") { errors.push("'GUNTI-PORLI' is an invalid segment."); }
	if (route.includes("GW SSM") { errors.push("'GW-SSM' is an invalid segment."); }
	if (route.includes("HAGIT MEEGL") { errors.push("'HAGIT-MEEGL' is an invalid segment."); }
	if (route.includes("HARKI NOGAL") { errors.push("'HARKI-NOGAL' is an invalid segment."); }
	if (route.includes("HELDE LHS") { errors.push("'HELDE-LHS' is an invalid segment."); }
	if (route.includes("HENDY POLTY") { errors.push("'HENDY-POLTY' is an invalid segment."); }
	if (route.includes("HIAKA UPP") { errors.push("'HIAKA-UPP' is an invalid segment."); }
	if (route.includes("HIXOR NIPPI") { errors.push("'HIXOR-NIPPI' is an invalid segment."); }
	if (route.includes("HML VBI") { errors.push("'HML-VBI' is an invalid segment."); }
	if (route.includes("HMO MIDRA") { errors.push("'HMO-MIDRA' is an invalid segment."); }
	if (route.includes("HOARY CRP") { errors.push("'HOARY-CRP' is an invalid segment."); }
	if (route.includes("HODDY EDWAL") { errors.push("'HODDY-EDWAL' is an invalid segment."); }
	if (route.includes("HOOCK SJI") { errors.push("'HOOCK-SJI' is an invalid segment."); }
	if (route.includes("HOWRR OFORD") { errors.push("'HOWRR-OFORD' is an invalid segment."); }
	if (route.includes("HRISN NESTS") { errors.push("'HRISN-NESTS' is an invalid segment."); }
	if (route.includes("HRRBR PHRED") { errors.push("'HRRBR-PHRED' is an invalid segment."); }
	if (route.includes("HRRBR CURIV") { errors.push("'HRRBR-CURIV' is an invalid segment."); }
	if (route.includes("HRV IZAAC") { errors.push("'HRV-IZAAC' is an invalid segment."); }
	if (route.includes("HULTN JIGZY") { errors.push("'HULTN-JIGZY' is an invalid segment."); }
	if (route.includes("HULTN FRIAR") { errors.push("'HULTN-FRIAR' is an invalid segment."); }
	if (route.includes("HULTN BGR") { errors.push("'HULTN-BGR' is an invalid segment."); }
	if (route.includes("HULTN ENE") { errors.push("'HULTN-ENE' is an invalid segment."); }
	if (route.includes("HULTN TOXAL") { errors.push("'HULTN-TOXAL' is an invalid segment."); }
	if (route.includes("HULTN REVEN") { errors.push("'HULTN-REVEN' is an invalid segment."); }
	if (route.includes("HULTN CABCI") { errors.push("'HULTN-CABCI' is an invalid segment."); }
	if (route.includes("HULTN VINDI") { errors.push("'HULTN-VINDI' is an invalid segment."); }
	if (route.includes("HULTN KERVO") { errors.push("'HULTN-KERVO' is an invalid segment."); }
	if (route.includes("HULTN KJOHN") { errors.push("'HULTN-KJOHN' is an invalid segment."); }
	if (route.includes("HULZE RALLS") { errors.push("'HULZE-RALLS' is an invalid segment."); }
	if (route.includes("HULZE CISAP") { errors.push("'HULZE-CISAP' is an invalid segment."); }
	if (route.includes("IBERG BRADD") { errors.push("'IBERG-BRADD' is an invalid segment."); }
	if (route.includes("ICEEE UNK") { errors.push("'ICEEE-UNK' is an invalid segment."); }
	if (route.includes("IDECA TUVOC") { errors.push("'IDECA-TUVOC' is an invalid segment."); }
	if (route.includes("IDNEY YYJ") { errors.push("'IDNEY-YYJ' is an invalid segment."); }
	if (route.includes("IGSOM SSR") { errors.push("'IGSOM-SSR' is an invalid segment."); }
	if (route.includes("IKBAN GDL") { errors.push("'IKBAN-GDL' is an invalid segment."); }
	if (route.includes("ILM SAGGY") { errors.push("'ILM-SAGGY' is an invalid segment."); }
	if (route.includes("INK EWM") { errors.push("'INK-EWM' is an invalid segment."); }
	if (route.includes("INW HAHAA") { errors.push("'INW-HAHAA' is an invalid segment."); }
	if (route.includes("INW HOXOL") { errors.push("'INW-HOXOL' is an invalid segment."); }
	if (route.includes("IPIME BGD") { errors.push("'IPIME-BGD' is an invalid segment."); }
	if (route.includes("IRBIM MAXAR") { errors.push("'IRBIM-MAXAR' is an invalid segment."); }
	if (route.includes("IRLOK BAREE") { errors.push("'IRLOK-BAREE' is an invalid segment."); }
	if (route.includes("IRLOK OMTOL") { errors.push("'IRLOK-OMTOL' is an invalid segment."); }
	if (route.includes("ISLES JOBOC") { errors.push("'ISLES-JOBOC' is an invalid segment."); }
	if (route.includes("IWANY BEZOV") { errors.push("'IWANY-BEZOV' is an invalid segment."); }
	if (route.includes("JAINS FRRAM") { errors.push("'JAINS-FRRAM' is an invalid segment."); }
	if (route.includes("JAKES PMM") { errors.push("'JAKES-PMM' is an invalid segment."); }
	if (route.includes("JANJO HINGE") { errors.push("'JANJO-HINGE' is an invalid segment."); }
	if (route.includes("JAROM TALGO") { errors.push("'JAROM-TALGO' is an invalid segment."); }
	if (route.includes("JCT EWC") { errors.push("'JCT-EWC' is an invalid segment."); }
	if (route.includes("JHW PONCT") { errors.push("'JHW-PONCT' is an invalid segment."); }
	if (route.includes("JIMAC GECAL") { errors.push("'JIMAC-GECAL' is an invalid segment."); }
	if (route.includes("JIMAC LAZEY") { errors.push("'JIMAC-LAZEY' is an invalid segment."); }
	if (route.includes("JIMAC NUMBR") { errors.push("'JIMAC-NUMBR' is an invalid segment."); }
	if (route.includes("JIMAC BALOO") { errors.push("'JIMAC-BALOO' is an invalid segment."); }
	if (route.includes("JIMAC WINGZ") { errors.push("'JIMAC-WINGZ' is an invalid segment."); }
	if (route.includes("JIMAC ANVER") { errors.push("'JIMAC-ANVER' is an invalid segment."); }
	if (route.includes("JIMAC PIREX") { errors.push("'JIMAC-PIREX' is an invalid segment."); }
	if (route.includes("JIMAC SEAVR") { errors.push("'JIMAC-SEAVR' is an invalid segment."); }
	if (route.includes("JOBOC MARIG") { errors.push("'JOBOC-MARIG' is an invalid segment."); }
	if (route.includes("JOBOC ACK") { errors.push("'JOBOC-ACK' is an invalid segment."); }
	if (route.includes("JOBOC LYNUS") { errors.push("'JOBOC-LYNUS' is an invalid segment."); }
	if (route.includes("JOBOC ZIBUT") { errors.push("'JOBOC-ZIBUT' is an invalid segment."); }
	if (route.includes("HUULL GNZZO") { errors.push("'HUULL-GNZZO' is an invalid segment."); }
	if (route.includes("HYDRO TURKI") { errors.push("'HYDRO-TURKI' is an invalid segment."); }
	if (route.includes("ICT TOTOE") { errors.push("'ICT-TOTOE' is an invalid segment."); }
	if (route.includes("ICT FSD") { errors.push("'ICT-FSD' is an invalid segment."); }
	if (route.includes("ICT ZOURA") { errors.push("'ICT-ZOURA' is an invalid segment."); }
	if (route.includes("IDOLS CARPX") { errors.push("'IDOLS-CARPX' is an invalid segment."); }
	if (route.includes("IDOLS SKARP") { errors.push("'IDOLS-SKARP' is an invalid segment."); }
	if (route.includes("IGSAS KAPUX") { errors.push("'IGSAS-KAPUX' is an invalid segment."); }
	if (route.includes("IGSAS POLTY") { errors.push("'IGSAS-POLTY' is an invalid segment."); }
	if (route.includes("IGSAS REEDO") { errors.push("'IGSAS-REEDO' is an invalid segment."); }
	if (route.includes("IGSAS LETAK") { errors.push("'IGSAS-LETAK' is an invalid segment."); }
	if (route.includes("ILAMA HMO") { errors.push("'ILAMA-HMO' is an invalid segment."); }
	if (route.includes("ILUGO SSM") { errors.push("'ILUGO-SSM' is an invalid segment."); }
	if (route.includes("IMORI POLTY") { errors.push("'IMORI-POLTY' is an invalid segment."); }
	if (route.includes("IORIO POKEG") { errors.push("'IORIO-POKEG' is an invalid segment."); }
	if (route.includes("IRBUX LEXIG") { errors.push("'IRBUX-LEXIG' is an invalid segment."); }
	if (route.includes("IRBUX MIBNO") { errors.push("'IRBUX-MIBNO' is an invalid segment."); }
	if (route.includes("IRBUX MUSLO") { errors.push("'IRBUX-MUSLO' is an invalid segment."); }
	if (route.includes("IRBUX RODBO") { errors.push("'IRBUX-RODBO' is an invalid segment."); }
	if (route.includes("IRLOK ANATI") { errors.push("'IRLOK-ANATI' is an invalid segment."); }
	if (route.includes("ITLUV GND") { errors.push("'ITLUV-GND' is an invalid segment."); }
	if (route.includes("IZAAC FTZ") { errors.push("'IZAAC-FTZ' is an invalid segment."); }
	if (route.includes("IZAAC HRISN") { errors.push("'IZAAC-HRISN' is an invalid segment."); }
	if (route.includes("JAGOR RUMFO") { errors.push("'JAGOR-RUMFO' is an invalid segment."); }
	if (route.includes("JANJO YWK") { errors.push("'JANJO-YWK' is an invalid segment."); }
	if (route.includes("JAZZI GARIC") { errors.push("'JAZZI-GARIC' is an invalid segment."); }
	if (route.includes("JAZZI SAGGY") { errors.push("'JAZZI-SAGGY' is an invalid segment."); }
	if (route.includes("JEBBY CARAC") { errors.push("'JEBBY-CARAC' is an invalid segment."); }
	if (route.includes("JENYY SOORY") { errors.push("'JENYY-SOORY' is an invalid segment."); }
	if (route.includes("JENYY MARIG") { errors.push("'JENYY-MARIG' is an invalid segment."); }
	if (route.includes("JENYY TILED") { errors.push("'JENYY-TILED' is an invalid segment."); }
	if (route.includes("JFK FOXWD") { errors.push("'JFK-FOXWD' is an invalid segment."); }
	if (route.includes("JHW TOPPS") { errors.push("'JHW-TOPPS' is an invalid segment."); }
	if (route.includes("JIGEB DISCO") { errors.push("'JIGEB-DISCO' is an invalid segment."); }
	if (route.includes("JIGEB DISCO") { errors.push("'JIGEB-DISCO' is an invalid segment."); }
	if (route.includes("JIJLU DALEY") { errors.push("'JIJLU-DALEY' is an invalid segment."); }
	if (route.includes("JOBOC KENDA") { errors.push("'JOBOC-KENDA' is an invalid segment."); }
	if (route.includes("JOBOC MUNEY") { errors.push("'JOBOC-MUNEY' is an invalid segment."); }
	if (route.includes("JOBOC KINGG") { errors.push("'JOBOC-KINGG' is an invalid segment."); }
	if (route.includes("JOVIE HENDY") { errors.push("'JOVIE-HENDY' is an invalid segment."); }
	if (route.includes("JOXIT RANEY") { errors.push("'JOXIT-RANEY' is an invalid segment."); }
	if (route.includes("JUDDS BRISS") { errors.push("'JUDDS-BRISS' is an invalid segment."); }
	if (route.includes("JURSY EPENE") { errors.push("'JURSY-EPENE' is an invalid segment."); }
	if (route.includes("JVL BAE") { errors.push("'JVL-BAE' is an invalid segment."); }
	if (route.includes("KARET TNT") { errors.push("'KARET-TNT' is an invalid segment."); }
	if (route.includes("KENDA LARGE") { errors.push("'KENDA-LARGE' is an invalid segment."); }
	if (route.includes("KINCH JETSS") { errors.push("'KINCH-JETSS' is an invalid segment."); }
	if (route.includes("JOBOC LACKS") { errors.push("'JOBOC-LACKS' is an invalid segment."); }
	if (route.includes("JOELE NAPUA") { errors.push("'JOELE-NAPUA' is an invalid segment."); }
	if (route.includes("JOELE SHARK") { errors.push("'JOELE-SHARK' is an invalid segment."); }
	if (route.includes("JOELE TOADS") { errors.push("'JOELE-TOADS' is an invalid segment."); }
	if (route.includes("JONEZ CHMLI") { errors.push("'JONEZ-CHMLI' is an invalid segment."); }
	if (route.includes("JOVIE CUDDY") { errors.push("'JOVIE-CUDDY' is an invalid segment."); }
	if (route.includes("JOXUL TURKI") { errors.push("'JOXUL-TURKI' is an invalid segment."); }
	if (route.includes("JUNVA TURKI") { errors.push("'JUNVA-TURKI' is an invalid segment."); }
	if (route.includes("KALMO NOSTO") { errors.push("'KALMO-NOSTO' is an invalid segment."); }
	if (route.includes("KANNI RIFLE") { errors.push("'KANNI-RIFLE' is an invalid segment."); }
	if (route.includes("KANUR LETAK") { errors.push("'KANUR-LETAK' is an invalid segment."); }
	if (route.includes("KAPUX HOCKE") { errors.push("'KAPUX-HOCKE' is an invalid segment."); }
	if (route.includes("KASAR FLUPS") { errors.push("'KASAR-FLUPS' is an invalid segment."); }
	if (route.includes("KATTS KITTN") { errors.push("'KATTS-KITTN' is an invalid segment."); }
	if (route.includes("KATTS OAL") { errors.push("'KATTS-OAL' is an invalid segment."); }
	if (route.includes("KAVRA CUL") { errors.push("'KAVRA-CUL' is an invalid segment."); }
	if (route.includes("KAVRA ILAVI") { errors.push("'KAVRA-ILAVI' is an invalid segment."); }
	if (route.includes("KAYCC KYLOH") { errors.push("'KAYCC-KYLOH' is an invalid segment."); }
	if (route.includes("KEGRI LARIE") { errors.push("'KEGRI-LARIE' is an invalid segment."); }
	if (route.includes("KEMPR CHIEZ") { errors.push("'KEMPR-CHIEZ' is an invalid segment."); }
	if (route.includes("KEMPR PRONI") { errors.push("'KEMPR-PRONI' is an invalid segment."); }
	if (route.includes("KEMPR SKARP") { errors.push("'KEMPR-SKARP' is an invalid segment."); }
	if (route.includes("KEMPR CLAPY") { errors.push("'KEMPR-CLAPY' is an invalid segment."); }
	if (route.includes("KEMSA DUGNO") { errors.push("'KEMSA-DUGNO' is an invalid segment."); }
	if (route.includes("KEMSA DUMRU") { errors.push("'KEMSA-DUMRU' is an invalid segment."); }
	if (route.includes("KEMSA BODRA") { errors.push("'KEMSA-BODRA' is an invalid segment."); }
	if (route.includes("KEMSA SOCAN") { errors.push("'KEMSA-SOCAN' is an invalid segment."); }
	if (route.includes("KEMSA PELEE") { errors.push("'KEMSA-PELEE' is an invalid segment."); }
	if (route.includes("KEMSA SEMTO") { errors.push("'KEMSA-SEMTO' is an invalid segment."); }
	if (route.includes("KEMSA MCKEE") { errors.push("'KEMSA-MCKEE' is an invalid segment."); }
	if (route.includes("KEMSA YPO") { errors.push("'KEMSA-YPO' is an invalid segment."); }
	if (route.includes("KEMSA SEGAN") { errors.push("'KEMSA-SEGAN' is an invalid segment."); }
	if (route.includes("KEMSA VEPTU") { errors.push("'KEMSA-VEPTU' is an invalid segment."); }
	if (route.includes("KEMSA LOPVI") { errors.push("'KEMSA-LOPVI' is an invalid segment."); }
	if (route.includes("KEMSA KELMU") { errors.push("'KEMSA-KELMU' is an invalid segment."); }
	if (route.includes("KEMSA VIRSO") { errors.push("'KEMSA-VIRSO' is an invalid segment."); }
	if (route.includes("KEMSA KLIPS") { errors.push("'KEMSA-KLIPS' is an invalid segment."); }
	if (route.includes("KEMSA YMO") { errors.push("'KEMSA-YMO' is an invalid segment."); }
	if (route.includes("KEMSA BEPEG") { errors.push("'KEMSA-BEPEG' is an invalid segment."); }
	if (route.includes("KEMSA MOGAG") { errors.push("'KEMSA-MOGAG' is an invalid segment."); }
	if (route.includes("KEMSA HELMO") { errors.push("'KEMSA-HELMO' is an invalid segment."); }
	if (route.includes("KEMSA SSM") { errors.push("'KEMSA-SSM' is an invalid segment."); }
	if (route.includes("KEMSA YPL") { errors.push("'KEMSA-YPL' is an invalid segment."); }
	if (route.includes("KEMSA FEDDY") { errors.push("'KEMSA-FEDDY' is an invalid segment."); }
	if (route.includes("KEMSA YGL") { errors.push("'KEMSA-YGL' is an invalid segment."); }
	if (route.includes("KEMSA FASSA") { errors.push("'KEMSA-FASSA' is an invalid segment."); }
	if (route.includes("KEMSA YTE") { errors.push("'KEMSA-YTE' is an invalid segment."); }
	if (route.includes("KEMSA TADIS") { errors.push("'KEMSA-TADIS' is an invalid segment."); }
	if (route.includes("KEMSA YXZ") { errors.push("'KEMSA-YXZ' is an invalid segment."); }
	if (route.includes("KEMSA DAXER") { errors.push("'KEMSA-DAXER' is an invalid segment."); }
	if (route.includes("KEMSA YQT") { errors.push("'KEMSA-YQT' is an invalid segment."); }
	if (route.includes("KEMSA YTL") { errors.push("'KEMSA-YTL' is an invalid segment."); }
	if (route.includes("KEMSA DUSMA") { errors.push("'KEMSA-DUSMA' is an invalid segment."); }
	if (route.includes("KEMSA MIBNA") { errors.push("'KEMSA-MIBNA' is an invalid segment."); }
	if (route.includes("KEMSA YER") { errors.push("'KEMSA-YER' is an invalid segment."); }
	if (route.includes("KEMSA IMIXA") { errors.push("'KEMSA-IMIXA' is an invalid segment."); }
	if (route.includes("KEMSA MEPNI") { errors.push("'KEMSA-MEPNI' is an invalid segment."); }
	if (route.includes("KEMSA RODBO") { errors.push("'KEMSA-RODBO' is an invalid segment."); }
	if (route.includes("KEMSA GRAVO") { errors.push("'KEMSA-GRAVO' is an invalid segment."); }
	if (route.includes("KEMSA PIGLA") { errors.push("'KEMSA-PIGLA' is an invalid segment."); }
	if (route.includes("KEMSA YXL") { errors.push("'KEMSA-YXL' is an invalid segment."); }
	if (route.includes("KEMSA MEMSO") { errors.push("'KEMSA-MEMSO' is an invalid segment."); }
	if (route.includes("KEMSA MIBNO") { errors.push("'KEMSA-MIBNO' is an invalid segment."); }
	if (route.includes("KEMSA GRAND") { errors.push("'KEMSA-GRAND' is an invalid segment."); }
	if (route.includes("KEMSA COPUR") { errors.push("'KEMSA-COPUR' is an invalid segment."); }
	if (route.includes("KENGS VUH") { errors.push("'KENGS-VUH' is an invalid segment."); }
	if (route.includes("KENLU POLTY") { errors.push("'KENLU-POLTY' is an invalid segment."); }
	if (route.includes("KENLU LETAK") { errors.push("'KENLU-LETAK' is an invalid segment."); }
	if (route.includes("KETLA RODBO") { errors.push("'KETLA-RODBO' is an invalid segment."); }
	if (route.includes("KETLA MIBNO") { errors.push("'KETLA-MIBNO' is an invalid segment."); }
	if (route.includes("KEYLO GEEKY") { errors.push("'KEYLO-GEEKY' is an invalid segment."); }
	if (route.includes("KIKUM ANRON") { errors.push("'KIKUM-ANRON' is an invalid segment."); }
	if (route.includes("KINER ANVER") { errors.push("'KINER-ANVER' is an invalid segment."); }
	if (route.includes("KINOL ISLAS") { errors.push("'KINOL-ISLAS' is an invalid segment."); }
	if (route.includes("KITTN ILC") { errors.push("'KITTN-ILC' is an invalid segment."); }
	if (route.includes("KOBET KERSO") { errors.push("'KOBET-KERSO' is an invalid segment."); }
	if (route.includes("KODIK BAREE") { errors.push("'KODIK-BAREE' is an invalid segment."); }
	if (route.includes("KOOKE CLETA") { errors.push("'KOOKE-CLETA' is an invalid segment."); }
	if (route.includes("KOTZA TKO") { errors.push("'KOTZA-TKO' is an invalid segment."); }
	if (route.includes("LARGE PENYT") { errors.push("'LARGE-PENYT' is an invalid segment."); }
	if (route.includes("LARGE JOBOC") { errors.push("'LARGE-JOBOC' is an invalid segment."); }
	if (route.includes("LASYO TBD") { errors.push("'LASYO-TBD' is an invalid segment."); }
	if (route.includes("LAZEY JIMAC") { errors.push("'LAZEY-JIMAC' is an invalid segment."); }
	if (route.includes("LAZEY SOORY") { errors.push("'LAZEY-SOORY' is an invalid segment."); }
	if (route.includes("LAZEY PIREX") { errors.push("'LAZEY-PIREX' is an invalid segment."); }
	if (route.includes("LAZEY SHEIL") { errors.push("'LAZEY-SHEIL' is an invalid segment."); }
	if (route.includes("LAZEY GECAL") { errors.push("'LAZEY-GECAL' is an invalid segment."); }
	if (route.includes("LAZEY BALTN") { errors.push("'LAZEY-BALTN' is an invalid segment."); }
	if (route.includes("LAZEY SEAVR") { errors.push("'LAZEY-SEAVR' is an invalid segment."); }
	if (route.includes("LAZEY BOVIC") { errors.push("'LAZEY-BOVIC' is an invalid segment."); }
	if (route.includes("LAZEY ANVER") { errors.push("'LAZEY-ANVER' is an invalid segment."); }
	if (route.includes("LAZEY ENAPI") { errors.push("'LAZEY-ENAPI' is an invalid segment."); }
	if (route.includes("LAZEY ANTIG") { errors.push("'LAZEY-ANTIG' is an invalid segment."); }
	if (route.includes("LAZEY DASER") { errors.push("'LAZEY-DASER' is an invalid segment."); }
	if (route.includes("LAZEY WINGZ") { errors.push("'LAZEY-WINGZ' is an invalid segment."); }
	if (route.includes("LEDRU UB") { errors.push("'LEDRU-UB' is an invalid segment."); }
	if (route.includes("LEV BAGGS") { errors.push("'LEV-BAGGS' is an invalid segment."); }
	if (route.includes("LFT LCH") { errors.push("'LFT-LCH' is an invalid segment."); }
	if (route.includes("LIH SHARK") { errors.push("'LIH-SHARK' is an invalid segment."); }
	if (route.includes("LINND KENDA") { errors.push("'LINND-KENDA' is an invalid segment."); }
	if (route.includes("LIVBA MATOR") { errors.push("'LIVBA-MATOR' is an invalid segment."); }
	if (route.includes("LLUND BAYYS") { errors.push("'LLUND-BAYYS' is an invalid segment."); }
	if (route.includes("LOMPI PORTI") { errors.push("'LOMPI-PORTI' is an invalid segment."); }
	if (route.includes("LOMSI BAREE") { errors.push("'LOMSI-BAREE' is an invalid segment."); }
	if (route.includes("LOMTA URTAK") { errors.push("'LOMTA-URTAK' is an invalid segment."); }
	if (route.includes("LRAIN SQIRE") { errors.push("'LRAIN-SQIRE' is an invalid segment."); }
	if (route.includes("LULOX GISTI") { errors.push("'LULOX-GISTI' is an invalid segment."); }
	if (route.includes("LUMES PINSO") { errors.push("'LUMES-PINSO' is an invalid segment."); }
	if (route.includes("LUMES NYMPH") { errors.push("'LUMES-NYMPH' is an invalid segment."); }
	if (route.includes("MADDY JVL") { errors.push("'MADDY-JVL' is an invalid segment."); }
	if (route.includes("KIPAL CUU") { errors.push("'KIPAL-CUU' is an invalid segment."); }
	if (route.includes("KISUK LETAK") { errors.push("'KISUK-LETAK' is an invalid segment."); }
	if (route.includes("KITOK SSM") { errors.push("'KITOK-SSM' is an invalid segment."); }
	if (route.includes("KITTN KATTS") { errors.push("'KITTN-KATTS' is an invalid segment."); }
	if (route.includes("KJOHN BIZEX") { errors.push("'KJOHN-BIZEX' is an invalid segment."); }
	if (route.includes("KMC TRE") { errors.push("'KMC-TRE' is an invalid segment."); }
	if (route.includes("KOATA RIELY") { errors.push("'KOATA-RIELY' is an invalid segment."); }
	if (route.includes("KOATA NEERO") { errors.push("'KOATA-NEERO' is an invalid segment."); }
	if (route.includes("KOFER PINDO") { errors.push("'KOFER-PINDO' is an invalid segment."); }
	if (route.includes("KOLAO ODUCA") { errors.push("'KOLAO-ODUCA' is an invalid segment."); }
	if (route.includes("KOLEA CLUTS") { errors.push("'KOLEA-CLUTS' is an invalid segment."); }
	if (route.includes("LAA TOTOE") { errors.push("'LAA-TOTOE' is an invalid segment."); }
	if (route.includes("LACKS LINND") { errors.push("'LACKS-LINND' is an invalid segment."); }
	if (route.includes("LAKES HELMO") { errors.push("'LAKES-HELMO' is an invalid segment."); }
	if (route.includes("LAMRO ATSIX") { errors.push("'LAMRO-ATSIX' is an invalid segment."); }
	if (route.includes("LAMSI STEIN") { errors.push("'LAMSI-STEIN' is an invalid segment."); }
	if (route.includes("LARGE SLATN") { errors.push("'LARGE-SLATN' is an invalid segment."); }
	if (route.includes("LAROS CBULA") { errors.push("'LAROS-CBULA' is an invalid segment."); }
	if (route.includes("LATIS BUTAL") { errors.push("'LATIS-BUTAL' is an invalid segment."); }
	if (route.includes("LBB HNKER") { errors.push("'LBB-HNKER' is an invalid segment."); }
	if (route.includes("LBB HYDRO") { errors.push("'LBB-HYDRO' is an invalid segment."); }
	if (route.includes("LEBOY CASHN") { errors.push("'LEBOY-CASHN' is an invalid segment."); }
	if (route.includes("LENAL VIBSI") { errors.push("'LENAL-VIBSI' is an invalid segment."); }
	if (route.includes("LENRI CBULA") { errors.push("'LENRI-CBULA' is an invalid segment."); }
	if (route.includes("LETAK ATENE") { errors.push("'LETAK-ATENE' is an invalid segment."); }
	if (route.includes("LEV IZAAC") { errors.push("'LEV-IZAAC' is an invalid segment."); }
	if (route.includes("LFANO KASAR") { errors.push("'LFANO-KASAR' is an invalid segment."); }
	if (route.includes("LGA WHITE") { errors.push("'LGA-WHITE' is an invalid segment."); }
	if (route.includes("LIBBO BRINY") { errors.push("'LIBBO-BRINY' is an invalid segment."); }
	if (route.includes("LIDOG LIMBO") { errors.push("'LIDOG-LIMBO' is an invalid segment."); }
	if (route.includes("LIH OUCHI") { errors.push("'LIH-OUCHI' is an invalid segment."); }
	if (route.includes("LILLS ETBUY") { errors.push("'LILLS-ETBUY' is an invalid segment."); }
	if (route.includes("LINND OWENZ") { errors.push("'LINND-OWENZ' is an invalid segment."); }
	if (route.includes("LOGVN RENAH") { errors.push("'LOGVN-RENAH' is an invalid segment."); }
	if (route.includes("LOMPI SUPRY") { errors.push("'LOMPI-SUPRY' is an invalid segment."); }
	if (route.includes("LOMSI YWK") { errors.push("'LOMSI-YWK' is an invalid segment."); }
	if (route.includes("LOOSE DEVAC") { errors.push("'LOOSE-DEVAC' is an invalid segment."); }
	if (route.includes("LORBA UCJ") { errors.push("'LORBA-UCJ' is an invalid segment."); }
	if (route.includes("LRD HFMAN") { errors.push("'LRD-HFMAN' is an invalid segment."); }
	if (route.includes("LUCTI LFANO") { errors.push("'LUCTI-LFANO' is an invalid segment."); }
	if (route.includes("LUCTI RUDLI") { errors.push("'LUCTI-RUDLI' is an invalid segment."); }
	if (route.includes("LYNUS MUNEY") { errors.push("'LYNUS-MUNEY' is an invalid segment."); }
	if (route.includes("LYNUS OKONU") { errors.push("'LYNUS-OKONU' is an invalid segment."); }
	if (route.includes("LYNUS OKONU") { errors.push("'LYNUS-OKONU' is an invalid segment."); }
	if (route.includes("LYONS TWF") { errors.push("'LYONS-TWF' is an invalid segment."); }
	if (route.includes("MALBE ABRAG") { errors.push("'MALBE-ABRAG' is an invalid segment."); }
	if (route.includes("MALIT IGOTT") { errors.push("'MALIT-IGOTT' is an invalid segment."); }
	if (route.includes("MARTN TAFFY") { errors.push("'MARTN-TAFFY' is an invalid segment."); }
	if (route.includes("MATOR LIVBA") { errors.push("'MATOR-LIVBA' is an invalid segment."); }
	if (route.includes("MAXAR IRBIM") { errors.push("'MAXAR-IRBIM' is an invalid segment."); }
	if (route.includes("MAXAR SINGA") { errors.push("'MAXAR-SINGA' is an invalid segment."); }
	if (route.includes("MCG ENM") { errors.push("'MCG-ENM' is an invalid segment."); }
	if (route.includes("MCG TKA") { errors.push("'MCG-TKA' is an invalid segment."); }
	if (route.includes("MELDI BAREE") { errors.push("'MELDI-BAREE' is an invalid segment."); }
	if (route.includes("MEMSO GRAMP") { errors.push("'MEMSO-GRAMP' is an invalid segment."); }
	if (route.includes("MICAS RENAH") { errors.push("'MICAS-RENAH' is an invalid segment."); }
	if (route.includes("MICAS LYMIN") { errors.push("'MICAS-LYMIN' is an invalid segment."); }
	if (route.includes("MIILS DEPRI") { errors.push("'MIILS-DEPRI' is an invalid segment."); }
	if (route.includes("MIILS OMALI") { errors.push("'MIILS-OMALI' is an invalid segment."); }
	if (route.includes("MIILS TOPPS") { errors.push("'MIILS-TOPPS' is an invalid segment."); }
	if (route.includes("MIILS ALLEX") { errors.push("'MIILS-ALLEX' is an invalid segment."); }
	if (route.includes("MIILS YQX") { errors.push("'MIILS-YQX' is an invalid segment."); }
	if (route.includes("MIILS ANTOV") { errors.push("'MIILS-ANTOV' is an invalid segment."); }
	if (route.includes("MIILS REVEN") { errors.push("'MIILS-REVEN' is an invalid segment."); }
	if (route.includes("MIILS PINTE") { errors.push("'MIILS-PINTE' is an invalid segment."); }
	if (route.includes("MIILS DERDO") { errors.push("'MIILS-DERDO' is an invalid segment."); }
	if (route.includes("MIILS SIKBO") { errors.push("'MIILS-SIKBO' is an invalid segment."); }
	if (route.includes("MIILS MIVAX") { errors.push("'MIILS-MIVAX' is an invalid segment."); }
	if (route.includes("MIILS MLT") { errors.push("'MIILS-MLT' is an invalid segment."); }
	if (route.includes("MIILS BGR") { errors.push("'MIILS-BGR' is an invalid segment."); }
	if (route.includes("MIILS PQI") { errors.push("'MIILS-PQI' is an invalid segment."); }
	if (route.includes("MIILS CABCI") { errors.push("'MIILS-CABCI' is an invalid segment."); }
	if (route.includes("MIILS KJOHN") { errors.push("'MIILS-KJOHN' is an invalid segment."); }
	if (route.includes("MIILS FRIAR") { errors.push("'MIILS-FRIAR' is an invalid segment."); }
	if (route.includes("MIKLE MOGAN") { errors.push("'MIKLE-MOGAN' is an invalid segment."); }
	if (route.includes("MIKLE LSU") { errors.push("'MIKLE-LSU' is an invalid segment."); }
	if (route.includes("MOGAN KNAPP") { errors.push("'MOGAN-KNAPP' is an invalid segment."); }
	if (route.includes("MOLLY EWM") { errors.push("'MOLLY-EWM' is an invalid segment."); }
	if (route.includes("MT BOKTO") { errors.push("'MT-BOKTO' is an invalid segment."); }
	if (route.includes("MUNOZ ANADA") { errors.push("'MUNOZ-ANADA' is an invalid segment."); }
	if (route.includes("MUSAK TAGRA") { errors.push("'MUSAK-TAGRA' is an invalid segment."); }
	if (route.includes("MWA DEVER") { errors.push("'MWA-DEVER' is an invalid segment."); }
	if (route.includes("MXE FLIRT") { errors.push("'MXE-FLIRT' is an invalid segment."); }
	if (route.includes("MZV TRIDE") { errors.push("'MZV-TRIDE' is an invalid segment."); }
	if (route.includes("MAM BRO") { errors.push("'MAM-BRO' is an invalid segment."); }
	if (route.includes("MARIG DRYED") { errors.push("'MARIG-DRYED' is an invalid segment."); }
	if (route.includes("MARIG ZIBUT") { errors.push("'MARIG-ZIBUT' is an invalid segment."); }
	if (route.includes("MARIG ATUGI") { errors.push("'MARIG-ATUGI' is an invalid segment."); }
	if (route.includes("MAXAR JELCO") { errors.push("'MAXAR-JELCO' is an invalid segment."); }
	if (route.includes("MAXAR DAXER") { errors.push("'MAXAR-DAXER' is an invalid segment."); }
	if (route.includes("MCKEY LIBBO") { errors.push("'MCKEY-LIBBO' is an invalid segment."); }
	if (route.includes("MDO HOM") { errors.push("'MDO-HOM' is an invalid segment."); }
	if (route.includes("MEANO ZIBUT") { errors.push("'MEANO-ZIBUT' is an invalid segment."); }
	if (route.includes("MEANO SOORY") { errors.push("'MEANO-SOORY' is an invalid segment."); }
	if (route.includes("MEGBE OTUPA") { errors.push("'MEGBE-OTUPA' is an invalid segment."); }
	if (route.includes("MEGBE OTEXA") { errors.push("'MEGBE-OTEXA' is an invalid segment."); }
	if (route.includes("MEGBE ILAMA") { errors.push("'MEGBE-ILAMA' is an invalid segment."); }
	if (route.includes("MEGBE MIDRA") { errors.push("'MEGBE-MIDRA' is an invalid segment."); }
	if (route.includes("MEMFS FTZ") { errors.push("'MEMFS-FTZ' is an invalid segment."); }
	if (route.includes("MGM THRSR") { errors.push("'MGM-THRSR' is an invalid segment."); }
	if (route.includes("MICAS RNDLY") { errors.push("'MICAS-RNDLY' is an invalid segment."); }
	if (route.includes("MICAS FODED") { errors.push("'MICAS-FODED' is an invalid segment."); }
	if (route.includes("MIDRA HMO") { errors.push("'MIDRA-HMO' is an invalid segment."); }
	if (route.includes("MIDRA VOKOM") { errors.push("'MIDRA-VOKOM' is an invalid segment."); }
	if (route.includes("MIILS AJJAY") { errors.push("'MIILS-AJJAY' is an invalid segment."); }
	if (route.includes("MIILS BEEKN") { errors.push("'MIILS-BEEKN' is an invalid segment."); }
	if (route.includes("MIILS KAYCC") { errors.push("'MIILS-KAYCC' is an invalid segment."); }
	if (route.includes("MIILS OBTEK") { errors.push("'MIILS-OBTEK' is an invalid segment."); }
	if (route.includes("MIILS LETAK") { errors.push("'MIILS-LETAK' is an invalid segment."); }
	if (route.includes("MILAT SIRIL") { errors.push("'MILAT-SIRIL' is an invalid segment."); }
	if (route.includes("MILLE RABAL") { errors.push("'MILLE-RABAL' is an invalid segment."); }
	if (route.includes("MKK FITES") { errors.push("'MKK-FITES' is an invalid segment."); }
	if (route.includes("MKK EBBER") { errors.push("'MKK-EBBER' is an invalid segment."); }
	if (route.includes("MMB TOTOE") { errors.push("'MMB-TOTOE' is an invalid segment."); }
	if (route.includes("MNDEZ FIPEK") { errors.push("'MNDEZ-FIPEK' is an invalid segment."); }
	if (route.includes("MNDEZ FIPEK") { errors.push("'MNDEZ-FIPEK' is an invalid segment."); }
	if (route.includes("MOGBO CHI") { errors.push("'MOGBO-CHI' is an invalid segment."); }
	if (route.includes("MOGBO KALMO") { errors.push("'MOGBO-KALMO' is an invalid segment."); }
	if (route.includes("MOLLR GAWKS") { errors.push("'MOLLR-GAWKS' is an invalid segment."); }
	if (route.includes("MQO HUULL") { errors.push("'MQO-HUULL' is an invalid segment."); }
	if (route.includes("MQO BURGL") { errors.push("'MQO-BURGL' is an invalid segment."); }
	if (route.includes("MQU BURGL") { errors.push("'MQU-BURGL' is an invalid segment."); }
	if (route.includes("MRF KIPAL") { errors.push("'MRF-KIPAL' is an invalid segment."); }
	if (route.includes("MRF FST") { errors.push("'MRF-FST' is an invalid segment."); }
	if (route.includes("MRF FST") { errors.push("'MRF-FST' is an invalid segment."); }
	if (route.includes("MT FRALK") { errors.push("'MT-FRALK' is an invalid segment."); }
	if (route.includes("MT REEDO") { errors.push("'MT-REEDO' is an invalid segment."); }
	if (route.includes("MT PUVOK") { errors.push("'MT-PUVOK' is an invalid segment."); }
	if (route.includes("MT SSM") { errors.push("'MT-SSM' is an invalid segment."); }
	if (route.includes("MUVOD LOGVN") { errors.push("'MUVOD-LOGVN' is an invalid segment."); }
	if (route.includes("MUVOD STAAL") { errors.push("'MUVOD-STAAL' is an invalid segment."); }
	if (route.includes("MZB OTAYY") { errors.push("'MZB-OTAYY' is an invalid segment."); }
	if (route.includes("NACYN BESSI") { errors.push("'NACYN-BESSI' is an invalid segment."); }
	if (route.includes("NADMA KAGLY") { errors.push("'NADMA-KAGLY' is an invalid segment."); }
	if (route.includes("NAITE CHRGE") { errors.push("'NAITE-CHRGE' is an invalid segment."); }
	if (route.includes("NALSI QUIRT") { errors.push("'NALSI-QUIRT' is an invalid segment."); }
	if (route.includes("NARNE LAA") { errors.push("'NARNE-LAA' is an invalid segment."); }
	if (route.includes("NEEKO BAREE") { errors.push("'NEEKO-BAREE' is an invalid segment."); }
	if (route.includes("NEFTU EMABU") { errors.push("'NEFTU-EMABU' is an invalid segment."); }
	if (route.includes("NICSO TAGRA") { errors.push("'NICSO-TAGRA' is an invalid segment."); }
	if (route.includes("NIFTY LAKES") { errors.push("'NIFTY-LAKES' is an invalid segment."); }
	if (route.includes("NUCAR OZENA") { errors.push("'NUCAR-OZENA' is an invalid segment."); }
	if (route.includes("NUMBR PIREX") { errors.push("'NUMBR-PIREX' is an invalid segment."); }
	if (route.includes("NUMBR DASER") { errors.push("'NUMBR-DASER' is an invalid segment."); }
	if (route.includes("NUMBR ANVER") { errors.push("'NUMBR-ANVER' is an invalid segment."); }
	if (route.includes("NUMBR GECAL") { errors.push("'NUMBR-GECAL' is an invalid segment."); }
	if (route.includes("NUMBR BALOO") { errors.push("'NUMBR-BALOO' is an invalid segment."); }
	if (route.includes("NUMBR ENAPI") { errors.push("'NUMBR-ENAPI' is an invalid segment."); }
	if (route.includes("NUMBR ANTIG") { errors.push("'NUMBR-ANTIG' is an invalid segment."); }
	if (route.includes("NUMBR SHEIL") { errors.push("'NUMBR-SHEIL' is an invalid segment."); }
	if (route.includes("NUMBR JIMAC") { errors.push("'NUMBR-JIMAC' is an invalid segment."); }
	if (route.includes("NUMBR SOORY") { errors.push("'NUMBR-SOORY' is an invalid segment."); }
	if (route.includes("NUMBR SEAVR") { errors.push("'NUMBR-SEAVR' is an invalid segment."); }
	if (route.includes("NUMBR BOVIC") { errors.push("'NUMBR-BOVIC' is an invalid segment."); }
	if (route.includes("NUMBR BALTN") { errors.push("'NUMBR-BALTN' is an invalid segment."); }
	if (route.includes("NYMPH SYA") { errors.push("'NYMPH-SYA' is an invalid segment."); }
	if (route.includes("OBK SAMME") { errors.push("'OBK-SAMME' is an invalid segment."); }
	if (route.includes("ODESA SWANN") { errors.push("'ODESA-SWANN' is an invalid segment."); }
	if (route.includes("OMALI KJOHN") { errors.push("'OMALI-KJOHN' is an invalid segment."); }
	if (route.includes("OMALI KJOHN") { errors.push("'OMALI-KJOHN' is an invalid segment."); }
	if (route.includes("OPENS HMV") { errors.push("'OPENS-HMV' is an invalid segment."); }
	if (route.includes("NADMA CANEL") { errors.push("'NADMA-CANEL' is an invalid segment."); }
	if (route.includes("NADMA ALSAK") { errors.push("'NADMA-ALSAK' is an invalid segment."); }
	if (route.includes("NADMA DARUB") { errors.push("'NADMA-DARUB' is an invalid segment."); }
	if (route.includes("NADMA MUSVA") { errors.push("'NADMA-MUSVA' is an invalid segment."); }
	if (route.includes("NADMA BEPEG") { errors.push("'NADMA-BEPEG' is an invalid segment."); }
	if (route.includes("NADMA SINVU") { errors.push("'NADMA-SINVU' is an invalid segment."); }
	if (route.includes("NADMA BOPUT") { errors.push("'NADMA-BOPUT' is an invalid segment."); }
	if (route.includes("NADMA EPMAN") { errors.push("'NADMA-EPMAN' is an invalid segment."); }
	if (route.includes("NADMA YFB") { errors.push("'NADMA-YFB' is an invalid segment."); }
	if (route.includes("NADMA BERUS") { errors.push("'NADMA-BERUS' is an invalid segment."); }
	if (route.includes("NADMA DAPAK") { errors.push("'NADMA-DAPAK' is an invalid segment."); }
	if (route.includes("NADMA TEFFO") { errors.push("'NADMA-TEFFO' is an invalid segment."); }
	if (route.includes("NAGOR SELEK") { errors.push("'NAGOR-SELEK' is an invalid segment."); }
	if (route.includes("NARMI COPPI") { errors.push("'NARMI-COPPI' is an invalid segment."); }
	if (route.includes("NLD SAT") { errors.push("'NLD-SAT' is an invalid segment."); }
	if (route.includes("NLD LRD") { errors.push("'NLD-LRD' is an invalid segment."); }
	if (route.includes("NLD PNG") { errors.push("'NLD-PNG' is an invalid segment."); }
	if (route.includes("NOCHI SSO") { errors.push("'NOCHI-SSO' is an invalid segment."); }
	if (route.includes("NODLE RNICH") { errors.push("'NODLE-RNICH' is an invalid segment."); }
	if (route.includes("NOVOG BZE") { errors.push("'NOVOG-BZE' is an invalid segment."); }
	if (route.includes("NOWAA SSM") { errors.push("'NOWAA-SSM' is an invalid segment."); }
	if (route.includes("NUCRE DIVKU") { errors.push("'NUCRE-DIVKU' is an invalid segment."); }
	if (route.includes("NULUK EDWAL") { errors.push("'NULUK-EDWAL' is an invalid segment."); }
	if (route.includes("NUMBR SHEIL") { errors.push("'NUMBR-SHEIL' is an invalid segment."); }
	if (route.includes("OAK MLF") { errors.push("'OAK-MLF' is an invalid segment."); }
	if (route.includes("OAL KATTS") { errors.push("'OAL-KATTS' is an invalid segment."); }
	if (route.includes("ODESA DQO") { errors.push("'ODESA-DQO' is an invalid segment."); }
	if (route.includes("OFORD EDWAL") { errors.push("'OFORD-EDWAL' is an invalid segment."); }
	if (route.includes("OGGRE OZENA") { errors.push("'OGGRE-OZENA' is an invalid segment."); }
	if (route.includes("OHARA FST") { errors.push("'OHARA-FST' is an invalid segment."); }
	if (route.includes("OHPEA OXANA") { errors.push("'OHPEA-OXANA' is an invalid segment."); }
	if (route.includes("OLABA LYH") { errors.push("'OLABA-LYH' is an invalid segment."); }
	if (route.includes("OLESI CUL") { errors.push("'OLESI-CUL' is an invalid segment."); }
	if (route.includes("OLESI CUU") { errors.push("'OLESI-CUU' is an invalid segment."); }
	if (route.includes("OLESI UKSEL") { errors.push("'OLESI-UKSEL' is an invalid segment."); }
	if (route.includes("OLGAT ABESI") { errors.push("'OLGAT-ABESI' is an invalid segment."); }
	if (route.includes("OMN DDANY") { errors.push("'OMN-DDANY' is an invalid segment."); }
	if (route.includes("OMTOL NEEKO") { errors.push("'OMTOL-NEEKO' is an invalid segment."); }
	if (route.includes("OMTOL SSM") { errors.push("'OMTOL-SSM' is an invalid segment."); }
	if (route.includes("OMTOL YWK") { errors.push("'OMTOL-YWK' is an invalid segment."); }
	if (route.includes("OMTOL JANJO") { errors.push("'OMTOL-JANJO' is an invalid segment."); }
	if (route.includes("OMTOL LOMSI") { errors.push("'OMTOL-LOMSI' is an invalid segment."); }
	if (route.includes("OMTOL HOIST") { errors.push("'OMTOL-HOIST' is an invalid segment."); }
	if (route.includes("ONEIL HEDMI") { errors.push("'ONEIL-HEDMI' is an invalid segment."); }
	if (route.includes("ONGOT PADRK") { errors.push("'ONGOT-PADRK' is an invalid segment."); }
	if (route.includes("ORF CLAPY") { errors.push("'ORF-CLAPY' is an invalid segment."); }
	if (route.includes("ORL DDANY") { errors.push("'ORL-DDANY' is an invalid segment."); }
	if (route.includes("ORNAI SIMLU") { errors.push("'ORNAI-SIMLU' is an invalid segment."); }
	if (route.includes("OTEKO ATUVI") { errors.push("'OTEKO-ATUVI' is an invalid segment."); }
	if (route.includes("OTT RAVNN") { errors.push("'OTT-RAVNN' is an invalid segment."); }
	if (route.includes("OVR BRIDJ") { errors.push("'OVR-BRIDJ' is an invalid segment."); }
	if (route.includes("OVR UKIPE") { errors.push("'OVR-UKIPE' is an invalid segment."); }
	if (route.includes("OVR MIDLE") { errors.push("'OVR-MIDLE' is an invalid segment."); }
	if (route.includes("OZENA NUCAR") { errors.push("'OZENA-NUCAR' is an invalid segment."); }
	if (route.includes("PAE EGRET") { errors.push("'PAE-EGRET' is an invalid segment."); }
	if (route.includes("PAE BEZOV") { errors.push("'PAE-BEZOV' is an invalid segment."); }
	if (route.includes("PAE BEZOV") { errors.push("'PAE-BEZOV' is an invalid segment."); }
	if (route.includes("PAE AMEKE") { errors.push("'PAE-AMEKE' is an invalid segment."); }
	if (route.includes("PALCO SJU") { errors.push("'PALCO-SJU' is an invalid segment."); }
	if (route.includes("PALCO PALDE") { errors.push("'PALCO-PALDE' is an invalid segment."); }
	if (route.includes("PALDE DONQU") { errors.push("'PALDE-DONQU' is an invalid segment."); }
	if (route.includes("PEAKY DVALL") { errors.push("'PEAKY-DVALL' is an invalid segment."); }
	if (route.includes("PELTU BAREE") { errors.push("'PELTU-BAREE' is an invalid segment."); }
	if (route.includes("PELTU BRADD") { errors.push("'PELTU-BRADD' is an invalid segment."); }
	if (route.includes("PENYT SLATN") { errors.push("'PENYT-SLATN' is an invalid segment."); }
	if (route.includes("PICHR HOMRR") { errors.push("'PICHR-HOMRR' is an invalid segment."); }
	if (route.includes("PICHR DBACK") { errors.push("'PICHR-DBACK' is an invalid segment."); }
	if (route.includes("PIDSO LAKES") { errors.push("'PIDSO-LAKES' is an invalid segment."); }
	if (route.includes("PIDSO ALSOP") { errors.push("'PIDSO-ALSOP' is an invalid segment."); }
	if (route.includes("PIREX LAZEY") { errors.push("'PIREX-LAZEY' is an invalid segment."); }
	if (route.includes("PIREX AMENO") { errors.push("'PIREX-AMENO' is an invalid segment."); }
	if (route.includes("PIREX ANTIG") { errors.push("'PIREX-ANTIG' is an invalid segment."); }
	if (route.includes("PIREX BALOO") { errors.push("'PIREX-BALOO' is an invalid segment."); }
	if (route.includes("PIREX JIMAC") { errors.push("'PIREX-JIMAC' is an invalid segment."); }
	if (route.includes("PIREX ENAPI") { errors.push("'PIREX-ENAPI' is an invalid segment."); }
	if (route.includes("PIREX NUMBR") { errors.push("'PIREX-NUMBR' is an invalid segment."); }
	if (route.includes("PIREX WINGZ") { errors.push("'PIREX-WINGZ' is an invalid segment."); }
	if (route.includes("PIREX BOVIC") { errors.push("'PIREX-BOVIC' is an invalid segment."); }
	if (route.includes("PIREX ANVER") { errors.push("'PIREX-ANVER' is an invalid segment."); }
	if (route.includes("PIREX DASER") { errors.push("'PIREX-DASER' is an invalid segment."); }
	if (route.includes("PIREX BALTN") { errors.push("'PIREX-BALTN' is an invalid segment."); }
	if (route.includes("PIREX SHEIL") { errors.push("'PIREX-SHEIL' is an invalid segment."); }
	if (route.includes("PITMN GEJYU") { errors.push("'PITMN-GEJYU' is an invalid segment."); }
	if (route.includes("PMM SAMME") { errors.push("'PMM-SAMME' is an invalid segment."); }
	if (route.includes("POKEG KOBET") { errors.push("'POKEG-KOBET' is an invalid segment."); }
	if (route.includes("PORTI LOMPI") { errors.push("'PORTI-LOMPI' is an invalid segment."); }
	if (route.includes("PORTI SUTKO") { errors.push("'PORTI-SUTKO' is an invalid segment."); }
	if (route.includes("PORTI JAROM") { errors.push("'PORTI-JAROM' is an invalid segment."); }
	if (route.includes("PQI SYR") { errors.push("'PQI-SYR' is an invalid segment."); }
	if (route.includes("PQI MIILS") { errors.push("'PQI-MIILS' is an invalid segment."); }
	if (route.includes("PSFIC EYNOR") { errors.push("'PSFIC-EYNOR' is an invalid segment."); }
	if (route.includes("ORNAI DOLFF") { errors.push("'ORNAI-DOLFF' is an invalid segment."); }
	if (route.includes("ORNAI WIDEN") { errors.push("'ORNAI-WIDEN' is an invalid segment."); }
	if (route.includes("ORNAI SEDAR") { errors.push("'ORNAI-SEDAR' is an invalid segment."); }
	if (route.includes("OSOVO ELABU") { errors.push("'OSOVO-ELABU' is an invalid segment."); }
	if (route.includes("OST NOSTO") { errors.push("'OST-NOSTO' is an invalid segment."); }
	if (route.includes("OTONA ODAXY") { errors.push("'OTONA-ODAXY' is an invalid segment."); }
	if (route.includes("OTUPA GRN") { errors.push("'OTUPA-GRN' is an invalid segment."); }
	if (route.includes("OTUPA IMUSU") { errors.push("'OTUPA-IMUSU' is an invalid segment."); }
	if (route.includes("OWENZ PREPI") { errors.push("'OWENZ-PREPI' is an invalid segment."); }
	if (route.includes("OWENZ RBV") { errors.push("'OWENZ-RBV' is an invalid segment."); }
	if (route.includes("OYSTY ZYDCO") { errors.push("'OYSTY-ZYDCO' is an invalid segment."); }
	if (route.includes("PADUD DIBAM") { errors.push("'PADUD-DIBAM' is an invalid segment."); }
	if (route.includes("PALCO THANK") { errors.push("'PALCO-THANK' is an invalid segment."); }
	if (route.includes("PALCO KOLAO") { errors.push("'PALCO-KOLAO' is an invalid segment."); }
	if (route.includes("PALCO VORCE") { errors.push("'PALCO-VORCE' is an invalid segment."); }
	if (route.includes("PALDE FEKKO") { errors.push("'PALDE-FEKKO' is an invalid segment."); }
	if (route.includes("PARIS FITES") { errors.push("'PARIS-FITES' is an invalid segment."); }
	if (route.includes("PASAS DESAT") { errors.push("'PASAS-DESAT' is an invalid segment."); }
	if (route.includes("PDN ODK") { errors.push("'PDN-ODK' is an invalid segment."); }
	if (route.includes("PELCN ROWSY") { errors.push("'PELCN-ROWSY' is an invalid segment."); }
	if (route.includes("PENSI MCN") { errors.push("'PENSI-MCN' is an invalid segment."); }
	if (route.includes("PER TOTOE") { errors.push("'PER-TOTOE' is an invalid segment."); }
	if (route.includes("PERRY GND") { errors.push("'PERRY-GND' is an invalid segment."); }
	if (route.includes("PETSI FROST") { errors.push("'PETSI-FROST' is an invalid segment."); }
	if (route.includes("PEVAL VAKSU") { errors.push("'PEVAL-VAKSU' is an invalid segment."); }
	if (route.includes("PIA CHZHD") { errors.push("'PIA-CHZHD' is an invalid segment."); }
	if (route.includes("PIBCI TURKI") { errors.push("'PIBCI-TURKI' is an invalid segment."); }
	if (route.includes("PINTT EDWAL") { errors.push("'PINTT-EDWAL' is an invalid segment."); }
	if (route.includes("PITRW JAINS") { errors.push("'PITRW-JAINS' is an invalid segment."); }
	if (route.includes("PJG ABA") { errors.push("'PJG-ABA' is an invalid segment."); }
	if (route.includes("PKACZ TARRY") { errors.push("'PKACZ-TARRY' is an invalid segment."); }
	if (route.includes("PNG NLD") { errors.push("'PNG-NLD' is an invalid segment."); }
	if (route.includes("PNG GEEKY") { errors.push("'PNG-GEEKY' is an invalid segment."); }
	if (route.includes("PNG NOTAL") { errors.push("'PNG-NOTAL' is an invalid segment."); }
	if (route.includes("PNT CHZHD") { errors.push("'PNT-CHZHD' is an invalid segment."); }
	if (route.includes("POBSE OHIOS") { errors.push("'POBSE-OHIOS' is an invalid segment."); }
	if (route.includes("POOFF HEDMI") { errors.push("'POOFF-HEDMI' is an invalid segment."); }
	if (route.includes("POWAL CHIKI") { errors.push("'POWAL-CHIKI' is an invalid segment."); }
	if (route.includes("PQI TAFFY") { errors.push("'PQI-TAFFY' is an invalid segment."); }
	if (route.includes("PQI QUBIS") { errors.push("'PQI-QUBIS' is an invalid segment."); }
	if (route.includes("PQI ALB") { errors.push("'PQI-ALB' is an invalid segment."); }
	if (route.includes("PQI OLABA") { errors.push("'PQI-OLABA' is an invalid segment."); }
	if (route.includes("PQI CEFOU") { errors.push("'PQI-CEFOU' is an invalid segment."); }
	if (route.includes("PRONI WAALT") { errors.push("'PRONI-WAALT' is an invalid segment."); }
	if (route.includes("PSP STOWN") { errors.push("'PSP-STOWN' is an invalid segment."); }
	if (route.includes("PSP SXC") { errors.push("'PSP-SXC' is an invalid segment."); }
	if (route.includes("PSP TNP") { errors.push("'PSP-TNP' is an invalid segment."); }
	if (route.includes("PUHEE UPP") { errors.push("'PUHEE-UPP' is an invalid segment."); }
	if (route.includes("PUT EBONY") { errors.push("'PUT-EBONY' is an invalid segment."); }
	if (route.includes("PUT ALLEX") { errors.push("'PUT-ALLEX' is an invalid segment."); }
	if (route.includes("PUT WHALE") { errors.push("'PUT-WHALE' is an invalid segment."); }
	if (route.includes("PUT BRADD") { errors.push("'PUT-BRADD' is an invalid segment."); }
	if (route.includes("PXR BXK") { errors.push("'PXR-BXK' is an invalid segment."); }
	if (route.includes("PXT HAYDO") { errors.push("'PXT-HAYDO' is an invalid segment."); }
	if (route.includes("QUBIS LODBU") { errors.push("'QUBIS-LODBU' is an invalid segment."); }
	if (route.includes("QUBIS BOKTO") { errors.push("'QUBIS-BOKTO' is an invalid segment."); }
	if (route.includes("QUIRT JAC") { errors.push("'QUIRT-JAC' is an invalid segment."); }
	if (route.includes("QUIRT LAMON") { errors.push("'QUIRT-LAMON' is an invalid segment."); }
	if (route.includes("RADUN MOATT") { errors.push("'RADUN-MOATT' is an invalid segment."); }
	if (route.includes("RADUN PRAWN") { errors.push("'RADUN-PRAWN' is an invalid segment."); }
	if (route.includes("RAKAM SFK") { errors.push("'RAKAM-SFK' is an invalid segment."); }
	if (route.includes("REEDO ART") { errors.push("'REEDO-ART' is an invalid segment."); }
	if (route.includes("RELIC RUBDA") { errors.push("'RELIC-RUBDA' is an invalid segment."); }
	if (route.includes("RELIC BOBTU") { errors.push("'RELIC-BOBTU' is an invalid segment."); }
	if (route.includes("REMAL TPG") { errors.push("'REMAL-TPG' is an invalid segment."); }
	if (route.includes("RIKAL BAREE") { errors.push("'RIKAL-BAREE' is an invalid segment."); }
	if (route.includes("RIKAL BRADD") { errors.push("'RIKAL-BRADD' is an invalid segment."); }
	if (route.includes("RIMMM CORKR") { errors.push("'RIMMM-CORKR' is an invalid segment."); }
	if (route.includes("ROADE DSNEE") { errors.push("'ROADE-DSNEE' is an invalid segment."); }
	if (route.includes("ROSTO FROST") { errors.push("'ROSTO-FROST' is an invalid segment."); }
	if (route.includes("ROWIN DEREC") { errors.push("'ROWIN-DEREC' is an invalid segment."); }
	if (route.includes("RQR LSU") { errors.push("'RQR-LSU' is an invalid segment."); }
	if (route.includes("RQR LCH") { errors.push("'RQR-LCH' is an invalid segment."); }
	if (route.includes("RSK SQIRE") { errors.push("'RSK-SQIRE' is an invalid segment."); }
	if (route.includes("PSP RZS") { errors.push("'PSP-RZS' is an invalid segment."); }
	if (route.includes("PULPS FITES") { errors.push("'PULPS-FITES' is an invalid segment."); }
	if (route.includes("PUT DANOL") { errors.push("'PUT-DANOL' is an invalid segment."); }
	if (route.includes("PUT TUSKY") { errors.push("'PUT-TUSKY' is an invalid segment."); }
	if (route.includes("PUT MIILS") { errors.push("'PUT-MIILS' is an invalid segment."); }
	if (route.includes("PUT TOPPS") { errors.push("'PUT-TOPPS' is an invalid segment."); }
	if (route.includes("PUVOK MT") { errors.push("'PUVOK-MT' is an invalid segment."); }
	if (route.includes("PXN AVE") { errors.push("'PXN-AVE' is an invalid segment."); }
	if (route.includes("QUBIS KAYCC") { errors.push("'QUBIS-KAYCC' is an invalid segment."); }
	if (route.includes("QUBIS BEEKN") { errors.push("'QUBIS-BEEKN' is an invalid segment."); }
	if (route.includes("RABAL KASAR") { errors.push("'RABAL-KASAR' is an invalid segment."); }
	if (route.includes("RABAT FITES") { errors.push("'RABAT-FITES' is an invalid segment."); }
	if (route.includes("RADDS SIE") { errors.push("'RADDS-SIE' is an invalid segment."); }
	if (route.includes("RAF AAR") { errors.push("'RAF-AAR' is an invalid segment."); }
	if (route.includes("RAFIN JEBBY") { errors.push("'RAFIN-JEBBY' is an invalid segment."); }
	if (route.includes("RAMAY KHAAN") { errors.push("'RAMAY-KHAAN' is an invalid segment."); }
	if (route.includes("RANAY TANJA") { errors.push("'RANAY-TANJA' is an invalid segment."); }
	if (route.includes("RANGR TVC") { errors.push("'RANGR-TVC' is an invalid segment."); }
	if (route.includes("RANGY EBONY") { errors.push("'RANGY-EBONY' is an invalid segment."); }
	if (route.includes("RANGY TOPPS") { errors.push("'RANGY-TOPPS' is an invalid segment."); }
	if (route.includes("RANGY ALLEX") { errors.push("'RANGY-ALLEX' is an invalid segment."); }
	if (route.includes("RANGY MLT") { errors.push("'RANGY-MLT' is an invalid segment."); }
	if (route.includes("RBS CHZHD") { errors.push("'RBS-CHZHD' is an invalid segment."); }
	if (route.includes("RDU BBOBO") { errors.push("'RDU-BBOBO' is an invalid segment."); }
	if (route.includes("RENAH SLUKA") { errors.push("'RENAH-SLUKA' is an invalid segment."); }
	if (route.includes("RESIA UNTAD") { errors.push("'RESIA-UNTAD' is an invalid segment."); }
	if (route.includes("RESQU DARUX") { errors.push("'RESQU-DARUX' is an invalid segment."); }
	if (route.includes("RESQU WEBBB") { errors.push("'RESQU-WEBBB' is an invalid segment."); }
	if (route.includes("REX ICEMN") { errors.push("'REX-ICEMN' is an invalid segment."); }
	if (route.includes("REX MFE") { errors.push("'REX-MFE' is an invalid segment."); }
	if (route.includes("REX COT") { errors.push("'REX-COT' is an invalid segment."); }
	if (route.includes("REX LRD") { errors.push("'REX-LRD' is an invalid segment."); }
	if (route.includes("RIFFT MALIT") { errors.push("'RIFFT-MALIT' is an invalid segment."); }
	if (route.includes("RIFLE HEADI") { errors.push("'RIFLE-HEADI' is an invalid segment."); }
	if (route.includes("RIGVA PINDO") { errors.push("'RIGVA-PINDO' is an invalid segment."); }
	if (route.includes("RIZIN FLW") { errors.push("'RIZIN-FLW' is an invalid segment."); }
	if (route.includes("RIZIN MQO") { errors.push("'RIZIN-MQO' is an invalid segment."); }
	if (route.includes("RNGRS JIMAC") { errors.push("'RNGRS-JIMAC' is an invalid segment."); }
	if (route.includes("ROARR LYONS") { errors.push("'ROARR-LYONS' is an invalid segment."); }
	if (route.includes("ROD CXR") { errors.push("'ROD-CXR' is an invalid segment."); }
	if (route.includes("ROGSA POLTY") { errors.push("'ROGSA-POLTY' is an invalid segment."); }
	if (route.includes("ROLLS TOTOE") { errors.push("'ROLLS-TOTOE' is an invalid segment."); }
	if (route.includes("ROSAH UPP") { errors.push("'ROSAH-UPP' is an invalid segment."); }
	if (route.includes("ROSIN TONYS") { errors.push("'ROSIN-TONYS' is an invalid segment."); }
	if (route.includes("ROTGI RAKAR") { errors.push("'ROTGI-RAKAR' is an invalid segment."); }
	if (route.includes("ROTGI XOPGI") { errors.push("'ROTGI-XOPGI' is an invalid segment."); }
	if (route.includes("ROTNA DAGUD") { errors.push("'ROTNA-DAGUD' is an invalid segment."); }
	if (route.includes("ROUND TVC") { errors.push("'ROUND-TVC' is an invalid segment."); }
	if (route.includes("ROUND POLTY") { errors.push("'ROUND-POLTY' is an invalid segment."); }
	if (route.includes("ROUND SSM") { errors.push("'ROUND-SSM' is an invalid segment."); }
	if (route.includes("RUBAY COORZ") { errors.push("'RUBAY-COORZ' is an invalid segment."); }
	if (route.includes("RUDLI KASAR") { errors.push("'RUDLI-KASAR' is an invalid segment."); }
	if (route.includes("RUMFO WRECK") { errors.push("'RUMFO-WRECK' is an invalid segment."); }
	if (route.includes("SAMME PMM") { errors.push("'SAMME-PMM' is an invalid segment."); }
	if (route.includes("SAVRY PRAWN") { errors.push("'SAVRY-PRAWN' is an invalid segment."); }
	if (route.includes("SAXAN BAREE") { errors.push("'SAXAN-BAREE' is an invalid segment."); }
	if (route.includes("SAXAN BRADD") { errors.push("'SAXAN-BRADD' is an invalid segment."); }
	if (route.includes("SBI CRIED") { errors.push("'SBI-CRIED' is an invalid segment."); }
	if (route.includes("SEARS YARNS") { errors.push("'SEARS-YARNS' is an invalid segment."); }
	if (route.includes("SEAVR LAZEY") { errors.push("'SEAVR-LAZEY' is an invalid segment."); }
	if (route.includes("SEAVR GECAL") { errors.push("'SEAVR-GECAL' is an invalid segment."); }
	if (route.includes("SEAVR SHEIL") { errors.push("'SEAVR-SHEIL' is an invalid segment."); }
	if (route.includes("SEAVR BALTN") { errors.push("'SEAVR-BALTN' is an invalid segment."); }
	if (route.includes("SEAVR BOVIC") { errors.push("'SEAVR-BOVIC' is an invalid segment."); }
	if (route.includes("SEAVR ANTIG") { errors.push("'SEAVR-ANTIG' is an invalid segment."); }
	if (route.includes("SEAVR AMENO") { errors.push("'SEAVR-AMENO' is an invalid segment."); }
	if (route.includes("SEAVR ANVER") { errors.push("'SEAVR-ANVER' is an invalid segment."); }
	if (route.includes("SEAVR ENAPI") { errors.push("'SEAVR-ENAPI' is an invalid segment."); }
	if (route.includes("SEAVR DASER") { errors.push("'SEAVR-DASER' is an invalid segment."); }
	if (route.includes("SEAVR BALOO") { errors.push("'SEAVR-BALOO' is an invalid segment."); }
	if (route.includes("SEAVR NUMBR") { errors.push("'SEAVR-NUMBR' is an invalid segment."); }
	if (route.includes("SEAVR JIMAC") { errors.push("'SEAVR-JIMAC' is an invalid segment."); }
	if (route.includes("SEAVR WINGZ") { errors.push("'SEAVR-WINGZ' is an invalid segment."); }
	if (route.includes("SEKMA ARNAL") { errors.push("'SEKMA-ARNAL' is an invalid segment."); }
	if (route.includes("SELEK IKBIX") { errors.push("'SELEK-IKBIX' is an invalid segment."); }
	if (route.includes("SEY ACK") { errors.push("'SEY-ACK' is an invalid segment."); }
	if (route.includes("SHAQQ CYY") { errors.push("'SHAQQ-CYY' is an invalid segment."); }
	if (route.includes("SHATA FOLDS") { errors.push("'SHATA-FOLDS' is an invalid segment."); }
	if (route.includes("SHEIL LAZEY") { errors.push("'SHEIL-LAZEY' is an invalid segment."); }
	if (route.includes("SHEIL NUMBR") { errors.push("'SHEIL-NUMBR' is an invalid segment."); }
	if (route.includes("SHEIL BALOO") { errors.push("'SHEIL-BALOO' is an invalid segment."); }
	if (route.includes("SHEIL BOVIC") { errors.push("'SHEIL-BOVIC' is an invalid segment."); }
	if (route.includes("SHEIL ANVER") { errors.push("'SHEIL-ANVER' is an invalid segment."); }
	if (route.includes("SHEIL DASER") { errors.push("'SHEIL-DASER' is an invalid segment."); }
	if (route.includes("SHEIL WINGZ") { errors.push("'SHEIL-WINGZ' is an invalid segment."); }
	if (route.includes("SHEIL PIREX") { errors.push("'SHEIL-PIREX' is an invalid segment."); }
	if (route.includes("SHEIL SEAVR") { errors.push("'SHEIL-SEAVR' is an invalid segment."); }
	if (route.includes("SHEIL ENAPI") { errors.push("'SHEIL-ENAPI' is an invalid segment."); }
	if (route.includes("SHLEP YAHOO") { errors.push("'SHLEP-YAHOO' is an invalid segment."); }
	if (route.includes("SHPRD SOT") { errors.push("'SHPRD-SOT' is an invalid segment."); }
	if (route.includes("SIE ACK") { errors.push("'SIE-ACK' is an invalid segment."); }
	if (route.includes("SIGIR ASIKO") { errors.push("'SIGIR-ASIKO' is an invalid segment."); }
	if (route.includes("SIMLU ORNAI") { errors.push("'SIMLU-ORNAI' is an invalid segment."); }
	if (route.includes("SINGA MAXAR") { errors.push("'SINGA-MAXAR' is an invalid segment."); }
	if (route.includes("SJI BAGGS") { errors.push("'SJI-BAGGS' is an invalid segment."); }
	if (route.includes("RUTOM ALG") { errors.push("'RUTOM-ALG' is an invalid segment."); }
	if (route.includes("RYANN YERIN") { errors.push("'RYANN-YERIN' is an invalid segment."); }
	if (route.includes("SAGGY RANAY") { errors.push("'SAGGY-RANAY' is an invalid segment."); }
	if (route.includes("SAGGY WAVES") { errors.push("'SAGGY-WAVES' is an invalid segment."); }
	if (route.includes("SAILE JAWZZ") { errors.push("'SAILE-JAWZZ' is an invalid segment."); }
	if (route.includes("SAT NLD") { errors.push("'SAT-NLD' is an invalid segment."); }
	if (route.includes("SAVIK YAALE") { errors.push("'SAVIK-YAALE' is an invalid segment."); }
	if (route.includes("SAVRY LOMTA") { errors.push("'SAVRY-LOMTA' is an invalid segment."); }
	if (route.includes("SAW OVETE") { errors.push("'SAW-OVETE' is an invalid segment."); }
	if (route.includes("SAXAN ANATI") { errors.push("'SAXAN-ANATI' is an invalid segment."); }
	if (route.includes("SAYTO SCOON") { errors.push("'SAYTO-SCOON' is an invalid segment."); }
	if (route.includes("SBI STUFT") { errors.push("'SBI-STUFT' is an invalid segment."); }
	if (route.includes("SCOON SAYTO") { errors.push("'SCOON-SAYTO' is an invalid segment."); }
	if (route.includes("SCOON FEKIK") { errors.push("'SCOON-FEKIK' is an invalid segment."); }
	if (route.includes("SCTRR AVE") { errors.push("'SCTRR-AVE' is an invalid segment."); }
	if (route.includes("SEBON ZARCO") { errors.push("'SEBON-ZARCO' is an invalid segment."); }
	if (route.includes("SEELO OKNEE") { errors.push("'SEELO-OKNEE' is an invalid segment."); }
	if (route.includes("SEELO OGGRE") { errors.push("'SEELO-OGGRE' is an invalid segment."); }
	if (route.includes("SEKMA OSUPA") { errors.push("'SEKMA-OSUPA' is an invalid segment."); }
	if (route.includes("SELBO POLTY") { errors.push("'SELBO-POLTY' is an invalid segment."); }
	if (route.includes("SELBO SSM") { errors.push("'SELBO-SSM' is an invalid segment."); }
	if (route.includes("SELEK MELDA") { errors.push("'SELEK-MELDA' is an invalid segment."); }
	if (route.includes("SGF TOTOE") { errors.push("'SGF-TOTOE' is an invalid segment."); }
	if (route.includes("SHALO TURKI") { errors.push("'SHALO-TURKI' is an invalid segment."); }
	if (route.includes("SHENU ENI") { errors.push("'SHENU-ENI' is an invalid segment."); }
	if (route.includes("SHENU RBL") { errors.push("'SHENU-RBL' is an invalid segment."); }
	if (route.includes("SHENU AMAKR") { errors.push("'SHENU-AMAKR' is an invalid segment."); }
	if (route.includes("SHOES TURKI") { errors.push("'SHOES-TURKI' is an invalid segment."); }
	if (route.includes("SHUMY VLA") { errors.push("'SHUMY-VLA' is an invalid segment."); }
	if (route.includes("SIE LAFLN") { errors.push("'SIE-LAFLN' is an invalid segment."); }
	if (route.includes("SIFRA CBULA") { errors.push("'SIFRA-CBULA' is an invalid segment."); }
	if (route.includes("SIGUN ISOKO") { errors.push("'SIGUN-ISOKO' is an invalid segment."); }
	if (route.includes("SIMAN MORLI") { errors.push("'SIMAN-MORLI' is an invalid segment."); }
	if (route.includes("SIMAN AROVI") { errors.push("'SIMAN-AROVI' is an invalid segment."); }
	if (route.includes("SIMSU KATCH") { errors.push("'SIMSU-KATCH' is an invalid segment."); }
	if (route.includes("SIMTO POLTY") { errors.push("'SIMTO-POLTY' is an invalid segment."); }
	if (route.includes("SINOB KOPGI") { errors.push("'SINOB-KOPGI' is an invalid segment."); }
	if (route.includes("SJI IZAAC") { errors.push("'SJI-IZAAC' is an invalid segment."); }
	if (route.includes("SJU ACONY") { errors.push("'SJU-ACONY' is an invalid segment."); }
	if (route.includes("SLATN PENYT") { errors.push("'SLATN-PENYT' is an invalid segment."); }
	if (route.includes("SLATN ISLES") { errors.push("'SLATN-ISLES' is an invalid segment."); }
	if (route.includes("SLATN ACK") { errors.push("'SLATN-ACK' is an invalid segment."); }
	if (route.includes("SLATN ZIBUT") { errors.push("'SLATN-ZIBUT' is an invalid segment."); }
	if (route.includes("SLATN SOORY") { errors.push("'SLATN-SOORY' is an invalid segment."); }
	if (route.includes("SLATN JENYY") { errors.push("'SLATN-JENYY' is an invalid segment."); }
	if (route.includes("SLATN YAALE") { errors.push("'SLATN-YAALE' is an invalid segment."); }
	if (route.includes("SLATN MUNEY") { errors.push("'SLATN-MUNEY' is an invalid segment."); }
	if (route.includes("SLATN LYNUS") { errors.push("'SLATN-LYNUS' is an invalid segment."); }
	if (route.includes("SLUGO ANU") { errors.push("'SLUGO-ANU' is an invalid segment."); }
	if (route.includes("SLUKA POKEG") { errors.push("'SLUKA-POKEG' is an invalid segment."); }
	if (route.includes("SQS FTZ") { errors.push("'SQS-FTZ' is an invalid segment."); }
	if (route.includes("SRQ LEV") { errors.push("'SRQ-LEV' is an invalid segment."); }
	if (route.includes("SRQ SJI") { errors.push("'SRQ-SJI' is an invalid segment."); }
	if (route.includes("SRQ HRV") { errors.push("'SRQ-HRV' is an invalid segment."); }
	if (route.includes("SSKEE INW") { errors.push("'SSKEE-INW' is an invalid segment."); }
	if (route.includes("SSKEE GUP") { errors.push("'SSKEE-GUP' is an invalid segment."); }
	if (route.includes("SSR IGSOM") { errors.push("'SSR-IGSOM' is an invalid segment."); }
	if (route.includes("STAFE SSM") { errors.push("'STAFE-SSM' is an invalid segment."); }
	if (route.includes("STJ UBELE") { errors.push("'STJ-UBELE' is an invalid segment."); }
	if (route.includes("STOWN TNP") { errors.push("'STOWN-TNP' is an invalid segment."); }
	if (route.includes("SUBKY LVM") { errors.push("'SUBKY-LVM' is an invalid segment."); }
	if (route.includes("SUPRY RUBDA") { errors.push("'SUPRY-RUBDA' is an invalid segment."); }
	if (route.includes("SUPRY PEPRA") { errors.push("'SUPRY-PEPRA' is an invalid segment."); }
	if (route.includes("SYR PQI") { errors.push("'SYR-PQI' is an invalid segment."); }
	if (route.includes("SYR FQM") { errors.push("'SYR-FQM' is an invalid segment."); }
	if (route.includes("TAFFY RIKAL") { errors.push("'TAFFY-RIKAL' is an invalid segment."); }
	if (route.includes("TAGIB TRC") { errors.push("'TAGIB-TRC' is an invalid segment."); }
	if (route.includes("TAGIB KATLA") { errors.push("'TAGIB-KATLA' is an invalid segment."); }
	if (route.includes("TALAG TNT") { errors.push("'TALAG-TNT' is an invalid segment."); }
	if (route.includes("TBC SQIRE") { errors.push("'TBC-SQIRE' is an invalid segment."); }
	if (route.includes("TETAG YEA") { errors.push("'TETAG-YEA' is an invalid segment."); }
	if (route.includes("THS JCT") { errors.push("'THS-JCT' is an invalid segment."); }
	if (route.includes("TKA GKN") { errors.push("'TKA-GKN' is an invalid segment."); }
	if (route.includes("TKA MCG") { errors.push("'TKA-MCG' is an invalid segment."); }
	if (route.includes("SKOTT GOGGS") { errors.push("'SKOTT-GOGGS' is an invalid segment."); }
	if (route.includes("SLATN STOOG") { errors.push("'SLATN-STOOG' is an invalid segment."); }
	if (route.includes("SLEEK BGD") { errors.push("'SLEEK-BGD' is an invalid segment."); }
	if (route.includes("SLUKA MNDEZ") { errors.push("'SLUKA-MNDEZ' is an invalid segment."); }
	if (route.includes("SNAGY SWRTZ") { errors.push("'SNAGY-SWRTZ' is an invalid segment."); }
	if (route.includes("SOCAN SSM") { errors.push("'SOCAN-SSM' is an invalid segment."); }
	if (route.includes("SOORY DRYED") { errors.push("'SOORY-DRYED' is an invalid segment."); }
	if (route.includes("SOORY MEANO") { errors.push("'SOORY-MEANO' is an invalid segment."); }
	if (route.includes("SOTHH BANKR") { errors.push("'SOTHH-BANKR' is an invalid segment."); }
	if (route.includes("SPADE TURKI") { errors.push("'SPADE-TURKI' is an invalid segment."); }
	if (route.includes("SPY EDWAL") { errors.push("'SPY-EDWAL' is an invalid segment."); }
	if (route.includes("SSM YTS") { errors.push("'SSM-YTS' is an invalid segment."); }
	if (route.includes("SSM OVETE") { errors.push("'SSM-OVETE' is an invalid segment."); }
	if (route.includes("STAFE TVC") { errors.push("'STAFE-TVC' is an invalid segment."); }
	if (route.includes("STEIN TEGRI") { errors.push("'STEIN-TEGRI' is an invalid segment."); }
	if (route.includes("STG MUDOS") { errors.push("'STG-MUDOS' is an invalid segment."); }
	if (route.includes("STL CHZHD") { errors.push("'STL-CHZHD' is an invalid segment."); }
	if (route.includes("STNGA MLU") { errors.push("'STNGA-MLU' is an invalid segment."); }
	if (route.includes("STOOG MOUGH") { errors.push("'STOOG-MOUGH' is an invalid segment."); }
	if (route.includes("STRRM BASER") { errors.push("'STRRM-BASER' is an invalid segment."); }
	if (route.includes("SUNEV OST") { errors.push("'SUNEV-OST' is an invalid segment."); }
	if (route.includes("SUSTI CTG") { errors.push("'SUSTI-CTG' is an invalid segment."); }
	if (route.includes("SWB GAWKS") { errors.push("'SWB-GAWKS' is an invalid segment."); }
	if (route.includes("SWEEP FITES") { errors.push("'SWEEP-FITES' is an invalid segment."); }
	if (route.includes("SWL SKARP") { errors.push("'SWL-SKARP' is an invalid segment."); }
	if (route.includes("SYR CABCI") { errors.push("'SYR-CABCI' is an invalid segment."); }
	if (route.includes("TADIS SSM") { errors.push("'TADIS-SSM' is an invalid segment."); }
	if (route.includes("TADIS DLH") { errors.push("'TADIS-DLH' is an invalid segment."); }
	if (route.includes("TAFFY KAYCC") { errors.push("'TAFFY-KAYCC' is an invalid segment."); }
	if (route.includes("TAFFY BEEKN") { errors.push("'TAFFY-BEEKN' is an invalid segment."); }
	if (route.includes("TAGUM ECK") { errors.push("'TAGUM-ECK' is an invalid segment."); }
	if (route.includes("TAGUM LETAK") { errors.push("'TAGUM-LETAK' is an invalid segment."); }
	if (route.includes("TALNO SYR") { errors.push("'TALNO-SYR' is an invalid segment."); }
	if (route.includes("TARRY DQN") { errors.push("'TARRY-DQN' is an invalid segment."); }
	if (route.includes("TARRY CVG") { errors.push("'TARRY-CVG' is an invalid segment."); }
	if (route.includes("TBE EZEEE") { errors.push("'TBE-EZEEE' is an invalid segment."); }
	if (route.includes("TEALS CUDDY") { errors.push("'TEALS-CUDDY' is an invalid segment."); }
	if (route.includes("TECKY AVE") { errors.push("'TECKY-AVE' is an invalid segment."); }
	if (route.includes("TED NODLE") { errors.push("'TED-NODLE' is an invalid segment."); }
	if (route.includes("TEGRI BULEN") { errors.push("'TEGRI-BULEN' is an invalid segment."); }
	if (route.includes("TGOLD PXR") { errors.push("'TGOLD-PXR' is an invalid segment."); }
	if (route.includes("TGOLD ONM") { errors.push("'TGOLD-ONM' is an invalid segment."); }
	if (route.includes("THRSR VLKNN") { errors.push("'THRSR-VLKNN' is an invalid segment."); }
	if (route.includes("THRSR BESTT") { errors.push("'THRSR-BESTT' is an invalid segment."); }
	if (route.includes("TOVAD GKN") { errors.push("'TOVAD-GKN' is an invalid segment."); }
	if (route.includes("TOXIT PRAWN") { errors.push("'TOXIT-PRAWN' is an invalid segment."); }
	if (route.includes("TOXIT YDP") { errors.push("'TOXIT-YDP' is an invalid segment."); }
	if (route.includes("TOXIT MOATT") { errors.push("'TOXIT-MOATT' is an invalid segment."); }
	if (route.includes("TOXIT LOMTA") { errors.push("'TOXIT-LOMTA' is an invalid segment."); }
	if (route.includes("TRM SHADI") { errors.push("'TRM-SHADI' is an invalid segment."); }
	if (route.includes("TRM BALDI") { errors.push("'TRM-BALDI' is an invalid segment."); }
	if (route.includes("TRM CORLA") { errors.push("'TRM-CORLA' is an invalid segment."); }
	if (route.includes("TUDEP BAREE") { errors.push("'TUDEP-BAREE' is an invalid segment."); }
	if (route.includes("TUDEP BRADD") { errors.push("'TUDEP-BRADD' is an invalid segment."); }
	if (route.includes("TUGET PIKRO") { errors.push("'TUGET-PIKRO' is an invalid segment."); }
	if (route.includes("TUL LOPPY") { errors.push("'TUL-LOPPY' is an invalid segment."); }
	if (route.includes("TUNTO GUMBO") { errors.push("'TUNTO-GUMBO' is an invalid segment."); }
	if (route.includes("TUSKY NANSO") { errors.push("'TUSKY-NANSO' is an invalid segment."); }
	if (route.includes("TUTOV RAMOB") { errors.push("'TUTOV-RAMOB' is an invalid segment."); }
	if (route.includes("UCA CFB") { errors.push("'UCA-CFB' is an invalid segment."); }
	if (route.includes("UCA EMABU") { errors.push("'UCA-EMABU' is an invalid segment."); }
	if (route.includes("UDGUV CZA") { errors.push("'UDGUV-CZA' is an invalid segment."); }
	if (route.includes("UKIPE BRIDJ") { errors.push("'UKIPE-BRIDJ' is an invalid segment."); }
	if (route.includes("UKIPE PSFIC") { errors.push("'UKIPE-PSFIC' is an invalid segment."); }
	if (route.includes("UMESI BAREE") { errors.push("'UMESI-BAREE' is an invalid segment."); }
	if (route.includes("UNK ICEEE") { errors.push("'UNK-ICEEE' is an invalid segment."); }
	if (route.includes("URSUS UCA") { errors.push("'URSUS-UCA' is an invalid segment."); }
	if (route.includes("URTAK BAREE") { errors.push("'URTAK-BAREE' is an invalid segment."); }
	if (route.includes("UTAHS ELMUC") { errors.push("'UTAHS-ELMUC' is an invalid segment."); }
	if (route.includes("VALLY NUCAR") { errors.push("'VALLY-NUCAR' is an invalid segment."); }
	if (route.includes("VALLY SUMRS") { errors.push("'VALLY-SUMRS' is an invalid segment."); }
	if (route.includes("VANSI STAFE") { errors.push("'VANSI-STAFE' is an invalid segment."); }
	if (route.includes("VANSI GRAMP") { errors.push("'VANSI-GRAMP' is an invalid segment."); }
	if (route.includes("VESMI BAREE") { errors.push("'VESMI-BAREE' is an invalid segment."); }
	if (route.includes("VIH LUYIR") { errors.push("'VIH-LUYIR' is an invalid segment."); }
	if (route.includes("VINKA EMOSA") { errors.push("'VINKA-EMOSA' is an invalid segment."); }
	if (route.includes("VIS DETOX") { errors.push("'VIS-DETOX' is an invalid segment."); }
	if (route.includes("VITOL RAFIN") { errors.push("'VITOL-RAFIN' is an invalid segment."); }
	if (route.includes("VLR MOT") { errors.push("'VLR-MOT' is an invalid segment."); }
	if (route.includes("TLA GOMUP") { errors.push("'TLA-GOMUP' is an invalid segment."); }
	if (route.includes("TOPPS YUL") { errors.push("'TOPPS-YUL' is an invalid segment."); }
	if (route.includes("TOPPS KAYCC") { errors.push("'TOPPS-KAYCC' is an invalid segment."); }
	if (route.includes("TOPPS JAROM") { errors.push("'TOPPS-JAROM' is an invalid segment."); }
	if (route.includes("TORIL SIRIL") { errors.push("'TORIL-SIRIL' is an invalid segment."); }
	if (route.includes("TOTOE ICT") { errors.push("'TOTOE-ICT' is an invalid segment."); }
	if (route.includes("TOTOE FTI") { errors.push("'TOTOE-FTI' is an invalid segment."); }
	if (route.includes("TOTOE CIM") { errors.push("'TOTOE-CIM' is an invalid segment."); }
	if (route.includes("TOTOE LAA") { errors.push("'TOTOE-LAA' is an invalid segment."); }
	if (route.includes("TOXAL CABCI") { errors.push("'TOXAL-CABCI' is an invalid segment."); }
	if (route.includes("TTH DELCO") { errors.push("'TTH-DELCO' is an invalid segment."); }
	if (route.includes("TUDAN TVC") { errors.push("'TUDAN-TVC' is an invalid segment."); }
	if (route.includes("TUSKY OOSHN") { errors.push("'TUSKY-OOSHN' is an invalid segment."); }
	if (route.includes("TYI OHPEA") { errors.push("'TYI-OHPEA' is an invalid segment."); }
	if (route.includes("UCJ ALTIB") { errors.push("'UCJ-ALTIB' is an invalid segment."); }
	if (route.includes("UDMUG LETAK") { errors.push("'UDMUG-LETAK' is an invalid segment."); }
	if (route.includes("UKBEV GERTU") { errors.push("'UKBEV-GERTU' is an invalid segment."); }
	if (route.includes("UKOKA HOAGG") { errors.push("'UKOKA-HOAGG' is an invalid segment."); }
	if (route.includes("UKUXA CUU") { errors.push("'UKUXA-CUU' is an invalid segment."); }
	if (route.includes("UKUXA CUL") { errors.push("'UKUXA-CUL' is an invalid segment."); }
	if (route.includes("UKUXA OLESI") { errors.push("'UKUXA-OLESI' is an invalid segment."); }
	if (route.includes("UKUXA PELSA") { errors.push("'UKUXA-PELSA' is an invalid segment."); }
	if (route.includes("ULQ BUTBA") { errors.push("'ULQ-BUTBA' is an invalid segment."); }
	if (route.includes("UNEKE TURKI") { errors.push("'UNEKE-TURKI' is an invalid segment."); }
	if (route.includes("UPP DEREC") { errors.push("'UPP-DEREC' is an invalid segment."); }
	if (route.includes("UPP EBBER") { errors.push("'UPP-EBBER' is an invalid segment."); }
	if (route.includes("UPP ARROW") { errors.push("'UPP-ARROW' is an invalid segment."); }
	if (route.includes("URTAK YDP") { errors.push("'URTAK-YDP' is an invalid segment."); }
	if (route.includes("VAKSU KOFER") { errors.push("'VAKSU-KOFER' is an invalid segment."); }
	if (route.includes("VANSI SSM") { errors.push("'VANSI-SSM' is an invalid segment."); }
	if (route.includes("VANSI TVC") { errors.push("'VANSI-TVC' is an invalid segment."); }
	if (route.includes("VBI DLH") { errors.push("'VBI-DLH' is an invalid segment."); }
	if (route.includes("VBI VESRU") { errors.push("'VBI-VESRU' is an invalid segment."); }
	if (route.includes("VCN WNSTN") { errors.push("'VCN-WNSTN' is an invalid segment."); }
	if (route.includes("VESRU YXL") { errors.push("'VESRU-YXL' is an invalid segment."); }
	if (route.includes("VESRU DLH") { errors.push("'VESRU-DLH' is an invalid segment."); }
	if (route.includes("VETRO TVC") { errors.push("'VETRO-TVC' is an invalid segment."); }
	if (route.includes("VIBSI PELMA") { errors.push("'VIBSI-PELMA' is an invalid segment."); }
	if (route.includes("VIBSI ANADA") { errors.push("'VIBSI-ANADA' is an invalid segment."); }
	if (route.includes("VIGRO POLTY") { errors.push("'VIGRO-POLTY' is an invalid segment."); }
	if (route.includes("VITOL RIFLE") { errors.push("'VITOL-RIFLE' is an invalid segment."); }
	if (route.includes("VITOL JAWZZ") { errors.push("'VITOL-JAWZZ' is an invalid segment."); }
	if (route.includes("VITOL OOSHN") { errors.push("'VITOL-OOSHN' is an invalid segment."); }
	if (route.includes("VIYAP CHS") { errors.push("'VIYAP-CHS' is an invalid segment."); }
	if (route.includes("VLV POLTY") { errors.push("'VLV-POLTY' is an invalid segment."); }
	if (route.includes("VODEK KENKI") { errors.push("'VODEK-KENKI' is an invalid segment."); }
	if (route.includes("VODEK COPUR") { errors.push("'VODEK-COPUR' is an invalid segment."); }
	if (route.includes("VODEK LOPVI") { errors.push("'VODEK-LOPVI' is an invalid segment."); }
	if (route.includes("VUH KELPP") { errors.push("'VUH-KELPP' is an invalid segment."); }
	if (route.includes("WACKS JVL") { errors.push("'WACKS-JVL' is an invalid segment."); }
	if (route.includes("WAVEZ OYSTY") { errors.push("'WAVEZ-OYSTY' is an invalid segment."); }
	if (route.includes("WHALE PERLU") { errors.push("'WHALE-PERLU' is an invalid segment."); }
	if (route.includes("WHATE EEN") { errors.push("'WHATE-EEN' is an invalid segment."); }
	if (route.includes("WINGZ LAZEY") { errors.push("'WINGZ-LAZEY' is an invalid segment."); }
	if (route.includes("WINGZ NUMBR") { errors.push("'WINGZ-NUMBR' is an invalid segment."); }
	if (route.includes("WINGZ PIREX") { errors.push("'WINGZ-PIREX' is an invalid segment."); }
	if (route.includes("WINGZ ANTIG") { errors.push("'WINGZ-ANTIG' is an invalid segment."); }
	if (route.includes("WINGZ BOVIC") { errors.push("'WINGZ-BOVIC' is an invalid segment."); }
	if (route.includes("WINGZ BALOO") { errors.push("'WINGZ-BALOO' is an invalid segment."); }
	if (route.includes("WINGZ ANVER") { errors.push("'WINGZ-ANVER' is an invalid segment."); }
	if (route.includes("WINGZ PIREX") { errors.push("'WINGZ-PIREX' is an invalid segment."); }
	if (route.includes("WINGZ GECAL") { errors.push("'WINGZ-GECAL' is an invalid segment."); }
	if (route.includes("WINGZ ENAPI") { errors.push("'WINGZ-ENAPI' is an invalid segment."); }
	if (route.includes("WINGZ SHEIL") { errors.push("'WINGZ-SHEIL' is an invalid segment."); }
	if (route.includes("WINGZ GECAL") { errors.push("'WINGZ-GECAL' is an invalid segment."); }
	if (route.includes("WINGZ BOVIC") { errors.push("'WINGZ-BOVIC' is an invalid segment."); }
	if (route.includes("WINGZ JIMAC") { errors.push("'WINGZ-JIMAC' is an invalid segment."); }
	if (route.includes("WINGZ JIMAC") { errors.push("'WINGZ-JIMAC' is an invalid segment."); }
	if (route.includes("WINGZ DASER") { errors.push("'WINGZ-DASER' is an invalid segment."); }
	if (route.includes("WINGZ ENAPI") { errors.push("'WINGZ-ENAPI' is an invalid segment."); }
	if (route.includes("WINGZ SOORY") { errors.push("'WINGZ-SOORY' is an invalid segment."); }
	if (route.includes("WINGZ BALTN") { errors.push("'WINGZ-BALTN' is an invalid segment."); }
	if (route.includes("WINGZ BALOO") { errors.push("'WINGZ-BALOO' is an invalid segment."); }
	if (route.includes("WINGZ SHEIL") { errors.push("'WINGZ-SHEIL' is an invalid segment."); }
	if (route.includes("WINGZ DASER") { errors.push("'WINGZ-DASER' is an invalid segment."); }
	if (route.includes("WINGZ BALTN") { errors.push("'WINGZ-BALTN' is an invalid segment."); }
	if (route.includes("WINGZ ANTIG") { errors.push("'WINGZ-ANTIG' is an invalid segment."); }
	if (route.includes("WINGZ LAZEY") { errors.push("'WINGZ-LAZEY' is an invalid segment."); }
	if (route.includes("WINGZ ANVER") { errors.push("'WINGZ-ANVER' is an invalid segment."); }
	if (route.includes("WISER GLL") { errors.push("'WISER-GLL' is an invalid segment."); }
	if (route.includes("WRING TTE") { errors.push("'WRING-TTE' is an invalid segment."); }
	if (route.includes("YAHOO KANNI") { errors.push("'YAHOO-KANNI' is an invalid segment."); }
	if (route.includes("YAHOO WHALE") { errors.push("'YAHOO-WHALE' is an invalid segment."); }
	if (route.includes("YAHOO DOVEY") { errors.push("'YAHOO-DOVEY' is an invalid segment."); }
	if (route.includes("YBC BOKTO") { errors.push("'YBC-BOKTO' is an invalid segment."); }
	if (route.includes("YBC OMALI") { errors.push("'YBC-OMALI' is an invalid segment."); }
	if (route.includes("YDP MT") { errors.push("'YDP-MT' is an invalid segment."); }
	if (route.includes("YEA GGW") { errors.push("'YEA-GGW' is an invalid segment."); }
	if (route.includes("YFB BODRA") { errors.push("'YFB-BODRA' is an invalid segment."); }
	if (route.includes("YFB DUTUM") { errors.push("'YFB-DUTUM' is an invalid segment."); }
	if (route.includes("YGP PQI") { errors.push("'YGP-PQI' is an invalid segment."); }
	if (route.includes("VODEK NALDI") { errors.push("'VODEK-NALDI' is an invalid segment."); }
	if (route.includes("VODEK DUGNO") { errors.push("'VODEK-DUGNO' is an invalid segment."); }
	if (route.includes("VODEK EPMAN") { errors.push("'VODEK-EPMAN' is an invalid segment."); }
	if (route.includes("VODEK FEDDY") { errors.push("'VODEK-FEDDY' is an invalid segment."); }
	if (route.includes("VODEK FASSA") { errors.push("'VODEK-FASSA' is an invalid segment."); }
	if (route.includes("VODEK TUNNI") { errors.push("'VODEK-TUNNI' is an invalid segment."); }
	if (route.includes("VODEK JELCO") { errors.push("'VODEK-JELCO' is an invalid segment."); }
	if (route.includes("VODIX POLTY") { errors.push("'VODIX-POLTY' is an invalid segment."); }
	if (route.includes("VOKOM KEYLO") { errors.push("'VOKOM-KEYLO' is an invalid segment."); }
	if (route.includes("VORCE CRUPE") { errors.push("'VORCE-CRUPE' is an invalid segment."); }
	if (route.includes("VUNIB OSUPA") { errors.push("'VUNIB-OSUPA' is an invalid segment."); }
	if (route.includes("WALLP ATUBI") { errors.push("'WALLP-ATUBI' is an invalid segment."); }
	if (route.includes("WALLP ELTES") { errors.push("'WALLP-ELTES' is an invalid segment."); }
	if (route.includes("WAPIO EBBER") { errors.push("'WAPIO-EBBER' is an invalid segment."); }
	if (route.includes("WAPPO ONOHI") { errors.push("'WAPPO-ONOHI' is an invalid segment."); }
	if (route.includes("WASIX ILAMA") { errors.push("'WASIX-ILAMA' is an invalid segment."); }
	if (route.includes("WASIX JALIL") { errors.push("'WASIX-JALIL' is an invalid segment."); }
	if (route.includes("WASIX MIDRA") { errors.push("'WASIX-MIDRA' is an invalid segment."); }
	if (route.includes("WASIX OTEXA") { errors.push("'WASIX-OTEXA' is an invalid segment."); }
	if (route.includes("WEBBB DARUX") { errors.push("'WEBBB-DARUX' is an invalid segment."); }
	if (route.includes("WEBBB SAVIK") { errors.push("'WEBBB-SAVIK' is an invalid segment."); }
	if (route.includes("WETRO CLAPY") { errors.push("'WETRO-CLAPY' is an invalid segment."); }
	if (route.includes("WETRO PRONI") { errors.push("'WETRO-PRONI' is an invalid segment."); }
	if (route.includes("WETRO CHIEZ") { errors.push("'WETRO-CHIEZ' is an invalid segment."); }
	if (route.includes("WETRO SKARP") { errors.push("'WETRO-SKARP' is an invalid segment."); }
	if (route.includes("WHALE RIFLE") { errors.push("'WHALE-RIFLE' is an invalid segment."); }
	if (route.includes("WHALE OOSHN") { errors.push("'WHALE-OOSHN' is an invalid segment."); }
	if (route.includes("WILYY ITCHE") { errors.push("'WILYY-ITCHE' is an invalid segment."); }
	if (route.includes("WINEY TURKI") { errors.push("'WINEY-TURKI' is an invalid segment."); }
	if (route.includes("WIPPO JALIL") { errors.push("'WIPPO-JALIL' is an invalid segment."); }
	if (route.includes("WIPPO MIDRA") { errors.push("'WIPPO-MIDRA' is an invalid segment."); }
	if (route.includes("WIPPO ILAMA") { errors.push("'WIPPO-ILAMA' is an invalid segment."); }
	if (route.includes("WOGOM SINCA") { errors.push("'WOGOM-SINCA' is an invalid segment."); }
	if (route.includes("XOPGI RAKAR") { errors.push("'XOPGI-RAKAR' is an invalid segment."); }
	if (route.includes("YBC ANCER") { errors.push("'YBC-ANCER' is an invalid segment."); }
	if (route.includes("YBC SSM") { errors.push("'YBC-SSM' is an invalid segment."); }
	if (route.includes("YBR SSM") { errors.push("'YBR-SSM' is an invalid segment."); }
	if (route.includes("YEA YEGGA") { errors.push("'YEA-YEGGA' is an invalid segment."); }
	if (route.includes("YEE SSM") { errors.push("'YEE-SSM' is an invalid segment."); }
	if (route.includes("YEGGA YEA") { errors.push("'YEGGA-YEA' is an invalid segment."); }
	if (route.includes("YEGGA YYL") { errors.push("'YEGGA-YYL' is an invalid segment."); }
	if (route.includes("YESKA RNICH") { errors.push("'YESKA-RNICH' is an invalid segment."); }
	if (route.includes("YGL SSM") { errors.push("'YGL-SSM' is an invalid segment."); }
	if (route.includes("YNA TOPPS") { errors.push("'YNA-TOPPS' is an invalid segment."); }
	if (route.includes("YPE TETAG") { errors.push("'YPE-TETAG' is an invalid segment."); }
	if (route.includes("YQL BIL") { errors.push("'YQL-BIL' is an invalid segment."); }
	if (route.includes("YVR HADER") { errors.push("'YVR-HADER' is an invalid segment."); }
	if (route.includes("YWL SODAC") { errors.push("'YWL-SODAC' is an invalid segment."); }
	if (route.includes("YXC MEKPI") { errors.push("'YXC-MEKPI' is an invalid segment."); }
	if (route.includes("YXJ GUDEN") { errors.push("'YXJ-GUDEN' is an invalid segment."); }
	if (route.includes("YXY GUDEN") { errors.push("'YXY-GUDEN' is an invalid segment."); }
	if (route.includes("YXY GAHAM") { errors.push("'YXY-GAHAM' is an invalid segment."); }
	if (route.includes("YYC ADVOX") { errors.push("'YYC-ADVOX' is an invalid segment."); }
	if (route.includes("YYD ANN") { errors.push("'YYD-ANN' is an invalid segment."); }
	if (route.includes("YYN GGW") { errors.push("'YYN-GGW' is an invalid segment."); }
	if (route.includes("YYQ BODRA") { errors.push("'YYQ-BODRA' is an invalid segment."); }
	if (route.includes("YYY YBC") { errors.push("'YYY-YBC' is an invalid segment."); }
	if (route.includes("YZV FQM") { errors.push("'YZV-FQM' is an invalid segment."); }
	if (route.includes("ZAYNE WORTO") { errors.push("'ZAYNE-WORTO' is an invalid segment."); }
	if (route.includes("ZAYNE DRK") { errors.push("'ZAYNE-DRK' is an invalid segment."); }
	if (route.includes("ZETIB BGD") { errors.push("'ZETIB-BGD' is an invalid segment."); }
	if (route.includes("ZIBUT LARGE") { errors.push("'ZIBUT-LARGE' is an invalid segment."); }
	if (route.includes("ZIBUT SOORY") { errors.push("'ZIBUT-SOORY' is an invalid segment."); }
	if (route.includes("ZUBLI LVM") { errors.push("'ZUBLI-LVM' is an invalid segment."); }
	if (route.includes("YMO TVC") { errors.push("'YMO-TVC' is an invalid segment."); }
	if (route.includes("YMO SSM") { errors.push("'YMO-SSM' is an invalid segment."); }
	if (route.includes("YONEK RNICH") { errors.push("'YONEK-RNICH' is an invalid segment."); }
	if (route.includes("YOW LETAK") { errors.push("'YOW-LETAK' is an invalid segment."); }
	if (route.includes("YQT OVETE") { errors.push("'YQT-OVETE' is an invalid segment."); }
	if (route.includes("YQT DLH") { errors.push("'YQT-DLH' is an invalid segment."); }
	if (route.includes("YSB TVC") { errors.push("'YSB-TVC' is an invalid segment."); }
	if (route.includes("YSB SSM") { errors.push("'YSB-SSM' is an invalid segment."); }
	if (route.includes("YSC POLTY") { errors.push("'YSC-POLTY' is an invalid segment."); }
	if (route.includes("YSP SSM") { errors.push("'YSP-SSM' is an invalid segment."); }
	if (route.includes("YTS TVC") { errors.push("'YTS-TVC' is an invalid segment."); }
	if (route.includes("YUL LETAK") { errors.push("'YUL-LETAK' is an invalid segment."); }
	if (route.includes("YUY POLTY") { errors.push("'YUY-POLTY' is an invalid segment."); }
	if (route.includes("YVO TVC") { errors.push("'YVO-TVC' is an invalid segment."); }
	if (route.includes("YVO ODAXY") { errors.push("'YVO-ODAXY' is an invalid segment."); }
	if (route.includes("YVO POLTY") { errors.push("'YVO-POLTY' is an invalid segment."); }
	if (route.includes("YXI POLTY") { errors.push("'YXI-POLTY' is an invalid segment."); }
	if (route.includes("YXI LETAK") { errors.push("'YXI-LETAK' is an invalid segment."); }
	if (route.includes("YXL DLH") { errors.push("'YXL-DLH' is an invalid segment."); }
	if (route.includes("YXZ TVC") { errors.push("'YXZ-TVC' is an invalid segment."); }
	if (route.includes("YYB POLTY") { errors.push("'YYB-POLTY' is an invalid segment."); }
	if (route.includes("YYB TVC") { errors.push("'YYB-TVC' is an invalid segment."); }
	if (route.includes("YYG TUSKY") { errors.push("'YYG-TUSKY' is an invalid segment."); }
	if (route.includes("YYG EBONY") { errors.push("'YYG-EBONY' is an invalid segment."); }
	if (route.includes("YYG ALLEX") { errors.push("'YYG-ALLEX' is an invalid segment."); }
	if (route.includes("YYZ TVC") { errors.push("'YYZ-TVC' is an invalid segment."); }
	if (route.includes("YZT KATCH") { errors.push("'YZT-KATCH' is an invalid segment."); }
	if (route.includes("ZALKI SSM") { errors.push("'ZALKI-SSM' is an invalid segment."); }
	if (route.includes("ZFP BAAGR") { errors.push("'ZFP-BAAGR' is an invalid segment."); }
	if (route.includes("ZIBUT MEANO") { errors.push("'ZIBUT-MEANO' is an invalid segment."); }
	if (route.includes("ZIBUT DRYED") { errors.push("'ZIBUT-DRYED' is an invalid segment."); }
	if (route.includes("ZIKAB CLUTS") { errors.push("'ZIKAB-CLUTS' is an invalid segment."); }
	if (route.includes("ZIKAB APACK") { errors.push("'ZIKAB-APACK' is an invalid segment."); }
	if (route.includes("ZOL GUNPA") { errors.push("'ZOL-GUNPA' is an invalid segment."); }
	if (route.includes("ZONLO TURKI") { errors.push("'ZONLO-TURKI' is an invalid segment."); }
	if (route.includes("ZQA DONEZ") { errors.push("'ZQA-DONEZ' is an invalid segment."); }
	return errors;
}
